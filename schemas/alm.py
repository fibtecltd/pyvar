"""schemas/alm.py — auto-generated Pydantic v2 contracts for the alm domain.

Generated by scripts/gen_p3.py from engine signatures. Request models are
precisely typed and enforce regulatory validators (CLAUDE.md s4). Response
models document engine return-dict keys; extra='allow' covers dynamically
built keys that static analysis cannot enumerate.
"""

from __future__ import annotations

from typing import Any

from pydantic import BaseModel, ConfigDict, Field, field_validator


class MacaulayDurationBalanceSheetRequest(BaseModel):
    """Request parameters for macaulay_duration_balance_sheet()."""

    cashflows: list[float] | list[list[float]]
    times: list[float] | list[list[float]]
    discount_rate: float


class MacaulayDurationBalanceSheetResponse(BaseModel):
    """Result of macaulay_duration_balance_sheet(). Mirrors the engine return dict."""

    model_config = ConfigDict(extra="allow", protected_namespaces=())

    macaulay_duration: Any = Field(default=None)
    present_value: Any = Field(default=None)


class ModifiedDurationBalanceSheetRequest(BaseModel):
    """Request parameters for modified_duration_balance_sheet()."""

    cashflows: list[float] | list[list[float]]
    times: list[float] | list[list[float]]
    discount_rate: float
    frequency: int = 1


class ModifiedDurationBalanceSheetResponse(BaseModel):
    """Result of modified_duration_balance_sheet(). Mirrors the engine return dict."""

    model_config = ConfigDict(extra="allow", protected_namespaces=())

    macaulay_duration: Any = Field(default=None)
    modified_duration: Any = Field(default=None)
    present_value: Any = Field(default=None)


class EffectiveDurationAlmRequest(BaseModel):
    """Request parameters for effective_duration_alm()."""

    pv_base: float
    pv_up: float
    pv_down: float
    rate_shock: float


class EffectiveDurationAlmResponse(BaseModel):
    """Result of effective_duration_alm(). Mirrors the engine return dict."""

    model_config = ConfigDict(extra="allow", protected_namespaces=())

    effective_duration: Any = Field(default=None)


class DurationGapAnalysisRequest(BaseModel):
    """Request parameters for duration_gap_analysis()."""

    asset_duration: float
    liability_duration: float
    total_assets: float
    total_liabilities: float
    rate_shock: float = 0.01


class DurationGapAnalysisResponse(BaseModel):
    """Result of duration_gap_analysis(). Mirrors the engine return dict."""

    model_config = ConfigDict(extra="allow", protected_namespaces=())

    delta_eve: Any = Field(default=None)
    duration_gap: Any = Field(default=None)
    equity: Any = Field(default=None)
    leverage_adjusted_gap: Any = Field(default=None)


class ConvexityGapRequest(BaseModel):
    """Request parameters for convexity_gap()."""

    asset_convexity: float
    liability_convexity: float
    total_assets: float
    total_liabilities: float


class ConvexityGapResponse(BaseModel):
    """Result of convexity_gap(). Mirrors the engine return dict."""

    model_config = ConfigDict(extra="allow", protected_namespaces=())

    convexity_gap: Any = Field(default=None)


class NiiSensitivityRateShockRequest(BaseModel):
    """Request parameters for nii_sensitivity_rate_shock()."""

    rate_sensitive_assets: float
    rate_sensitive_liabilities: float
    rate_shock: float


class NiiSensitivityRateShockResponse(BaseModel):
    """Result of nii_sensitivity_rate_shock(). Mirrors the engine return dict."""

    model_config = ConfigDict(extra="allow", protected_namespaces=())

    delta_nii: Any = Field(default=None)
    repricing_gap: Any = Field(default=None)


class NiiSimulationBaselineRequest(BaseModel):
    """Request parameters for nii_simulation_baseline()."""

    asset_balances: list[float] | list[list[float]]
    asset_rates: list[float] | list[list[float]]
    liability_balances: list[float] | list[list[float]]
    liability_rates: list[float] | list[list[float]]


class NiiSimulationBaselineResponse(BaseModel):
    """Result of nii_simulation_baseline(). Mirrors the engine return dict."""

    model_config = ConfigDict(extra="allow", protected_namespaces=())

    interest_expense: Any = Field(default=None)
    interest_income: Any = Field(default=None)
    nii: Any = Field(default=None)


class NiiSimulationStressRequest(BaseModel):
    """Request parameters for nii_simulation_stress()."""

    asset_balances: list[float] | list[list[float]]
    asset_rates: list[float] | list[list[float]]
    liability_balances: list[float] | list[list[float]]
    liability_rates: list[float] | list[list[float]]
    rate_shock: float
    asset_beta: float = 1.0
    liability_beta: float = 0.5


class NiiSimulationStressResponse(BaseModel):
    """Result of nii_simulation_stress(). Mirrors the engine return dict."""

    model_config = ConfigDict(extra="allow", protected_namespaces=())

    baseline_nii: Any = Field(default=None)
    delta_nii: Any = Field(default=None)
    stressed_nii: Any = Field(default=None)


class EconomicValueOfEquityEveRequest(BaseModel):
    """Request parameters for economic_value_of_equity_eve()."""

    asset_cashflows: list[float] | list[list[float]]
    asset_times: list[float] | list[list[float]]
    liability_cashflows: list[float] | list[list[float]]
    liability_times: list[float] | list[list[float]]
    asset_rates: list[float] | list[list[float]]
    liability_rates: list[float] | list[list[float]]


class EconomicValueOfEquityEveResponse(BaseModel):
    """Result of economic_value_of_equity_eve(). Mirrors the engine return dict."""

    model_config = ConfigDict(extra="allow", protected_namespaces=())

    eve: Any = Field(default=None)
    pv_assets: Any = Field(default=None)
    pv_liabilities: Any = Field(default=None)


class EveSensitivityAnalysisRequest(BaseModel):
    """Request parameters for eve_sensitivity_analysis()."""

    net_cashflows: list[float] | list[list[float]]
    times: list[float] | list[list[float]]
    base_rates: list[float] | list[list[float]]
    parallel_bps: float = 200.0
    short_bps: float = 300.0
    long_bps: float = 150.0


class EveSensitivityAnalysisResponse(BaseModel):
    """Result of eve_sensitivity_analysis(). Mirrors the engine return dict."""

    model_config = ConfigDict(extra="allow", protected_namespaces=())

    base_eve: Any = Field(default=None)
    delta_eve: Any = Field(default=None)
    worst_case: Any = Field(default=None)


class LiquidityAdjustedNiiRequest(BaseModel):
    """Request parameters for liquidity_adjusted_nii()."""

    base_nii: float
    liquidity_buffer: float
    buffer_yield: float
    funding_cost: float
    liquidity_premium: float


class LiquidityAdjustedNiiResponse(BaseModel):
    """Result of liquidity_adjusted_nii(). Mirrors the engine return dict."""

    model_config = ConfigDict(extra="allow", protected_namespaces=())

    adjusted_nii: Any = Field(default=None)
    buffer_carry: Any = Field(default=None)


class IrrbbSixStandardRateShocksRequest(BaseModel):
    """Request parameters for irrbb_six_standard_rate_shocks()."""

    tenors: list[float] | list[list[float]]
    parallel_bps: float = 200.0
    short_bps: float = 300.0
    long_bps: float = 150.0


class IrrbbSixStandardRateShocksResponse(BaseModel):
    """Result of irrbb_six_standard_rate_shocks(). Mirrors the engine return dict."""

    model_config = ConfigDict(extra="allow", protected_namespaces=())

    result: Any = Field(default=None)


class IrrbbStandardisedFrameworkRequest(BaseModel):
    """Request parameters for irrbb_standardised_framework()."""

    net_cashflows: list[float] | list[list[float]]
    times: list[float] | list[list[float]]
    base_rates: list[float] | list[list[float]]
    parallel_bps: float = 200.0
    short_bps: float = 300.0
    long_bps: float = 150.0


class IrrbbStandardisedFrameworkResponse(BaseModel):
    """Result of irrbb_standardised_framework(). Mirrors the engine return dict."""

    model_config = ConfigDict(extra="allow", protected_namespaces=())

    base_eve: Any = Field(default=None)
    delta_eve: Any = Field(default=None)
    worst_case: Any = Field(default=None)
    worst_scenario: Any = Field(default=None)


class IrrbbInternalModelRequest(BaseModel):
    """Request parameters for irrbb_internal_model()."""

    net_cashflows: list[float] | list[list[float]]
    times: list[float] | list[list[float]]
    base_rates: list[float] | list[list[float]]
    rate_scenarios: list[float] | list[list[float]]


class IrrbbInternalModelResponse(BaseModel):
    """Result of irrbb_internal_model(). Mirrors the engine return dict."""

    model_config = ConfigDict(extra="allow", protected_namespaces=())

    base_eve: Any = Field(default=None)
    mean_delta_eve: Any = Field(default=None)
    worst_case_99: Any = Field(default=None)


class RepricingGapAnalysisRequest(BaseModel):
    """Request parameters for repricing_gap_analysis()."""

    bucket_assets: list[float] | list[list[float]]
    bucket_liabilities: list[float] | list[list[float]]
    bucket_labels: list[str] | None = None


class RepricingGapAnalysisResponse(BaseModel):
    """Result of repricing_gap_analysis(). Mirrors the engine return dict."""

    model_config = ConfigDict(extra="allow", protected_namespaces=())

    cumulative_gap: Any = Field(default=None)
    gap: Any = Field(default=None)
    total_gap: Any = Field(default=None)


class RepricingMaturityProfileRequest(BaseModel):
    """Request parameters for repricing_maturity_profile()."""

    balances: list[float] | list[list[float]]
    repricing_times: list[float] | list[list[float]]
    n_buckets: int
    bucket_edges: list[float] | list[list[float]]


class RepricingMaturityProfileResponse(BaseModel):
    """Result of repricing_maturity_profile(). Mirrors the engine return dict."""

    model_config = ConfigDict(extra="allow", protected_namespaces=())

    profile: Any = Field(default=None)
    weighted_avg_repricing: Any = Field(default=None)


class StaticGapAnalysisRequest(BaseModel):
    """Request parameters for static_gap_analysis()."""

    bucket_assets: list[float] | list[list[float]]
    bucket_liabilities: list[float] | list[list[float]]


class StaticGapAnalysisResponse(BaseModel):
    """Result of static_gap_analysis(). Mirrors the engine return dict."""

    model_config = ConfigDict(extra="allow", protected_namespaces=())

    cumulative_gap: Any = Field(default=None)
    gap: Any = Field(default=None)
    gap_ratio: Any = Field(default=None)


class DynamicGapAnalysisRequest(BaseModel):
    """Request parameters for dynamic_gap_analysis()."""

    bucket_assets: list[float] | list[list[float]]
    bucket_liabilities: list[float] | list[list[float]]
    asset_growth: list[float] | list[list[float]]
    liability_growth: list[float] | list[list[float]]
    n_periods: int


class DynamicGapAnalysisResponse(BaseModel):
    """Result of dynamic_gap_analysis(). Mirrors the engine return dict."""

    model_config = ConfigDict(extra="allow", protected_namespaces=())

    projected_total_gap: Any = Field(default=None)


class BasisRiskIrrbbRequest(BaseModel):
    """Request parameters for basis_risk_irrbb()."""

    asset_balances: list[float] | list[list[float]]
    asset_index_shifts: list[float] | list[list[float]]
    liability_balances: list[float] | list[list[float]]
    liability_index_shifts: list[float] | list[list[float]]


class BasisRiskIrrbbResponse(BaseModel):
    """Result of basis_risk_irrbb(). Mirrors the engine return dict."""

    model_config = ConfigDict(extra="allow", protected_namespaces=())

    basis_risk_nii: Any = Field(default=None)


class OptionRiskIrrbbRequest(BaseModel):
    """Request parameters for option_risk_irrbb()."""

    automatic_option_value: float
    behavioural_option_value: float
    notional: float


class OptionRiskIrrbbResponse(BaseModel):
    """Result of option_risk_irrbb(). Mirrors the engine return dict."""

    model_config = ConfigDict(extra="allow", protected_namespaces=())

    option_risk_ratio: Any = Field(default=None)
    total_option_risk: Any = Field(default=None)


class PipelineRiskMeasurementRequest(BaseModel):
    """Request parameters for pipeline_risk_measurement()."""

    pipeline_notional: float
    pull_through_rate: float
    rate_lock_period: float
    rate_volatility: float


class PipelineRiskMeasurementResponse(BaseModel):
    """Result of pipeline_risk_measurement(). Mirrors the engine return dict."""

    model_config = ConfigDict(extra="allow", protected_namespaces=())

    expected_exposure: Any = Field(default=None)
    rate_risk: Any = Field(default=None)


class InterestRateRiskCapitalIrrbbRequest(BaseModel):
    """Request parameters for interest_rate_risk_capital_irrbb()."""

    worst_delta_eve: float
    tier1_capital: float
    outlier_threshold: float = 0.15


class InterestRateRiskCapitalIrrbbResponse(BaseModel):
    """Result of interest_rate_risk_capital_irrbb(). Mirrors the engine return dict."""

    model_config = ConfigDict(extra="allow", protected_namespaces=())

    capital_add_on: Any = Field(default=None)
    eve_decline: Any = Field(default=None)
    is_outlier: Any = Field(default=None)
    threshold_amount: Any = Field(default=None)


class AssetLiabilityMismatchIndexRequest(BaseModel):
    """Request parameters for asset_liability_mismatch_index()."""

    asset_durations: list[float] | list[list[float]]
    asset_amounts: list[float] | list[list[float]]
    liability_durations: list[float] | list[list[float]]
    liability_amounts: list[float] | list[list[float]]


class AssetLiabilityMismatchIndexResponse(BaseModel):
    """Result of asset_liability_mismatch_index(). Mirrors the engine return dict."""

    model_config = ConfigDict(extra="allow", protected_namespaces=())

    asset_dollar_duration: Any = Field(default=None)
    liability_dollar_duration: Any = Field(default=None)
    mismatch_index: Any = Field(default=None)


class LoanPrepaymentRateCprRequest(BaseModel):
    """Request parameters for loan_prepayment_rate_cpr()."""

    smm: float | None = None
    cpr: float | None = None


class LoanPrepaymentRateCprResponse(BaseModel):
    """Result of loan_prepayment_rate_cpr(). Mirrors the engine return dict."""

    model_config = ConfigDict(extra="allow", protected_namespaces=())

    cpr: Any = Field(default=None)
    smm: Any = Field(default=None)


class PrepaymentModelMortgagesRequest(BaseModel):
    """Request parameters for prepayment_model_mortgages()."""

    principal: float
    annual_rate: float
    term_years: int
    cpr: float


class PrepaymentModelMortgagesResponse(BaseModel):
    """Result of prepayment_model_mortgages(). Mirrors the engine return dict."""

    model_config = ConfigDict(extra="allow", protected_namespaces=())

    smm: Any = Field(default=None)
    total_cashflow: Any = Field(default=None)
    weighted_average_life: Any = Field(default=None)


class BehaviouralModellingNmdsRequest(BaseModel):
    """Request parameters for behavioural_modelling_nmds()."""

    total_balance: float
    core_fraction: float
    core_runoff_rate: float
    non_core_runoff_rate: float
    horizon_years: float = 5.0


class BehaviouralModellingNmdsResponse(BaseModel):
    """Result of behavioural_modelling_nmds(). Mirrors the engine return dict."""

    model_config = ConfigDict(extra="allow", protected_namespaces=())

    core_balance: Any = Field(default=None)
    effective_life: Any = Field(default=None)
    non_core_balance: Any = Field(default=None)
    surviving_balance: Any = Field(default=None)


class NonMaturityDepositStabilityRequest(BaseModel):
    """Request parameters for non_maturity_deposit_stability()."""

    balance_history: list[float] | list[list[float]]
    confidence_level: float = 0.99

    @field_validator("confidence_level")
    @classmethod
    def _confidence_range(cls, v: float) -> float:
        if v is not None and not (0.90 <= v <= 0.9999):
            raise ValueError(f"confidence_level {v} outside regulatory range [0.90, 0.9999]")
        return v


class NonMaturityDepositStabilityResponse(BaseModel):
    """Result of non_maturity_deposit_stability(). Mirrors the engine return dict."""

    model_config = ConfigDict(extra="allow", protected_namespaces=())

    stable_balance: Any = Field(default=None)
    stable_fraction: Any = Field(default=None)
    volatile_balance: Any = Field(default=None)


class CoreDepositDurationRequest(BaseModel):
    """Request parameters for core_deposit_duration()."""

    core_balance: float
    decay_rate: float
    discount_rate: float
    max_years: float = 30.0


class CoreDepositDurationResponse(BaseModel):
    """Result of core_deposit_duration(). Mirrors the engine return dict."""

    model_config = ConfigDict(extra="allow", protected_namespaces=())

    core_deposit_duration: Any = Field(default=None)
    present_value: Any = Field(default=None)
    closed_form_duration: Any = Field(default=None)
    closed_form_present_value: Any = Field(default=None)


class FundsTransferPricingFtpRequest(BaseModel):
    """Request parameters for funds_transfer_pricing_ftp()."""

    notional: float
    customer_rate: float
    ftp_rate: float
    liquidity_premium: float = 0.0
    is_asset: bool = True


class FundsTransferPricingFtpResponse(BaseModel):
    """Result of funds_transfer_pricing_ftp(). Mirrors the engine return dict."""

    model_config = ConfigDict(extra="allow", protected_namespaces=())

    commercial_margin: Any = Field(default=None)
    funding_value: Any = Field(default=None)
    total_spread: Any = Field(default=None)


class FtpCurveConstructionRequest(BaseModel):
    """Request parameters for ftp_curve_construction()."""

    tenors: list[float] | list[list[float]]
    base_curve: list[float] | list[list[float]]
    liquidity_spreads: list[float] | list[list[float]]


class FtpCurveConstructionResponse(BaseModel):
    """Result of ftp_curve_construction(). Mirrors the engine return dict."""

    model_config = ConfigDict(extra="allow", protected_namespaces=())

    ftp_curve: Any = Field(default=None)
    tenors: Any = Field(default=None)


class StructuralHedgeOptimisationRequest(BaseModel):
    """Request parameters for structural_hedge_optimisation()."""

    target_duration: float
    instrument_durations: list[float] | list[list[float]]
    instrument_max_notional: list[float] | list[list[float]]
    equity_notional: float


class StructuralHedgeOptimisationResponse(BaseModel):
    """Result of structural_hedge_optimisation(). Mirrors the engine return dict."""

    model_config = ConfigDict(extra="allow", protected_namespaces=())

    achieved_duration: Any = Field(default=None)
    hedge_notionals: Any = Field(default=None)
    residual: Any = Field(default=None)


class AlmStressTestRequest(BaseModel):
    """Request parameters for alm_stress_test()."""

    net_cashflows: list[float] | list[list[float]]
    times: list[float] | list[list[float]]
    base_rates: list[float] | list[list[float]]
    tier1_capital: float
    parallel_bps: float = 200.0


class AlmStressTestResponse(BaseModel):
    """Result of alm_stress_test(). Mirrors the engine return dict."""

    model_config = ConfigDict(extra="allow", protected_namespaces=())

    breaches_15pct: Any = Field(default=None)
    eve_capital_ratio: Any = Field(default=None)
    scenario_results: Any = Field(default=None)
    worst_case_eve: Any = Field(default=None)


class BalanceSheetProjectionModelRequest(BaseModel):
    """Request parameters for balance_sheet_projection_model()."""

    initial_assets: float
    initial_liabilities: float
    asset_growth_rate: float
    liability_growth_rate: float
    asset_yield: float
    liability_cost: float
    n_years: int


class BalanceSheetProjectionModelResponse(BaseModel):
    """Result of balance_sheet_projection_model(). Mirrors the engine return dict."""

    model_config = ConfigDict(extra="allow", protected_namespaces=())

    assets: Any = Field(default=None)
    equity: Any = Field(default=None)
    liabilities: Any = Field(default=None)
    nii: Any = Field(default=None)

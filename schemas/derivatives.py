"""schemas/derivatives.py — auto-generated Pydantic v2 contracts for the derivatives domain.

Generated by scripts/gen_p3.py from engine signatures. Request models are
precisely typed and enforce regulatory validators (CLAUDE.md s4). Response
models document engine return-dict keys; extra='allow' covers dynamically
built keys that static analysis cannot enumerate.
"""

from __future__ import annotations

from typing import Any

from pydantic import BaseModel, ConfigDict, Field


class BlackScholesEuropeanOptionRequest(BaseModel):
    """Request parameters for black_scholes_european_option()."""

    spot: float
    strike: float
    rate: float
    sigma: float
    tau: float
    option_type: str = "call"
    div_yield: float = 0.0


class BlackScholesEuropeanOptionResponse(BaseModel):
    """Result of black_scholes_european_option(). Mirrors the engine return dict."""

    model_config = ConfigDict(extra="allow", protected_namespaces=())

    d1: Any = Field(default=None)
    d2: Any = Field(default=None)
    price: Any = Field(default=None)


class BlackScholesGreeksRequest(BaseModel):
    """Request parameters for black_scholes_greeks()."""

    spot: float
    strike: float
    rate: float
    sigma: float
    tau: float
    option_type: str = "call"
    div_yield: float = 0.0


class BlackScholesGreeksResponse(BaseModel):
    """Result of black_scholes_greeks(). Mirrors the engine return dict."""

    model_config = ConfigDict(extra="allow", protected_namespaces=())

    delta: Any = Field(default=None)
    gamma: Any = Field(default=None)
    rho: Any = Field(default=None)
    theta: Any = Field(default=None)
    vega: Any = Field(default=None)


class BinomialTreeOptionPricerRequest(BaseModel):
    """Request parameters for binomial_tree_option_pricer()."""

    spot: float
    strike: float
    rate: float
    sigma: float
    tau: float
    n_steps: int = 500
    option_type: str = "call"
    style: str = "european"
    div_yield: float = 0.0


class BinomialTreeOptionPricerResponse(BaseModel):
    """Result of binomial_tree_option_pricer(). Mirrors the engine return dict."""

    model_config = ConfigDict(extra="allow", protected_namespaces=())

    n_steps: Any = Field(default=None)
    price: Any = Field(default=None)
    style: Any = Field(default=None)


class TrinomialTreeOptionPricerRequest(BaseModel):
    """Request parameters for trinomial_tree_option_pricer()."""

    spot: float
    strike: float
    rate: float
    sigma: float
    tau: float
    n_steps: int = 300
    option_type: str = "call"
    style: str = "european"
    div_yield: float = 0.0


class TrinomialTreeOptionPricerResponse(BaseModel):
    """Result of trinomial_tree_option_pricer(). Mirrors the engine return dict."""

    model_config = ConfigDict(extra="allow", protected_namespaces=())

    n_steps: Any = Field(default=None)
    price: Any = Field(default=None)
    style: Any = Field(default=None)


class MonteCarloOptionPricerRequest(BaseModel):
    """Request parameters for monte_carlo_option_pricer()."""

    spot: float
    strike: float
    rate: float
    sigma: float
    tau: float
    n_simulations: int = 100000
    option_type: str = "call"
    div_yield: float = 0.0
    seed: int = 12345


class MonteCarloOptionPricerResponse(BaseModel):
    """Result of monte_carlo_option_pricer(). Mirrors the engine return dict."""

    model_config = ConfigDict(extra="allow", protected_namespaces=())

    n_simulations: Any = Field(default=None)
    price: Any = Field(default=None)
    std_error: Any = Field(default=None)


class DigitalOptionPricerRequest(BaseModel):
    """Request parameters for digital_option_pricer()."""

    spot: float
    strike: float
    rate: float
    sigma: float
    tau: float
    option_type: str = "call"
    payout: float = 1.0
    div_yield: float = 0.0


class DigitalOptionPricerResponse(BaseModel):
    """Result of digital_option_pricer(). Mirrors the engine return dict."""

    model_config = ConfigDict(extra="allow", protected_namespaces=())

    d2: Any = Field(default=None)
    price: Any = Field(default=None)


class BarrierOptionPricerRequest(BaseModel):
    """Request parameters for barrier_option_pricer()."""

    spot: float
    strike: float
    barrier: float
    rate: float
    sigma: float
    tau: float
    option_type: str = "call"
    barrier_type: str = "down-and-out"
    div_yield: float = 0.0
    rebate: float = 0.0


class BarrierOptionPricerResponse(BaseModel):
    """Result of barrier_option_pricer(). Mirrors the engine return dict."""

    model_config = ConfigDict(extra="allow", protected_namespaces=())

    price: Any = Field(default=None)
    vanilla: Any = Field(default=None)


class AsianOptionPricerRequest(BaseModel):
    """Request parameters for asian_option_pricer()."""

    spot: float
    strike: float
    rate: float
    sigma: float
    tau: float
    n_steps: int = 100
    n_simulations: int = 100000
    option_type: str = "call"
    average_type: str = "arithmetic"
    div_yield: float = 0.0
    seed: int = 21
    qmc: bool = False
    greeks: bool = False


class AsianOptionPricerResponse(BaseModel):
    """Result of asian_option_pricer(). Mirrors the engine return dict."""

    model_config = ConfigDict(extra="allow", protected_namespaces=())

    delta: Any = Field(default=None)
    gamma: Any = Field(default=None)
    price: Any = Field(default=None)
    rho: Any = Field(default=None)
    std_error: Any = Field(default=None)
    theta: Any = Field(default=None)
    vega: Any = Field(default=None)


class LookbackOptionPricerRequest(BaseModel):
    """Request parameters for lookback_option_pricer()."""

    spot: float
    strike: float
    rate: float
    sigma: float
    tau: float
    n_steps: int = 100
    n_simulations: int = 100000
    option_type: str = "call"
    strike_type: str = "floating"
    div_yield: float = 0.0
    seed: int = 31
    qmc: bool = False
    greeks: bool = False


class LookbackOptionPricerResponse(BaseModel):
    """Result of lookback_option_pricer(). Mirrors the engine return dict."""

    model_config = ConfigDict(extra="allow", protected_namespaces=())

    delta: Any = Field(default=None)
    gamma: Any = Field(default=None)
    price: Any = Field(default=None)
    rho: Any = Field(default=None)
    std_error: Any = Field(default=None)
    theta: Any = Field(default=None)
    vega: Any = Field(default=None)


class AmericanOptionLsmRequest(BaseModel):
    """Request parameters for american_option_lsm()."""

    spot: float
    strike: float
    rate: float
    sigma: float
    tau: float
    n_steps: int = 50
    n_simulations: int = 100000
    option_type: str = "put"
    div_yield: float = 0.0
    seed: int = 41
    greeks: bool = False


class AmericanOptionLsmResponse(BaseModel):
    """Result of american_option_lsm(). Mirrors the engine return dict."""

    model_config = ConfigDict(extra="allow", protected_namespaces=())

    delta: Any = Field(default=None)
    gamma: Any = Field(default=None)
    price: Any = Field(default=None)
    rho: Any = Field(default=None)
    theta: Any = Field(default=None)
    vega: Any = Field(default=None)


class BermudanOptionPricerRequest(BaseModel):
    """Request parameters for bermudan_option_pricer()."""

    spot: float
    strike: float
    rate: float
    sigma: float
    tau: float
    exercise_dates: int = 4
    n_steps: int = 48
    n_simulations: int = 100000
    option_type: str = "put"
    div_yield: float = 0.0
    seed: int = 51
    greeks: bool = False


class BermudanOptionPricerResponse(BaseModel):
    """Result of bermudan_option_pricer(). Mirrors the engine return dict."""

    model_config = ConfigDict(extra="allow", protected_namespaces=())

    delta: Any = Field(default=None)
    exercise_dates: Any = Field(default=None)
    gamma: Any = Field(default=None)
    price: Any = Field(default=None)
    rho: Any = Field(default=None)
    theta: Any = Field(default=None)
    vega: Any = Field(default=None)


class RainbowOptionPricerRequest(BaseModel):
    """Request parameters for rainbow_option_pricer()."""

    spots: list[float] | list[list[float]]
    strike: float
    rate: float
    sigmas: list[float] | list[list[float]]
    tau: float
    correlation: list[float] | list[list[float]]
    n_simulations: int = 100000
    option_type: str = "call"
    rainbow_type: str = "best-of"
    seed: int = 61
    greeks: bool = False


class RainbowOptionPricerResponse(BaseModel):
    """Result of rainbow_option_pricer(). Mirrors the engine return dict.

    delta/gamma/vega are one entry per asset when greeks=True (diagonal
    sensitivities only, no cross-asset terms); theta/rho are scalar.
    """

    model_config = ConfigDict(extra="allow", protected_namespaces=())

    delta: Any = Field(default=None)
    gamma: Any = Field(default=None)
    price: Any = Field(default=None)
    rho: Any = Field(default=None)
    std_error: Any = Field(default=None)
    theta: Any = Field(default=None)
    vega: Any = Field(default=None)


class BasketOptionPricerRequest(BaseModel):
    """Request parameters for basket_option_pricer()."""

    spots: list[float] | list[list[float]]
    weights: list[float] | list[list[float]]
    strike: float
    rate: float
    sigmas: list[float] | list[list[float]]
    tau: float
    correlation: list[float] | list[list[float]]
    n_simulations: int = 100000
    option_type: str = "call"
    seed: int = 71
    greeks: bool = False


class BasketOptionPricerResponse(BaseModel):
    """Result of basket_option_pricer(). Mirrors the engine return dict.

    delta/gamma/vega are one entry per asset when greeks=True (diagonal
    sensitivities only, no cross-asset terms); theta/rho are scalar.
    """

    model_config = ConfigDict(extra="allow", protected_namespaces=())

    delta: Any = Field(default=None)
    gamma: Any = Field(default=None)
    price: Any = Field(default=None)
    rho: Any = Field(default=None)
    std_error: Any = Field(default=None)
    theta: Any = Field(default=None)
    vega: Any = Field(default=None)


class SpreadOptionKirkApproximationRequest(BaseModel):
    """Request parameters for spread_option_kirk_approximation()."""

    spot1: float
    spot2: float
    strike: float
    rate: float
    sigma1: float
    sigma2: float
    rho: float
    tau: float
    option_type: str = "call"


class SpreadOptionKirkApproximationResponse(BaseModel):
    """Result of spread_option_kirk_approximation(). Mirrors the engine return dict."""

    model_config = ConfigDict(extra="allow", protected_namespaces=())

    price: Any = Field(default=None)


class CompoundOptionPricerRequest(BaseModel):
    """Request parameters for compound_option_pricer()."""

    spot: float
    strike_underlying: float
    strike_compound: float
    rate: float
    sigma: float
    tau_compound: float
    tau_underlying: float
    n_simulations: int = 200000
    compound_type: str = "call-on-call"
    div_yield: float = 0.0
    seed: int = 81
    greeks: bool = False


class CompoundOptionPricerResponse(BaseModel):
    """Result of compound_option_pricer(). Mirrors the engine return dict."""

    model_config = ConfigDict(extra="allow", protected_namespaces=())

    delta: Any = Field(default=None)
    gamma: Any = Field(default=None)
    price: Any = Field(default=None)
    rho: Any = Field(default=None)
    std_error: Any = Field(default=None)
    theta: Any = Field(default=None)
    vega: Any = Field(default=None)


class ChooserOptionPricerRequest(BaseModel):
    """Request parameters for chooser_option_pricer()."""

    spot: float
    strike: float
    rate: float
    sigma: float
    tau_choose: float
    tau_expiry: float
    div_yield: float = 0.0


class ChooserOptionPricerResponse(BaseModel):
    """Result of chooser_option_pricer(). Mirrors the engine return dict."""

    model_config = ConfigDict(extra="allow", protected_namespaces=())

    call_value: Any = Field(default=None)
    price: Any = Field(default=None)
    put_value: Any = Field(default=None)


class BondPricerFixedCouponRequest(BaseModel):
    """Request parameters for bond_pricer_fixed_coupon()."""

    face_value: float
    coupon_rate: float
    yield_rate: float
    maturity: float
    frequency: int = 2


class BondPricerFixedCouponResponse(BaseModel):
    """Result of bond_pricer_fixed_coupon(). Mirrors the engine return dict."""

    model_config = ConfigDict(extra="allow", protected_namespaces=())

    cashflows: Any = Field(default=None)
    clean_price: Any = Field(default=None)
    price: Any = Field(default=None)


class BondPricerFloatingRateRequest(BaseModel):
    """Request parameters for bond_pricer_floating_rate()."""

    face_value: float
    reference_rates: list[float] | list[list[float]]
    spread: float
    discount_rates: list[float] | list[list[float]]
    maturity: float
    frequency: int = 4


class BondPricerFloatingRateResponse(BaseModel):
    """Result of bond_pricer_floating_rate(). Mirrors the engine return dict."""

    model_config = ConfigDict(extra="allow", protected_namespaces=())

    cashflows: Any = Field(default=None)
    price: Any = Field(default=None)


class ZeroCouponBondPricerRequest(BaseModel):
    """Request parameters for zero_coupon_bond_pricer()."""

    face_value: float
    yield_rate: float
    maturity: float
    frequency: int = 2


class ZeroCouponBondPricerResponse(BaseModel):
    """Result of zero_coupon_bond_pricer(). Mirrors the engine return dict."""

    model_config = ConfigDict(extra="allow", protected_namespaces=())

    discount_factor: Any = Field(default=None)
    price: Any = Field(default=None)


class InflationLinkedBondPricerRequest(BaseModel):
    """Request parameters for inflation_linked_bond_pricer()."""

    face_value: float
    real_coupon_rate: float
    real_yield: float
    maturity: float
    inflation_rate: float
    frequency: int = 2


class InflationLinkedBondPricerResponse(BaseModel):
    """Result of inflation_linked_bond_pricer(). Mirrors the engine return dict."""

    model_config = ConfigDict(extra="allow", protected_namespaces=())

    cashflows: Any = Field(default=None)
    index_ratio_final: Any = Field(default=None)
    price: Any = Field(default=None)


class CallableBondPricerRequest(BaseModel):
    """Request parameters for callable_bond_pricer()."""

    face_value: float
    coupon_rate: float
    short_rate: float
    rate_vol: float
    maturity: float
    call_price: float
    frequency: int = 1


class CallableBondPricerResponse(BaseModel):
    """Result of callable_bond_pricer(). Mirrors the engine return dict."""

    model_config = ConfigDict(extra="allow", protected_namespaces=())

    price: Any = Field(default=None)
    straight_price: Any = Field(default=None)


class PuttableBondPricerRequest(BaseModel):
    """Request parameters for puttable_bond_pricer()."""

    face_value: float
    coupon_rate: float
    short_rate: float
    rate_vol: float
    maturity: float
    put_price: float
    frequency: int = 1


class PuttableBondPricerResponse(BaseModel):
    """Result of puttable_bond_pricer(). Mirrors the engine return dict."""

    model_config = ConfigDict(extra="allow", protected_namespaces=())

    price: Any = Field(default=None)
    straight_price: Any = Field(default=None)


class ConvertibleBondPricerRequest(BaseModel):
    """Request parameters for convertible_bond_pricer()."""

    face_value: float
    coupon_rate: float
    yield_rate: float
    maturity: float
    conversion_ratio: float
    stock_price: float
    frequency: int = 2


class ConvertibleBondPricerResponse(BaseModel):
    """Result of convertible_bond_pricer(). Mirrors the engine return dict."""

    model_config = ConfigDict(extra="allow", protected_namespaces=())

    bond_floor: Any = Field(default=None)
    conversion_value: Any = Field(default=None)
    price: Any = Field(default=None)


class DurationMacaulayRequest(BaseModel):
    """Request parameters for duration_macaulay()."""

    cashflows: list[float] | list[list[float]]
    times: list[float] | list[list[float]]
    yield_rate: float
    frequency: int = 2


class DurationMacaulayResponse(BaseModel):
    """Result of duration_macaulay(). Mirrors the engine return dict."""

    model_config = ConfigDict(extra="allow", protected_namespaces=())

    macaulay_duration: Any = Field(default=None)
    price: Any = Field(default=None)


class ModifiedDurationRequest(BaseModel):
    """Request parameters for modified_duration()."""

    cashflows: list[float] | list[list[float]]
    times: list[float] | list[list[float]]
    yield_rate: float
    frequency: int = 2


class ModifiedDurationResponse(BaseModel):
    """Result of modified_duration(). Mirrors the engine return dict."""

    model_config = ConfigDict(extra="allow", protected_namespaces=())

    macaulay_duration: Any = Field(default=None)
    modified_duration: Any = Field(default=None)
    price: Any = Field(default=None)


class EffectiveDurationRequest(BaseModel):
    """Request parameters for effective_duration()."""

    price_base: float
    price_up: float
    price_down: float
    yield_shock: float


class EffectiveDurationResponse(BaseModel):
    """Result of effective_duration(). Mirrors the engine return dict."""

    model_config = ConfigDict(extra="allow", protected_namespaces=())

    effective_duration: Any = Field(default=None)


class ConvexityRequest(BaseModel):
    """Request parameters for convexity()."""

    cashflows: list[float] | list[list[float]]
    times: list[float] | list[list[float]]
    yield_rate: float
    frequency: int = 2


class ConvexityResponse(BaseModel):
    """Result of convexity(). Mirrors the engine return dict."""

    model_config = ConfigDict(extra="allow", protected_namespaces=())

    convexity: Any = Field(default=None)
    price: Any = Field(default=None)


class Dv01PvbpRequest(BaseModel):
    """Request parameters for dv01_pvbp()."""

    cashflows: list[float] | list[list[float]]
    times: list[float] | list[list[float]]
    yield_rate: float
    frequency: int = 2


class Dv01PvbpResponse(BaseModel):
    """Result of dv01_pvbp(). Mirrors the engine return dict."""

    model_config = ConfigDict(extra="allow", protected_namespaces=())

    dv01: Any = Field(default=None)
    price: Any = Field(default=None)


class YieldToMaturityRequest(BaseModel):
    """Request parameters for yield_to_maturity()."""

    price: float
    cashflows: list[float] | list[list[float]]
    times: list[float] | list[list[float]]
    frequency: int = 2


class YieldToMaturityResponse(BaseModel):
    """Result of yield_to_maturity(). Mirrors the engine return dict."""

    model_config = ConfigDict(extra="allow", protected_namespaces=())

    ytm: Any = Field(default=None)


class YieldToCallRequest(BaseModel):
    """Request parameters for yield_to_call()."""

    price: float
    face_value: float
    coupon_rate: float
    call_price: float
    call_date: float
    frequency: int = 2


class YieldToCallResponse(BaseModel):
    """Result of yield_to_call(). Mirrors the engine return dict."""

    model_config = ConfigDict(extra="allow", protected_namespaces=())

    ytc: Any = Field(default=None)


class ZSpreadCalculatorRequest(BaseModel):
    """Request parameters for z_spread_calculator()."""

    price: float
    cashflows: list[float] | list[list[float]]
    times: list[float] | list[list[float]]
    zero_rates: list[float] | list[list[float]]
    frequency: int = 2


class ZSpreadCalculatorResponse(BaseModel):
    """Result of z_spread_calculator(). Mirrors the engine return dict."""

    model_config = ConfigDict(extra="allow", protected_namespaces=())

    z_spread: Any = Field(default=None)
    z_spread_bps: Any = Field(default=None)


class AssetSwapSpreadRequest(BaseModel):
    """Request parameters for asset_swap_spread()."""

    bond_price: float
    cashflows: list[float] | list[list[float]]
    times: list[float] | list[list[float]]
    swap_rates: list[float] | list[list[float]]
    face_value: float = 100.0
    frequency: int = 2


class AssetSwapSpreadResponse(BaseModel):
    """Result of asset_swap_spread(). Mirrors the engine return dict."""

    model_config = ConfigDict(extra="allow", protected_namespaces=())

    asset_swap_spread: Any = Field(default=None)
    asset_swap_spread_bps: Any = Field(default=None)


class OasOptionAdjustedSpreadRequest(BaseModel):
    """Request parameters for oas_option_adjusted_spread()."""

    market_price: float
    face_value: float
    coupon_rate: float
    short_rate: float
    rate_vol: float
    maturity: float
    call_price: float
    frequency: int = 1


class OasOptionAdjustedSpreadResponse(BaseModel):
    """Result of oas_option_adjusted_spread(). Mirrors the engine return dict."""

    model_config = ConfigDict(extra="allow", protected_namespaces=())

    oas: Any = Field(default=None)
    oas_bps: Any = Field(default=None)


class NelsonSiegelCurveFitRequest(BaseModel):
    """Request parameters for nelson_siegel_curve_fit()."""

    maturities: list[float] | list[list[float]]
    yields: list[float] | list[list[float]]
    tau_init: float = 1.5


class NelsonSiegelCurveFitResponse(BaseModel):
    """Result of nelson_siegel_curve_fit(). Mirrors the engine return dict."""

    model_config = ConfigDict(extra="allow", protected_namespaces=())

    beta0: Any = Field(default=None)
    beta1: Any = Field(default=None)
    beta2: Any = Field(default=None)
    rmse: Any = Field(default=None)
    tau: Any = Field(default=None)


class NelsonSiegelSvenssonCurveRequest(BaseModel):
    """Request parameters for nelson_siegel_svensson_curve()."""

    maturities: list[float] | list[list[float]]
    yields: list[float] | list[list[float]]
    tau1_init: float = 1.5
    tau2_init: float = 8.0


class NelsonSiegelSvenssonCurveResponse(BaseModel):
    """Result of nelson_siegel_svensson_curve(). Mirrors the engine return dict."""

    model_config = ConfigDict(extra="allow", protected_namespaces=())

    beta0: Any = Field(default=None)
    beta1: Any = Field(default=None)
    beta2: Any = Field(default=None)
    beta3: Any = Field(default=None)
    rmse: Any = Field(default=None)
    tau1: Any = Field(default=None)
    tau2: Any = Field(default=None)


class BootstrapYieldCurveRequest(BaseModel):
    """Request parameters for bootstrap_yield_curve()."""

    par_rates: list[float] | list[list[float]]
    maturities: list[float] | list[list[float]]
    frequency: int = 1
    face_value: float = 1.0


class BootstrapYieldCurveResponse(BaseModel):
    """Result of bootstrap_yield_curve(). Mirrors the engine return dict."""

    model_config = ConfigDict(extra="allow", protected_namespaces=())

    discount_factors: Any = Field(default=None)
    zero_rates: Any = Field(default=None)


class SwapRateCurveRequest(BaseModel):
    """Request parameters for swap_rate_curve()."""

    discount_factors: list[float] | list[list[float]]
    maturities: list[float] | list[list[float]]
    frequency: int = 1


class SwapRateCurveResponse(BaseModel):
    """Result of swap_rate_curve(). Mirrors the engine return dict."""

    model_config = ConfigDict(extra="allow", protected_namespaces=())

    swap_rates: Any = Field(default=None)


class OisCurveSoniaSofrRequest(BaseModel):
    """Request parameters for ois_curve_sonia_sofr()."""

    ois_rates: list[float] | list[list[float]]
    maturities: list[float] | list[list[float]]
    frequency: int = 1


class OisCurveSoniaSofrResponse(BaseModel):
    """Result of ois_curve_sonia_sofr(). Mirrors the engine return dict."""

    model_config = ConfigDict(extra="allow", protected_namespaces=())

    discount_factors: Any = Field(default=None)
    zero_rates: Any = Field(default=None)


class ForwardRateAgreementFraRequest(BaseModel):
    """Request parameters for forward_rate_agreement_fra()."""

    notional: float
    fra_rate: float
    forward_rate: float
    start: float
    end: float
    discount_factor: float


class ForwardRateAgreementFraResponse(BaseModel):
    """Result of forward_rate_agreement_fra(). Mirrors the engine return dict."""

    model_config = ConfigDict(extra="allow", protected_namespaces=())

    accrual: Any = Field(default=None)
    value: Any = Field(default=None)


class InterestRateSwapIrsPricerRequest(BaseModel):
    """Request parameters for interest_rate_swap_irs_pricer()."""

    notional: float
    fixed_rate: float
    forward_rates: list[float] | list[list[float]]
    discount_factors: list[float] | list[list[float]]
    accruals: list[float] | list[list[float]]
    pay_fixed: bool = True


class InterestRateSwapIrsPricerResponse(BaseModel):
    """Result of interest_rate_swap_irs_pricer(). Mirrors the engine return dict."""

    model_config = ConfigDict(extra="allow", protected_namespaces=())

    annuity: Any = Field(default=None)
    fixed_pv: Any = Field(default=None)
    float_pv: Any = Field(default=None)
    par_rate: Any = Field(default=None)
    value: Any = Field(default=None)


class CrossCurrencySwapPricerRequest(BaseModel):
    """Request parameters for cross_currency_swap_pricer()."""

    notional_dom: float
    notional_for: float
    fixed_rate_dom: float
    fixed_rate_for: float
    df_dom: list[float] | list[list[float]]
    df_for: list[float] | list[list[float]]
    accruals: list[float] | list[list[float]]
    fx_spot: float
    pay_domestic: bool = True


class CrossCurrencySwapPricerResponse(BaseModel):
    """Result of cross_currency_swap_pricer(). Mirrors the engine return dict."""

    model_config = ConfigDict(extra="allow", protected_namespaces=())

    pv_domestic_leg: Any = Field(default=None)
    pv_foreign_leg: Any = Field(default=None)
    value: Any = Field(default=None)


class OvernightIndexSwapOisRequest(BaseModel):
    """Request parameters for overnight_index_swap_ois()."""

    notional: float
    fixed_rate: float
    compounded_rate: float
    accrual: float
    discount_factor: float
    pay_fixed: bool = True


class OvernightIndexSwapOisResponse(BaseModel):
    """Result of overnight_index_swap_ois(). Mirrors the engine return dict."""

    model_config = ConfigDict(extra="allow", protected_namespaces=())

    value: Any = Field(default=None)


class TotalReturnSwapTrsRequest(BaseModel):
    """Request parameters for total_return_swap_trs()."""

    notional: float
    asset_return: float
    financing_rate: float
    accrual: float
    discount_factor: float
    receive_total_return: bool = True


class TotalReturnSwapTrsResponse(BaseModel):
    """Result of total_return_swap_trs(). Mirrors the engine return dict."""

    model_config = ConfigDict(extra="allow", protected_namespaces=())

    value: Any = Field(default=None)


class CreditDefaultSwapCdsPricerRequest(BaseModel):
    """Request parameters for credit_default_swap_cds_pricer()."""

    notional: float
    spread: float
    hazard_rate: float
    recovery_rate: float
    maturity: float
    discount_rate: float
    frequency: int = 4


class CreditDefaultSwapCdsPricerResponse(BaseModel):
    """Result of credit_default_swap_cds_pricer(). Mirrors the engine return dict."""

    model_config = ConfigDict(extra="allow", protected_namespaces=())

    par_spread: Any = Field(default=None)
    premium_leg: Any = Field(default=None)
    protection_leg: Any = Field(default=None)
    value: Any = Field(default=None)


class EquitySwapPricerRequest(BaseModel):
    """Request parameters for equity_swap_pricer()."""

    notional: float
    equity_return: float
    funding_rate: float
    accrual: float
    discount_factor: float
    receive_equity: bool = True


class EquitySwapPricerResponse(BaseModel):
    """Result of equity_swap_pricer(). Mirrors the engine return dict."""

    model_config = ConfigDict(extra="allow", protected_namespaces=())

    value: Any = Field(default=None)


class CapletFloorletPricerBlackRequest(BaseModel):
    """Request parameters for caplet_floorlet_pricer_black()."""

    notional: float
    forward_rate: float
    strike: float
    vol: float
    expiry: float
    accrual: float
    discount_factor: float
    option_type: str = "caplet"


class CapletFloorletPricerBlackResponse(BaseModel):
    """Result of caplet_floorlet_pricer_black(). Mirrors the engine return dict."""

    model_config = ConfigDict(extra="allow", protected_namespaces=())

    price: Any = Field(default=None)


class CapFloorPricerRequest(BaseModel):
    """Request parameters for cap_floor_pricer()."""

    notional: float
    forward_rates: list[float] | list[list[float]]
    strike: float
    vols: list[float] | list[list[float]]
    expiries: list[float] | list[list[float]]
    accruals: list[float] | list[list[float]]
    discount_factors: list[float] | list[list[float]]
    option_type: str = "cap"


class CapFloorPricerResponse(BaseModel):
    """Result of cap_floor_pricer(). Mirrors the engine return dict."""

    model_config = ConfigDict(extra="allow", protected_namespaces=())

    caplet_prices: Any = Field(default=None)
    price: Any = Field(default=None)


class SwaptionPricerBlackRequest(BaseModel):
    """Request parameters for swaption_pricer_black()."""

    notional: float
    forward_swap_rate: float
    strike: float
    vol: float
    expiry: float
    annuity: float
    option_type: str = "payer"


class SwaptionPricerBlackResponse(BaseModel):
    """Result of swaption_pricer_black(). Mirrors the engine return dict."""

    model_config = ConfigDict(extra="allow", protected_namespaces=())

    price: Any = Field(default=None)


class SwaptionPricerSabrRequest(BaseModel):
    """Request parameters for swaption_pricer_sabr()."""

    notional: float
    forward_swap_rate: float
    strike: float
    expiry: float
    annuity: float
    alpha: float
    beta: float
    rho: float
    nu: float
    option_type: str = "payer"


class SwaptionPricerSabrResponse(BaseModel):
    """Result of swaption_pricer_sabr(). Mirrors the engine return dict."""

    model_config = ConfigDict(extra="allow", protected_namespaces=())

    price: Any = Field(default=None)
    sabr_vol: Any = Field(default=None)


class VasicekInterestRateModelRequest(BaseModel):
    """Request parameters for vasicek_interest_rate_model()."""

    r0: float
    kappa: float
    theta: float
    sigma: float
    maturity: float
    n_steps: int = 100
    n_simulations: int = 50000
    seed: int = 101


class VasicekInterestRateModelResponse(BaseModel):
    """Result of vasicek_interest_rate_model(). Mirrors the engine return dict."""

    model_config = ConfigDict(extra="allow", protected_namespaces=())

    A: Any = Field(default=None)
    B: Any = Field(default=None)
    bond_price: Any = Field(default=None)
    mc_mean_rate: Any = Field(default=None)


class CoxIngersollRossModelRequest(BaseModel):
    """Request parameters for cox_ingersoll_ross_model()."""

    r0: float
    kappa: float
    theta: float
    sigma: float
    maturity: float
    n_steps: int = 100
    n_simulations: int = 50000
    seed: int = 102


class CoxIngersollRossModelResponse(BaseModel):
    """Result of cox_ingersoll_ross_model(). Mirrors the engine return dict."""

    model_config = ConfigDict(extra="allow", protected_namespaces=())

    bond_price: Any = Field(default=None)
    feller_satisfied: Any = Field(default=None)
    mc_mean_rate: Any = Field(default=None)


class HullWhiteShortRateModelRequest(BaseModel):
    """Request parameters for hull_white_short_rate_model()."""

    r0: float
    kappa: float
    sigma: float
    maturity: float
    theta_const: float = 0.03
    n_steps: int = 100
    n_simulations: int = 50000
    seed: int = 103


class HullWhiteShortRateModelResponse(BaseModel):
    """Result of hull_white_short_rate_model(). Mirrors the engine return dict."""

    model_config = ConfigDict(extra="allow", protected_namespaces=())

    bond_price: Any = Field(default=None)
    mc_mean_rate: Any = Field(default=None)


class LmmBgmRateModelRequest(BaseModel):
    """Request parameters for lmm_bgm_rate_model()."""

    forward_rates: list[float] | list[list[float]]
    vols: list[float] | list[list[float]]
    tenor: float
    horizon: float
    n_steps: int = 20
    n_simulations: int = 20000
    seed: int = 104


class LmmBgmRateModelResponse(BaseModel):
    """Result of lmm_bgm_rate_model(). Mirrors the engine return dict."""

    model_config = ConfigDict(extra="allow", protected_namespaces=())

    all_positive: Any = Field(default=None)
    mean_terminal_rates: Any = Field(default=None)


class HestonStochasticVolatilityModelRequest(BaseModel):
    """Request parameters for heston_stochastic_volatility_model()."""

    spot: float
    strike: float
    rate: float
    tau: float
    v0: float
    kappa: float
    theta: float
    sigma: float
    rho: float
    option_type: str = "call"


class HestonStochasticVolatilityModelResponse(BaseModel):
    """Result of heston_stochastic_volatility_model(). Mirrors the engine return dict."""

    model_config = ConfigDict(extra="allow", protected_namespaces=())

    P1: Any = Field(default=None)
    P2: Any = Field(default=None)
    price: Any = Field(default=None)


class SabrVolatilityModelRequest(BaseModel):
    """Request parameters for sabr_volatility_model()."""

    forward: float
    strike: float
    tau: float
    alpha: float
    beta: float
    rho: float
    nu: float


class SabrVolatilityModelResponse(BaseModel):
    """Result of sabr_volatility_model(). Mirrors the engine return dict."""

    model_config = ConfigDict(extra="allow", protected_namespaces=())

    implied_vol: Any = Field(default=None)


class LocalVolatilityDupireModelRequest(BaseModel):
    """Request parameters for local_volatility_dupire_model()."""

    strikes: list[float] | list[list[float]]
    maturities: list[float] | list[list[float]]
    call_surface: list[float] | list[list[float]]
    rate: float
    spot: float


class LocalVolatilityDupireModelResponse(BaseModel):
    """Result of local_volatility_dupire_model(). Mirrors the engine return dict."""

    model_config = ConfigDict(extra="allow", protected_namespaces=())

    local_vol: Any = Field(default=None)
    maturities_full: Any = Field(default=None)
    strikes_full: Any = Field(default=None)


class RoughVolatilityRbergomiModelRequest(BaseModel):
    """Request parameters for rough_volatility_rbergomi_model()."""

    spot: float
    strike: float
    rate: float
    tau: float
    xi: float = 0.04
    eta: float = 1.5
    hurst: float = 0.1
    rho: float = -0.7
    n_steps: int = 50
    n_simulations: int = 50000
    option_type: str = "call"
    seed: int = 2024
    control_variate: bool = False
    greeks: bool = False


class RoughVolatilityRbergomiModelResponse(BaseModel):
    """Result of rough_volatility_rbergomi_model(). Mirrors the engine return dict.

    When greeks=True, "vega" is sensitivity to xi (forward-variance level),
    not to eta or hurst.
    """

    model_config = ConfigDict(extra="allow", protected_namespaces=())

    delta: Any = Field(default=None)
    gamma: Any = Field(default=None)
    price: Any = Field(default=None)
    rho: Any = Field(default=None)
    std_error: Any = Field(default=None)
    theta: Any = Field(default=None)
    vega: Any = Field(default=None)


class VarianceGammaModelRequest(BaseModel):
    """Request parameters for variance_gamma_model()."""

    spot: float
    strike: float
    rate: float
    tau: float
    sigma: float = 0.2
    theta: float = -0.1
    nu: float = 0.2
    n_simulations: int = 100000
    option_type: str = "call"
    seed: int = 7
    greeks: bool = False


class VarianceGammaModelResponse(BaseModel):
    """Result of variance_gamma_model(). Mirrors the engine return dict.

    When greeks=True, "vega" is sensitivity to sigma (diffusion vol); the
    "theta" key is the Greek (time decay), unrelated to this model's own
    theta request parameter (skew).
    """

    model_config = ConfigDict(extra="allow", protected_namespaces=())

    delta: Any = Field(default=None)
    gamma: Any = Field(default=None)
    price: Any = Field(default=None)
    rho: Any = Field(default=None)
    std_error: Any = Field(default=None)
    theta: Any = Field(default=None)
    vega: Any = Field(default=None)


class NormalInverseGaussianModelRequest(BaseModel):
    """Request parameters for normal_inverse_gaussian_model()."""

    spot: float
    strike: float
    rate: float
    tau: float
    alpha: float = 15.0
    beta: float = -5.0
    delta: float = 0.5
    n_simulations: int = 100000
    option_type: str = "call"
    seed: int = 11
    greeks: bool = False


class NormalInverseGaussianModelResponse(BaseModel):
    """Result of normal_inverse_gaussian_model(). Mirrors the engine return dict.

    When greeks=True, "vega" is sensitivity to this model's own delta
    request parameter (IG scale); the "delta" key in this response is the
    Greek (spot sensitivity), unrelated to that request parameter despite
    the shared name.
    """

    model_config = ConfigDict(extra="allow", protected_namespaces=())

    delta: Any = Field(default=None)
    gamma: Any = Field(default=None)
    price: Any = Field(default=None)
    rho: Any = Field(default=None)
    std_error: Any = Field(default=None)
    theta: Any = Field(default=None)
    vega: Any = Field(default=None)


class DisplacedDiffusionModelRequest(BaseModel):
    """Request parameters for displaced_diffusion_model()."""

    spot: float
    strike: float
    rate: float
    sigma: float
    tau: float
    displacement: float
    option_type: str = "call"
    div_yield: float = 0.0


class DisplacedDiffusionModelResponse(BaseModel):
    """Result of displaced_diffusion_model(). Mirrors the engine return dict."""

    model_config = ConfigDict(extra="allow", protected_namespaces=())

    price: Any = Field(default=None)


class FxForwardPricerRequest(BaseModel):
    """Request parameters for fx_forward_pricer()."""

    spot: float
    rate_domestic: float
    rate_foreign: float
    tau: float
    notional: float = 1.0
    contracted_forward: float | None = None


class FxForwardPricerResponse(BaseModel):
    """Result of fx_forward_pricer(). Mirrors the engine return dict."""

    model_config = ConfigDict(extra="allow", protected_namespaces=())

    result: Any = Field(default=None)


class FxOptionPricerGarmanKohlhagenRequest(BaseModel):
    """Request parameters for fx_option_pricer_garman_kohlhagen()."""

    spot: float
    strike: float
    rate_domestic: float
    rate_foreign: float
    sigma: float
    tau: float
    option_type: str = "call"
    notional: float = 1.0


class FxOptionPricerGarmanKohlhagenResponse(BaseModel):
    """Result of fx_option_pricer_garman_kohlhagen(). Mirrors the engine return dict."""

    model_config = ConfigDict(extra="allow", protected_namespaces=())

    d1: Any = Field(default=None)
    d2: Any = Field(default=None)
    price: Any = Field(default=None)

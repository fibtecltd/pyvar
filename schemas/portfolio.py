"""schemas/portfolio.py — auto-generated Pydantic v2 contracts for the portfolio domain.

Generated by scripts/gen_p3.py from engine signatures. Request models are
precisely typed and enforce regulatory validators (CLAUDE.md s4). Response
models document engine return-dict keys; extra='allow' covers dynamically
built keys that static analysis cannot enumerate.
"""

from __future__ import annotations

from typing import Any

from pydantic import BaseModel, ConfigDict, Field, field_validator


class MeanVarianceOptimisationRequest(BaseModel):
    """Request parameters for mean_variance_optimisation()."""

    mean_returns: list[float] | list[list[float]]
    cov_matrix: list[float] | list[list[float]]
    risk_aversion: float = 1.0
    allow_short: bool = False
    risk_free: float = 0.0
    periods_per_year: int = 252


class MeanVarianceOptimisationResponse(BaseModel):
    """Result of mean_variance_optimisation(). Mirrors the engine return dict."""

    model_config = ConfigDict(extra="allow", protected_namespaces=())

    result: Any = Field(default=None)


class MinimumVariancePortfolioRequest(BaseModel):
    """Request parameters for minimum_variance_portfolio()."""

    cov_matrix: list[float] | list[list[float]]
    allow_short: bool = False
    mean_returns: list[float] | list[list[float]] | None = None
    risk_free: float = 0.0
    periods_per_year: int = 252


class MinimumVariancePortfolioResponse(BaseModel):
    """Result of minimum_variance_portfolio(). Mirrors the engine return dict."""

    model_config = ConfigDict(extra="allow", protected_namespaces=())

    result: Any = Field(default=None)


class MaximumSharpeRatioPortfolioRequest(BaseModel):
    """Request parameters for maximum_sharpe_ratio_portfolio()."""

    mean_returns: list[float] | list[list[float]]
    cov_matrix: list[float] | list[list[float]]
    risk_free: float = 0.0
    allow_short: bool = False
    periods_per_year: int = 252


class MaximumSharpeRatioPortfolioResponse(BaseModel):
    """Result of maximum_sharpe_ratio_portfolio(). Mirrors the engine return dict."""

    model_config = ConfigDict(extra="allow", protected_namespaces=())

    result: Any = Field(default=None)


class RiskParityPortfolioRequest(BaseModel):
    """Request parameters for risk_parity_portfolio()."""

    cov_matrix: list[float] | list[list[float]]
    mean_returns: list[float] | list[list[float]] | None = None
    risk_free: float = 0.0
    periods_per_year: int = 252


class RiskParityPortfolioResponse(BaseModel):
    """Result of risk_parity_portfolio(). Mirrors the engine return dict."""

    model_config = ConfigDict(extra="allow", protected_namespaces=())

    result: Any = Field(default=None)


class EqualWeightPortfolioRequest(BaseModel):
    """Request parameters for equal_weight_portfolio()."""

    n_assets: int
    mean_returns: list[float] | list[list[float]] | None = None
    cov_matrix: list[float] | list[list[float]] | None = None
    risk_free: float = 0.0
    periods_per_year: int = 252


class EqualWeightPortfolioResponse(BaseModel):
    """Result of equal_weight_portfolio(). Mirrors the engine return dict."""

    model_config = ConfigDict(extra="allow", protected_namespaces=())

    n_assets: Any = Field(default=None)
    weights: Any = Field(default=None)


class BlackLittermanModelRequest(BaseModel):
    """Request parameters for black_litterman_model()."""

    market_weights: list[float] | list[list[float]]
    cov_matrix: list[float] | list[list[float]]
    p_matrix: list[float] | list[list[float]]
    q_views: list[float] | list[list[float]]
    risk_aversion: float = 2.5
    tau: float = 0.05
    omega: list[float] | list[list[float]] | None = None


class BlackLittermanModelResponse(BaseModel):
    """Result of black_litterman_model(). Mirrors the engine return dict."""

    model_config = ConfigDict(extra="allow", protected_namespaces=())

    implied_returns: Any = Field(default=None)
    posterior_returns: Any = Field(default=None)
    weights: Any = Field(default=None)


class ResampledEfficientFrontierRequest(BaseModel):
    """Request parameters for resampled_efficient_frontier()."""

    mean_returns: list[float] | list[list[float]]
    cov_matrix: list[float] | list[list[float]]
    n_resamples: int = 50
    n_obs: int = 250
    seed: int = 2024


class ResampledEfficientFrontierResponse(BaseModel):
    """Result of resampled_efficient_frontier(). Mirrors the engine return dict."""

    model_config = ConfigDict(extra="allow", protected_namespaces=())

    n_resamples: Any = Field(default=None)
    weight_std: Any = Field(default=None)
    weights: Any = Field(default=None)


class RobustPortfolioOptimisationRequest(BaseModel):
    """Request parameters for robust_portfolio_optimisation()."""

    mean_returns: list[float] | list[list[float]]
    cov_matrix: list[float] | list[list[float]]
    uncertainty: float = 0.05
    risk_aversion: float = 1.0
    periods_per_year: int = 252


class RobustPortfolioOptimisationResponse(BaseModel):
    """Result of robust_portfolio_optimisation(). Mirrors the engine return dict."""

    model_config = ConfigDict(extra="allow", protected_namespaces=())

    result: Any = Field(default=None)


class CvarConstrainedOptimisationRequest(BaseModel):
    """Request parameters for cvar_constrained_optimisation()."""

    scenario_returns: list[float] | list[list[float]]
    mean_returns: list[float] | list[list[float]] | None = None
    confidence_level: float = 0.95
    cvar_limit: float = 0.05
    periods_per_year: int = 252

    @field_validator("confidence_level")
    @classmethod
    def _confidence_range(cls, v: float) -> float:
        if v is not None and not (0.90 <= v <= 0.9999):
            raise ValueError(f"confidence_level {v} outside regulatory range [0.90, 0.9999]")
        return v


class CvarConstrainedOptimisationResponse(BaseModel):
    """Result of cvar_constrained_optimisation(). Mirrors the engine return dict."""

    model_config = ConfigDict(extra="allow", protected_namespaces=())

    cvar: Any = Field(default=None)
    expected_return: Any = Field(default=None)
    success: Any = Field(default=None)
    var: Any = Field(default=None)
    weights: Any = Field(default=None)


class FactorBasedOptimisationRequest(BaseModel):
    """Request parameters for factor_based_optimisation()."""

    factor_exposures: list[float] | list[list[float]]
    factor_cov: list[float] | list[list[float]]
    specific_var: list[float] | list[list[float]]
    target_exposures: list[float] | list[list[float]] | None = None
    periods_per_year: int = 252


class FactorBasedOptimisationResponse(BaseModel):
    """Result of factor_based_optimisation(). Mirrors the engine return dict."""

    model_config = ConfigDict(extra="allow", protected_namespaces=())

    factor_exposures: Any = Field(default=None)
    success: Any = Field(default=None)
    volatility: Any = Field(default=None)
    weights: Any = Field(default=None)


class SharpeRatioRequest(BaseModel):
    """Request parameters for sharpe_ratio()."""

    returns: list[float] | list[list[float]]
    risk_free: float = 0.0
    periods_per_year: int = 252
    ddof: int = 0


class SharpeRatioResponse(BaseModel):
    """Result of sharpe_ratio(). Mirrors the engine return dict."""

    model_config = ConfigDict(extra="allow", protected_namespaces=())

    ddof: Any = Field(default=None)
    mean_excess: Any = Field(default=None)
    periods_per_year: Any = Field(default=None)
    sharpe: Any = Field(default=None)
    sharpe_period: Any = Field(default=None)
    volatility: Any = Field(default=None)


class SortinoRatioRequest(BaseModel):
    """Request parameters for sortino_ratio()."""

    returns: list[float] | list[list[float]]
    risk_free: float = 0.0
    target: float = 0.0
    periods_per_year: int = 252


class SortinoRatioResponse(BaseModel):
    """Result of sortino_ratio(). Mirrors the engine return dict."""

    model_config = ConfigDict(extra="allow", protected_namespaces=())

    downside_deviation: Any = Field(default=None)
    mean_excess: Any = Field(default=None)
    periods_per_year: Any = Field(default=None)
    sortino: Any = Field(default=None)
    sortino_period: Any = Field(default=None)


class CalmarRatioRequest(BaseModel):
    """Request parameters for calmar_ratio()."""

    returns: list[float] | list[list[float]]
    periods_per_year: int = 252


class CalmarRatioResponse(BaseModel):
    """Result of calmar_ratio(). Mirrors the engine return dict."""

    model_config = ConfigDict(extra="allow", protected_namespaces=())

    annualised_return: Any = Field(default=None)
    calmar: Any = Field(default=None)
    max_drawdown: Any = Field(default=None)
    periods_per_year: Any = Field(default=None)


class InformationRatioRequest(BaseModel):
    """Request parameters for information_ratio()."""

    returns: list[float] | list[list[float]]
    benchmark_returns: list[float] | list[list[float]]
    periods_per_year: int = 252


class InformationRatioResponse(BaseModel):
    """Result of information_ratio(). Mirrors the engine return dict."""

    model_config = ConfigDict(extra="allow", protected_namespaces=())

    active_return: Any = Field(default=None)
    information_ratio: Any = Field(default=None)
    periods_per_year: Any = Field(default=None)
    tracking_error: Any = Field(default=None)


class TreynorRatioRequest(BaseModel):
    """Request parameters for treynor_ratio()."""

    returns: list[float] | list[list[float]]
    benchmark_returns: list[float] | list[list[float]]
    risk_free: float = 0.0
    periods_per_year: int = 252


class TreynorRatioResponse(BaseModel):
    """Result of treynor_ratio(). Mirrors the engine return dict."""

    model_config = ConfigDict(extra="allow", protected_namespaces=())

    beta: Any = Field(default=None)
    mean_excess: Any = Field(default=None)
    periods_per_year: Any = Field(default=None)
    treynor: Any = Field(default=None)


class JensensAlphaRequest(BaseModel):
    """Request parameters for jensens_alpha()."""

    returns: list[float] | list[list[float]]
    benchmark_returns: list[float] | list[list[float]]
    risk_free: float = 0.0
    periods_per_year: int = 252


class JensensAlphaResponse(BaseModel):
    """Result of jensens_alpha(). Mirrors the engine return dict."""

    model_config = ConfigDict(extra="allow", protected_namespaces=())

    alpha: Any = Field(default=None)
    alpha_period: Any = Field(default=None)
    beta: Any = Field(default=None)
    periods_per_year: Any = Field(default=None)


class OmegaRatioRequest(BaseModel):
    """Request parameters for omega_ratio()."""

    returns: list[float] | list[list[float]]
    threshold: float = 0.0


class OmegaRatioResponse(BaseModel):
    """Result of omega_ratio(). Mirrors the engine return dict."""

    model_config = ConfigDict(extra="allow", protected_namespaces=())

    gain: Any = Field(default=None)
    loss: Any = Field(default=None)
    omega: Any = Field(default=None)
    threshold: Any = Field(default=None)


class TailRatioRequest(BaseModel):
    """Request parameters for tail_ratio()."""

    returns: list[float] | list[list[float]]
    tail: float = 0.05


class TailRatioResponse(BaseModel):
    """Result of tail_ratio(). Mirrors the engine return dict."""

    model_config = ConfigDict(extra="allow", protected_namespaces=())

    left_tail: Any = Field(default=None)
    right_tail: Any = Field(default=None)
    tail: Any = Field(default=None)
    tail_ratio: Any = Field(default=None)


class UlcerIndexRequest(BaseModel):
    """Request parameters for ulcer_index()."""

    returns: list[float] | list[list[float]]
    is_equity_curve: bool = False


class UlcerIndexResponse(BaseModel):
    """Result of ulcer_index(). Mirrors the engine return dict."""

    model_config = ConfigDict(extra="allow", protected_namespaces=())

    n_obs: Any = Field(default=None)
    ulcer_index: Any = Field(default=None)


class MaximumDrawdownRequest(BaseModel):
    """Request parameters for maximum_drawdown()."""

    returns: list[float] | list[list[float]]
    is_equity_curve: bool = False


class MaximumDrawdownResponse(BaseModel):
    """Result of maximum_drawdown(). Mirrors the engine return dict."""

    model_config = ConfigDict(extra="allow", protected_namespaces=())

    max_drawdown: Any = Field(default=None)
    n_obs: Any = Field(default=None)
    peak_index: Any = Field(default=None)
    trough_index: Any = Field(default=None)


class AverageDrawdownRequest(BaseModel):
    """Request parameters for average_drawdown()."""

    returns: list[float] | list[list[float]]
    is_equity_curve: bool = False


class AverageDrawdownResponse(BaseModel):
    """Result of average_drawdown(). Mirrors the engine return dict."""

    model_config = ConfigDict(extra="allow", protected_namespaces=())

    average_drawdown: Any = Field(default=None)
    max_drawdown: Any = Field(default=None)
    n_obs: Any = Field(default=None)


class DrawdownDurationRequest(BaseModel):
    """Request parameters for drawdown_duration()."""

    returns: list[float] | list[list[float]]
    is_equity_curve: bool = False


class DrawdownDurationResponse(BaseModel):
    """Result of drawdown_duration(). Mirrors the engine return dict."""

    model_config = ConfigDict(extra="allow", protected_namespaces=())

    current_duration: Any = Field(default=None)
    max_duration: Any = Field(default=None)
    n_obs: Any = Field(default=None)


class ConditionalDrawdownAtRiskRequest(BaseModel):
    """Request parameters for conditional_drawdown_at_risk()."""

    returns: list[float] | list[list[float]]
    confidence_level: float = 0.95
    is_equity_curve: bool = False

    @field_validator("confidence_level")
    @classmethod
    def _confidence_range(cls, v: float) -> float:
        if v is not None and not (0.90 <= v <= 0.9999):
            raise ValueError(f"confidence_level {v} outside regulatory range [0.90, 0.9999]")
        return v


class ConditionalDrawdownAtRiskResponse(BaseModel):
    """Result of conditional_drawdown_at_risk(). Mirrors the engine return dict."""

    model_config = ConfigDict(extra="allow", protected_namespaces=())

    cdar: Any = Field(default=None)
    confidence_level: Any = Field(default=None)
    dar: Any = Field(default=None)
    n_obs: Any = Field(default=None)


class ReturnAttributionBrinsonRequest(BaseModel):
    """Request parameters for return_attribution_brinson()."""

    portfolio_weights: list[float] | list[list[float]]
    benchmark_weights: list[float] | list[list[float]]
    portfolio_returns: list[float] | list[list[float]]
    benchmark_returns: list[float] | list[list[float]]
    segment_names: list[str] | None = None


class ReturnAttributionBrinsonResponse(BaseModel):
    """Result of return_attribution_brinson(). Mirrors the engine return dict."""

    model_config = ConfigDict(extra="allow", protected_namespaces=())

    active_return: Any = Field(default=None)
    allocation: Any = Field(default=None)
    interaction: Any = Field(default=None)
    selection: Any = Field(default=None)
    total_allocation: Any = Field(default=None)
    total_interaction: Any = Field(default=None)
    total_selection: Any = Field(default=None)


class FactorReturnAttributionRequest(BaseModel):
    """Request parameters for factor_return_attribution()."""

    factor_exposures: list[float] | list[list[float]]
    factor_returns: list[float] | list[list[float]]
    specific_return: float
    factor_names: list[str] | None = None


class FactorReturnAttributionResponse(BaseModel):
    """Result of factor_return_attribution(). Mirrors the engine return dict."""

    model_config = ConfigDict(extra="allow", protected_namespaces=())

    contributions: Any = Field(default=None)
    factor_total: Any = Field(default=None)
    specific_return: Any = Field(default=None)
    total_return: Any = Field(default=None)


class SectorAttributionRequest(BaseModel):
    """Request parameters for sector_attribution()."""

    portfolio_weights: list[float] | list[list[float]]
    benchmark_weights: list[float] | list[list[float]]
    portfolio_returns: list[float] | list[list[float]]
    benchmark_returns: list[float] | list[list[float]]
    sector_names: list[str] | None = None


class SectorAttributionResponse(BaseModel):
    """Result of sector_attribution(). Mirrors the engine return dict."""

    model_config = ConfigDict(extra="allow", protected_namespaces=())

    result: Any = Field(default=None)


class CurrencyAttributionRequest(BaseModel):
    """Request parameters for currency_attribution()."""

    local_returns: list[float] | list[list[float]]
    fx_returns: list[float] | list[list[float]]
    weights: list[float] | list[list[float]]
    currency_names: list[str] | None = None
    local_risk_free: list[float] | list[list[float]] | None = None
    base_risk_free: float | None = None


class CurrencyAttributionResponse(BaseModel):
    """Result of currency_attribution(). Mirrors the engine return dict."""

    model_config = ConfigDict(extra="allow", protected_namespaces=())

    base_cash_effect: Any = Field(default=None)
    currency_effect: Any = Field(default=None)
    currency_interaction_effect: Any = Field(default=None)
    currency_surprise_effect: Any = Field(default=None)
    local_effect: Any = Field(default=None)
    total_base_cash: Any = Field(default=None)
    total_currency: Any = Field(default=None)
    total_currency_interaction: Any = Field(default=None)
    total_currency_surprise: Any = Field(default=None)
    total_local: Any = Field(default=None)
    total_return: Any = Field(default=None)


class GicsSectorExposureRequest(BaseModel):
    """Request parameters for gics_sector_exposure()."""

    weights: list[float] | list[list[float]]
    sector_codes: list[str]


class GicsSectorExposureResponse(BaseModel):
    """Result of gics_sector_exposure(). Mirrors the engine return dict."""

    model_config = ConfigDict(extra="allow", protected_namespaces=())

    largest_sector: Any = Field(default=None)
    n_sectors: Any = Field(default=None)
    sector_exposure: Any = Field(default=None)


class FactorExposureAnalysisBarraRequest(BaseModel):
    """Request parameters for factor_exposure_analysis_barra()."""

    asset_exposures: list[float] | list[list[float]]
    weights: list[float] | list[list[float]]
    factor_names: list[str] | None = None


class FactorExposureAnalysisBarraResponse(BaseModel):
    """Result of factor_exposure_analysis_barra(). Mirrors the engine return dict."""

    model_config = ConfigDict(extra="allow", protected_namespaces=())

    dominant_factor: Any = Field(default=None)
    factor_exposures: Any = Field(default=None)


class FamaFrench3FactorModelRequest(BaseModel):
    """Request parameters for fama_french_3_factor_model()."""

    excess_returns: list[float] | list[list[float]]
    factors: list[float] | list[list[float]]


class FamaFrench3FactorModelResponse(BaseModel):
    """Result of fama_french_3_factor_model(). Mirrors the engine return dict."""

    model_config = ConfigDict(extra="allow", protected_namespaces=())

    result: Any = Field(default=None)


class Carhart4FactorModelRequest(BaseModel):
    """Request parameters for carhart_4_factor_model()."""

    excess_returns: list[float] | list[list[float]]
    factors: list[float] | list[list[float]]


class Carhart4FactorModelResponse(BaseModel):
    """Result of carhart_4_factor_model(). Mirrors the engine return dict."""

    model_config = ConfigDict(extra="allow", protected_namespaces=())

    result: Any = Field(default=None)


class FamaFrench5FactorModelRequest(BaseModel):
    """Request parameters for fama_french_5_factor_model()."""

    excess_returns: list[float] | list[list[float]]
    factors: list[float] | list[list[float]]


class FamaFrench5FactorModelResponse(BaseModel):
    """Result of fama_french_5_factor_model(). Mirrors the engine return dict."""

    model_config = ConfigDict(extra="allow", protected_namespaces=())

    result: Any = Field(default=None)


class PrincipalComponentAnalysisRequest(BaseModel):
    """Request parameters for principal_component_analysis()."""

    returns_matrix: list[float] | list[list[float]]
    n_components: int | None = None


class PrincipalComponentAnalysisResponse(BaseModel):
    """Result of principal_component_analysis(). Mirrors the engine return dict."""

    model_config = ConfigDict(extra="allow", protected_namespaces=())

    components: Any = Field(default=None)
    cumulative_variance: Any = Field(default=None)
    eigenvalues: Any = Field(default=None)
    explained_variance_ratio: Any = Field(default=None)
    n_components: Any = Field(default=None)


class CorrelationClusteringRequest(BaseModel):
    """Request parameters for correlation_clustering()."""

    returns_matrix: list[float] | list[list[float]]
    n_clusters: int = 2


class CorrelationClusteringResponse(BaseModel):
    """Result of correlation_clustering(). Mirrors the engine return dict."""

    model_config = ConfigDict(extra="allow", protected_namespaces=())

    average_intra_correlation: Any = Field(default=None)
    labels: Any = Field(default=None)
    n_clusters: Any = Field(default=None)


class RegimeDetectionHmmRequest(BaseModel):
    """Request parameters for regime_detection_hmm()."""

    returns: list[float] | list[list[float]]
    n_iter: int = 50


class RegimeDetectionHmmResponse(BaseModel):
    """Result of regime_detection_hmm(). Mirrors the engine return dict."""

    model_config = ConfigDict(extra="allow", protected_namespaces=())

    current_regime: Any = Field(default=None)
    means: Any = Field(default=None)
    regime_labels: Any = Field(default=None)
    stress_regime: Any = Field(default=None)
    variances: Any = Field(default=None)
    weights: Any = Field(default=None)


class PortfolioBetaRequest(BaseModel):
    """Request parameters for portfolio_beta()."""

    returns: list[float] | list[list[float]]
    benchmark_returns: list[float] | list[list[float]]


class PortfolioBetaResponse(BaseModel):
    """Result of portfolio_beta(). Mirrors the engine return dict."""

    model_config = ConfigDict(extra="allow", protected_namespaces=())

    beta: Any = Field(default=None)
    correlation: Any = Field(default=None)
    r_squared: Any = Field(default=None)


class ActiveShareRequest(BaseModel):
    """Request parameters for active_share()."""

    weights: list[float] | list[list[float]]
    benchmark_weights: list[float] | list[list[float]]


class ActiveShareResponse(BaseModel):
    """Result of active_share(). Mirrors the engine return dict."""

    model_config = ConfigDict(extra="allow", protected_namespaces=())

    active_share: Any = Field(default=None)
    n_assets: Any = Field(default=None)


class TrackingErrorRequest(BaseModel):
    """Request parameters for tracking_error()."""

    returns: list[float] | list[list[float]]
    benchmark_returns: list[float] | list[list[float]]
    periods_per_year: int = 252


class TrackingErrorResponse(BaseModel):
    """Result of tracking_error(). Mirrors the engine return dict."""

    model_config = ConfigDict(extra="allow", protected_namespaces=())

    mean_active_return: Any = Field(default=None)
    periods_per_year: Any = Field(default=None)
    tracking_error: Any = Field(default=None)
    tracking_error_period: Any = Field(default=None)


class ResidualRiskRequest(BaseModel):
    """Request parameters for residual_risk()."""

    returns: list[float] | list[list[float]]
    benchmark_returns: list[float] | list[list[float]]
    periods_per_year: int = 252


class ResidualRiskResponse(BaseModel):
    """Result of residual_risk(). Mirrors the engine return dict."""

    model_config = ConfigDict(extra="allow", protected_namespaces=())

    alpha: Any = Field(default=None)
    beta: Any = Field(default=None)
    periods_per_year: Any = Field(default=None)
    residual_risk: Any = Field(default=None)
    residual_risk_period: Any = Field(default=None)


class PortfolioTurnoverRequest(BaseModel):
    """Request parameters for portfolio_turnover()."""

    weights_before: list[float] | list[list[float]]
    weights_after: list[float] | list[list[float]]


class PortfolioTurnoverResponse(BaseModel):
    """Result of portfolio_turnover(). Mirrors the engine return dict."""

    model_config = ConfigDict(extra="allow", protected_namespaces=())

    turnover: Any = Field(default=None)
    two_way_turnover: Any = Field(default=None)


class TransactionCostAnalysisRequest(BaseModel):
    """Request parameters for transaction_cost_analysis()."""

    trade_prices: list[float] | list[list[float]]
    benchmark_prices: list[float] | list[list[float]]
    trade_quantities: list[float] | list[list[float]]
    side: int = 1
    decision_price: float | list[float] | None = None


class TransactionCostAnalysisResponse(BaseModel):
    """Result of transaction_cost_analysis(). Mirrors the engine return dict."""

    model_config = ConfigDict(extra="allow", protected_namespaces=())

    delay_cost: Any = Field(default=None)
    delay_cost_bps: Any = Field(default=None)
    implementation_shortfall: Any = Field(default=None)
    implementation_shortfall_bps: Any = Field(default=None)
    n_fills: Any = Field(default=None)
    slippage_bps: Any = Field(default=None)
    total_cost: Any = Field(default=None)
    total_quantity: Any = Field(default=None)


class MarginalContributionToRiskRequest(BaseModel):
    """Request parameters for marginal_contribution_to_risk()."""

    weights: list[float] | list[list[float]]
    cov_matrix: list[float] | list[list[float]]


class MarginalContributionToRiskResponse(BaseModel):
    """Result of marginal_contribution_to_risk(). Mirrors the engine return dict."""

    model_config = ConfigDict(extra="allow", protected_namespaces=())

    component: Any = Field(default=None)
    marginal: Any = Field(default=None)
    percent_contribution: Any = Field(default=None)
    portfolio_volatility: Any = Field(default=None)


class DiversificationRatioRequest(BaseModel):
    """Request parameters for diversification_ratio()."""

    weights: list[float] | list[list[float]]
    cov_matrix: list[float] | list[list[float]]


class DiversificationRatioResponse(BaseModel):
    """Result of diversification_ratio(). Mirrors the engine return dict."""

    model_config = ConfigDict(extra="allow", protected_namespaces=())

    diversification_ratio: Any = Field(default=None)
    portfolio_volatility: Any = Field(default=None)
    weighted_avg_vol: Any = Field(default=None)


class CorrelationMatrixPortfolioRequest(BaseModel):
    """Request parameters for correlation_matrix_portfolio()."""

    returns_matrix: list[float] | list[list[float]]


class CorrelationMatrixPortfolioResponse(BaseModel):
    """Result of correlation_matrix_portfolio(). Mirrors the engine return dict."""

    model_config = ConfigDict(extra="allow", protected_namespaces=())

    average_correlation: Any = Field(default=None)
    correlation_matrix: Any = Field(default=None)
    n_assets: Any = Field(default=None)


class ConcentrationRiskHhiRequest(BaseModel):
    """Request parameters for concentration_risk_hhi()."""

    weights: list[float] | list[list[float]]


class ConcentrationRiskHhiResponse(BaseModel):
    """Result of concentration_risk_hhi(). Mirrors the engine return dict."""

    model_config = ConfigDict(extra="allow", protected_namespaces=())

    effective_n: Any = Field(default=None)
    hhi: Any = Field(default=None)
    n_assets: Any = Field(default=None)
    normalised_hhi: Any = Field(default=None)


class LiquidityAdjustedPortfolioVarRequest(BaseModel):
    """Request parameters for liquidity_adjusted_portfolio_var()."""

    weights: list[float] | list[list[float]]
    cov_matrix: list[float] | list[list[float]]
    bid_ask_spreads: list[float] | list[list[float]]
    portfolio_value: float
    confidence_level: float = 0.99
    horizon_days: int = 1

    @field_validator("confidence_level")
    @classmethod
    def _confidence_range(cls, v: float) -> float:
        if v is not None and not (0.90 <= v <= 0.9999):
            raise ValueError(f"confidence_level {v} outside regulatory range [0.90, 0.9999]")
        return v


class LiquidityAdjustedPortfolioVarResponse(BaseModel):
    """Result of liquidity_adjusted_portfolio_var(). Mirrors the engine return dict."""

    model_config = ConfigDict(extra="allow", protected_namespaces=())

    confidence_level: Any = Field(default=None)
    horizon_days: Any = Field(default=None)
    liquidity_cost_pct: Any = Field(default=None)
    lvar_abs: Any = Field(default=None)
    lvar_pct: Any = Field(default=None)
    market_var_pct: Any = Field(default=None)


class MonteCarloPortfolioSimulationRequest(BaseModel):
    """Request parameters for monte_carlo_portfolio_simulation()."""

    weights: list[float] | list[list[float]]
    mean_returns: list[float] | list[list[float]]
    cov_matrix: list[float] | list[list[float]]
    portfolio_value: float = 1000000.0
    horizon: int = 10
    n_simulations: int = 10000
    confidence_level: float = 0.99
    seed: int = 12345

    @field_validator("confidence_level")
    @classmethod
    def _confidence_range(cls, v: float) -> float:
        if v is not None and not (0.90 <= v <= 0.9999):
            raise ValueError(f"confidence_level {v} outside regulatory range [0.90, 0.9999]")
        return v


class MonteCarloPortfolioSimulationResponse(BaseModel):
    """Result of monte_carlo_portfolio_simulation(). Mirrors the engine return dict."""

    model_config = ConfigDict(extra="allow", protected_namespaces=())

    confidence_level: Any = Field(default=None)
    cvar_abs: Any = Field(default=None)
    expected_pnl: Any = Field(default=None)
    n_simulations: Any = Field(default=None)
    std_pnl: Any = Field(default=None)
    var_abs: Any = Field(default=None)


class RebalancingOptimiserRequest(BaseModel):
    """Request parameters for rebalancing_optimiser()."""

    current_weights: list[float] | list[list[float]]
    target_weights: list[float] | list[list[float]]
    cost_bps: list[float] | list[list[float]]
    no_trade_band: float = 0.0
    asset_volatility: list[float] | list[list[float]] | None = None
    risk_aversion: float | None = None


class RebalancingOptimiserResponse(BaseModel):
    """Result of rebalancing_optimiser(). Mirrors the engine return dict."""

    model_config = ConfigDict(extra="allow", protected_namespaces=())

    derived_no_trade_band: Any = Field(default=None)
    new_weights: Any = Field(default=None)
    total_cost: Any = Field(default=None)
    trades: Any = Field(default=None)
    turnover: Any = Field(default=None)


class EsgScoreIntegrationRequest(BaseModel):
    """Request parameters for esg_score_integration()."""

    weights: list[float] | list[list[float]]
    esg_scores: list[float] | list[list[float]]
    min_esg_score: float | None = None
    cov_matrix: list[float] | list[list[float]] | None = None


class EsgScoreIntegrationResponse(BaseModel):
    """Result of esg_score_integration(). Mirrors the engine return dict."""

    model_config = ConfigDict(extra="allow", protected_namespaces=())

    result: Any = Field(default=None)


class CarbonFootprintAttributionRequest(BaseModel):
    """Request parameters for carbon_footprint_attribution()."""

    weights: list[float] | list[list[float]]
    carbon_intensities: list[float] | list[list[float]]
    portfolio_value: float
    asset_names: list[str] | None = None


class CarbonFootprintAttributionResponse(BaseModel):
    """Result of carbon_footprint_attribution(). Mirrors the engine return dict."""

    model_config = ConfigDict(extra="allow", protected_namespaces=())

    contributions: Any = Field(default=None)
    largest_contributor: Any = Field(default=None)
    total_financed_emissions: Any = Field(default=None)
    waci: Any = Field(default=None)

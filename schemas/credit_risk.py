"""schemas/credit_risk.py — auto-generated Pydantic v2 contracts for the credit-risk domain.

Generated by scripts/gen_p3.py from engine signatures. Request models are
precisely typed and enforce regulatory validators (CLAUDE.md s4). Response
models document engine return-dict keys; extra='allow' covers dynamically
built keys that static analysis cannot enumerate.
"""

from __future__ import annotations

from typing import Any

from pydantic import BaseModel, ConfigDict, Field, field_validator


class ProbabilityOfDefaultPdEstimationRequest(BaseModel):
    """Request parameters for probability_of_default_pd_estimation()."""

    n_defaults: list[float] | list[list[float]]
    n_obligors: list[float] | list[list[float]]
    floor: float = 0.0003


class ProbabilityOfDefaultPdEstimationResponse(BaseModel):
    """Result of probability_of_default_pd_estimation(). Mirrors the engine return dict."""

    model_config = ConfigDict(extra="allow", protected_namespaces=())

    floor: Any = Field(default=None)
    n_total: Any = Field(default=None)
    pd_cohort: Any = Field(default=None)
    pd_pooled: Any = Field(default=None)
    pd_raw: Any = Field(default=None)


class LossGivenDefaultLgdModelRequest(BaseModel):
    """Request parameters for loss_given_default_lgd_model()."""

    recovery_amounts: list[float] | list[list[float]]
    exposure_amounts: list[float] | list[list[float]]
    workout_cost_rate: float = 0.0


class LossGivenDefaultLgdModelResponse(BaseModel):
    """Result of loss_given_default_lgd_model(). Mirrors the engine return dict."""

    model_config = ConfigDict(extra="allow", protected_namespaces=())

    lgd: Any = Field(default=None)
    lgd_mean: Any = Field(default=None)
    n_facilities: Any = Field(default=None)
    recovery_rate: Any = Field(default=None)


class ExposureAtDefaultEadCalculatorRequest(BaseModel):
    """Request parameters for exposure_at_default_ead_calculator()."""

    drawn: float
    undrawn: float
    credit_conversion_factor: float = 0.75


class ExposureAtDefaultEadCalculatorResponse(BaseModel):
    """Result of exposure_at_default_ead_calculator(). Mirrors the engine return dict."""

    model_config = ConfigDict(extra="allow", protected_namespaces=())

    ccf: Any = Field(default=None)
    drawn: Any = Field(default=None)
    ead: Any = Field(default=None)
    undrawn: Any = Field(default=None)


class ExpectedLossElComputationRequest(BaseModel):
    """Request parameters for expected_loss_el_computation()."""

    pd: float
    lgd: float
    ead: float


class ExpectedLossElComputationResponse(BaseModel):
    """Result of expected_loss_el_computation(). Mirrors the engine return dict."""

    model_config = ConfigDict(extra="allow", protected_namespaces=())

    ead: Any = Field(default=None)
    el: Any = Field(default=None)
    el_rate: Any = Field(default=None)
    lgd: Any = Field(default=None)
    pd: Any = Field(default=None)


class UnexpectedLossUlComputationRequest(BaseModel):
    """Request parameters for unexpected_loss_ul_computation()."""

    pd: list[float] | list[list[float]]
    lgd: list[float] | list[list[float]]
    ead: list[float] | list[list[float]]


class UnexpectedLossUlComputationResponse(BaseModel):
    """Result of unexpected_loss_ul_computation(). Mirrors the engine return dict."""

    model_config = ConfigDict(extra="allow", protected_namespaces=())

    n_exposures: Any = Field(default=None)
    ul: Any = Field(default=None)
    ul_independent: Any = Field(default=None)
    ul_sum: Any = Field(default=None)


class RecoveryRateEstimationRequest(BaseModel):
    """Request parameters for recovery_rate_estimation()."""

    recovery_amounts: list[float] | list[list[float]]
    exposure_amounts: list[float] | list[list[float]]
    discount_factors: list[float] | list[list[float]] | None = None


class RecoveryRateEstimationResponse(BaseModel):
    """Result of recovery_rate_estimation(). Mirrors the engine return dict."""

    model_config = ConfigDict(extra="allow", protected_namespaces=())

    lgd: Any = Field(default=None)
    n_facilities: Any = Field(default=None)
    recovery_rate: Any = Field(default=None)
    recovery_rate_mean: Any = Field(default=None)


class DownturnLgdAdjustmentRequest(BaseModel):
    """Request parameters for downturn_lgd_adjustment()."""

    lgd_long_run: float
    downturn_multiplier: float = 1.0
    floor: float = 0.0
    method: str = "multiplicative"


class DownturnLgdAdjustmentResponse(BaseModel):
    """Result of downturn_lgd_adjustment(). Mirrors the engine return dict."""

    model_config = ConfigDict(extra="allow", protected_namespaces=())

    floor: Any = Field(default=None)
    lgd_downturn: Any = Field(default=None)
    lgd_long_run: Any = Field(default=None)
    multiplier: Any = Field(default=None)
    method: Any = Field(default=None)


class IrbFoundationApproachCapitalRequest(BaseModel):
    """Request parameters for irb_foundation_approach_capital()."""

    pd: float
    ead: float
    maturity: float = 2.5
    lgd: float = 0.45
    seniority: str = "senior_unsecured"


class IrbFoundationApproachCapitalResponse(BaseModel):
    """Result of irb_foundation_approach_capital(). Mirrors the engine return dict."""

    model_config = ConfigDict(extra="allow", protected_namespaces=())

    result: Any = Field(default=None)


class IrbAdvancedApproachCapitalRequest(BaseModel):
    """Request parameters for irb_advanced_approach_capital()."""

    pd: float
    lgd: float
    ead: float
    maturity: float = 2.5
    correlation: float | None = None


class IrbAdvancedApproachCapitalResponse(BaseModel):
    """Result of irb_advanced_approach_capital(). Mirrors the engine return dict."""

    model_config = ConfigDict(extra="allow", protected_namespaces=())

    result: Any = Field(default=None)


class BaselStandardisedApproachRwaRequest(BaseModel):
    """Request parameters for basel_standardised_approach_rwa()."""

    ead: float
    risk_weight: float
    credit_risk_mitigation: float = 0.0


class BaselStandardisedApproachRwaResponse(BaseModel):
    """Result of basel_standardised_approach_rwa(). Mirrors the engine return dict."""

    model_config = ConfigDict(extra="allow", protected_namespaces=())

    capital_required: Any = Field(default=None)
    net_exposure: Any = Field(default=None)
    risk_weight: Any = Field(default=None)
    rwa: Any = Field(default=None)


class MaturityAdjustmentBaselIrbRequest(BaseModel):
    """Request parameters for maturity_adjustment_basel_irb()."""

    pd: float
    maturity: float


class MaturityAdjustmentBaselIrbResponse(BaseModel):
    """Result of maturity_adjustment_basel_irb(). Mirrors the engine return dict."""

    model_config = ConfigDict(extra="allow", protected_namespaces=())

    b: Any = Field(default=None)
    maturity: Any = Field(default=None)
    maturity_adjustment: Any = Field(default=None)
    pd_used: Any = Field(default=None)


class SmeCorrelationFactorBaselRequest(BaseModel):
    """Request parameters for sme_correlation_factor_basel()."""

    pd: float
    annual_sales_millions: float


class SmeCorrelationFactorBaselResponse(BaseModel):
    """Result of sme_correlation_factor_basel(). Mirrors the engine return dict."""

    model_config = ConfigDict(extra="allow", protected_namespaces=())

    correlation_corporate: Any = Field(default=None)
    correlation_sme: Any = Field(default=None)
    sales_used: Any = Field(default=None)
    size_adjustment: Any = Field(default=None)


class CreditVarMonteCarloRequest(BaseModel):
    """Request parameters for credit_var_monte_carlo()."""

    pd: list[float] | list[list[float]]
    lgd: list[float] | list[list[float]]
    ead: list[float] | list[list[float]]
    asset_correlation: float | list[float] = 0.15
    confidence_level: float = 0.999
    n_simulations: int = 50000
    seed: int = 12345

    @field_validator("confidence_level")
    @classmethod
    def _confidence_range(cls, v: float) -> float:
        if v is not None and not (0.90 <= v <= 0.9999):
            raise ValueError(f"confidence_level {v} outside regulatory range [0.90, 0.9999]")
        return v


class CreditVarMonteCarloResponse(BaseModel):
    """Result of credit_var_monte_carlo(). Mirrors the engine return dict."""

    model_config = ConfigDict(extra="allow", protected_namespaces=())

    confidence_level: Any = Field(default=None)
    cvar: Any = Field(default=None)
    el: Any = Field(default=None)
    loss_std: Any = Field(default=None)
    n_simulations: Any = Field(default=None)
    ul: Any = Field(default=None)
    var: Any = Field(default=None)


class CreditVarAnalyticalVasicekRequest(BaseModel):
    """Request parameters for credit_var_analytical_vasicek()."""

    pd: float
    lgd: float
    ead_total: float
    asset_correlation: float = 0.15
    confidence_level: float = 0.999

    @field_validator("confidence_level")
    @classmethod
    def _confidence_range(cls, v: float) -> float:
        if v is not None and not (0.90 <= v <= 0.9999):
            raise ValueError(f"confidence_level {v} outside regulatory range [0.90, 0.9999]")
        return v


class CreditVarAnalyticalVasicekResponse(BaseModel):
    """Result of credit_var_analytical_vasicek(). Mirrors the engine return dict."""

    model_config = ConfigDict(extra="allow", protected_namespaces=())

    confidence_level: Any = Field(default=None)
    el: Any = Field(default=None)
    loss_rate_q: Any = Field(default=None)
    ul: Any = Field(default=None)
    var: Any = Field(default=None)


class CreditmetricsPortfolioModelRequest(BaseModel):
    """Request parameters for creditmetrics_portfolio_model()."""

    exposures: list[float] | list[list[float]]
    pd: list[float] | list[list[float]]
    lgd: list[float] | list[list[float]]
    asset_correlation: float = 0.2
    confidence_level: float = 0.99
    n_simulations: int = 20000
    seed: int = 2024
    transition_matrix: list[list[float]] | None = None
    current_rating: list[int] | None = None
    state_loss_pct: list[float] | None = None

    @field_validator("confidence_level")
    @classmethod
    def _confidence_range(cls, v: float) -> float:
        if v is not None and not (0.90 <= v <= 0.9999):
            raise ValueError(f"confidence_level {v} outside regulatory range [0.90, 0.9999]")
        return v


class CreditmetricsPortfolioModelResponse(BaseModel):
    """Result of creditmetrics_portfolio_model(). Mirrors the engine return dict."""

    model_config = ConfigDict(extra="allow", protected_namespaces=())

    result: Any = Field(default=None)


class KmvMertonDistanceToDefaultRequest(BaseModel):
    """Request parameters for kmv_merton_distance_to_default()."""

    asset_value: float
    debt_face_value: float
    asset_volatility: float
    risk_free_rate: float = 0.0
    asset_drift: float | None = None
    horizon: float = 1.0


class KmvMertonDistanceToDefaultResponse(BaseModel):
    """Result of kmv_merton_distance_to_default(). Mirrors the engine return dict."""

    model_config = ConfigDict(extra="allow", protected_namespaces=())

    distance_to_default: Any = Field(default=None)
    horizon: Any = Field(default=None)
    leverage: Any = Field(default=None)
    pd: Any = Field(default=None)


class DefaultCorrelationMatrixRequest(BaseModel):
    """Request parameters for default_correlation_matrix()."""

    pd: list[float] | list[list[float]]
    asset_correlation: list[float] | list[list[float]]


class DefaultCorrelationMatrixResponse(BaseModel):
    """Result of default_correlation_matrix(). Mirrors the engine return dict."""

    model_config = ConfigDict(extra="allow", protected_namespaces=())

    matrix: Any = Field(default=None)
    n: Any = Field(default=None)


class CreditConcentrationRiskHhiRequest(BaseModel):
    """Request parameters for credit_concentration_risk_hhi()."""

    exposures: list[float] | list[list[float]]


class CreditConcentrationRiskHhiResponse(BaseModel):
    """Result of credit_concentration_risk_hhi(). Mirrors the engine return dict."""

    model_config = ConfigDict(extra="allow", protected_namespaces=())

    effective_n: Any = Field(default=None)
    hhi: Any = Field(default=None)
    max_share: Any = Field(default=None)
    n_names: Any = Field(default=None)


class CounterpartyCreditRiskCcrExposureRequest(BaseModel):
    """Request parameters for counterparty_credit_risk_ccr_exposure()."""

    mark_to_market: float
    add_on: float
    collateral: float = 0.0


class CounterpartyCreditRiskCcrExposureResponse(BaseModel):
    """Result of counterparty_credit_risk_ccr_exposure(). Mirrors the engine return dict."""

    model_config = ConfigDict(extra="allow", protected_namespaces=())

    add_on: Any = Field(default=None)
    current_exposure: Any = Field(default=None)
    exposure: Any = Field(default=None)


class CurrentExposureMethodCemRequest(BaseModel):
    """Request parameters for current_exposure_method_cem()."""

    mark_to_market: float
    notional: float
    add_on_factor: float


class CurrentExposureMethodCemResponse(BaseModel):
    """Result of current_exposure_method_cem(). Mirrors the engine return dict."""

    model_config = ConfigDict(extra="allow", protected_namespaces=())

    ead: Any = Field(default=None)
    potential_future_exposure: Any = Field(default=None)
    replacement_cost: Any = Field(default=None)


class StandardisedApproachCcrSaCcrRequest(BaseModel):
    """Request parameters for standardised_approach_ccr_sa_ccr()."""

    mark_to_market: float
    collateral: float
    add_on_aggregate: float
    alpha: float = 1.4


class StandardisedApproachCcrSaCcrResponse(BaseModel):
    """Result of standardised_approach_ccr_sa_ccr(). Mirrors the engine return dict."""

    model_config = ConfigDict(extra="allow", protected_namespaces=())

    alpha: Any = Field(default=None)
    ead: Any = Field(default=None)
    multiplier: Any = Field(default=None)
    pfe: Any = Field(default=None)
    replacement_cost: Any = Field(default=None)


class PotentialFutureExposurePfeRequest(BaseModel):
    """Request parameters for potential_future_exposure_pfe()."""

    initial_value: float
    volatility: float
    time_steps: list[float] | list[list[float]]
    quantile: float = 0.95
    drift: float = 0.0
    n_paths: int = 20000
    seed: int = 909


class PotentialFutureExposurePfeResponse(BaseModel):
    """Result of potential_future_exposure_pfe(). Mirrors the engine return dict."""

    model_config = ConfigDict(extra="allow", protected_namespaces=())

    ee: Any = Field(default=None)
    peak_pfe: Any = Field(default=None)
    pfe: Any = Field(default=None)
    quantile: Any = Field(default=None)
    time_steps: Any = Field(default=None)


class ExpectedPositiveExposureEpeRequest(BaseModel):
    """Request parameters for expected_positive_exposure_epe()."""

    expected_exposure: list[float] | list[list[float]]
    time_steps: list[float] | list[list[float]]


class ExpectedPositiveExposureEpeResponse(BaseModel):
    """Result of expected_positive_exposure_epe(). Mirrors the engine return dict."""

    model_config = ConfigDict(extra="allow", protected_namespaces=())

    epe: Any = Field(default=None)
    horizon: Any = Field(default=None)


class EffectiveEpeRegulatoryRequest(BaseModel):
    """Request parameters for effective_epe_regulatory()."""

    expected_exposure: list[float] | list[list[float]]
    time_steps: list[float] | list[list[float]]


class EffectiveEpeRegulatoryResponse(BaseModel):
    """Result of effective_epe_regulatory(). Mirrors the engine return dict."""

    model_config = ConfigDict(extra="allow", protected_namespaces=())

    eee: Any = Field(default=None)
    eepe: Any = Field(default=None)
    horizon: Any = Field(default=None)


class CollateralHaircutCalculationRequest(BaseModel):
    """Request parameters for collateral_haircut_calculation()."""

    collateral_value: float
    haircut_collateral: float
    haircut_fx: float = 0.0


class CollateralHaircutCalculationResponse(BaseModel):
    """Result of collateral_haircut_calculation(). Mirrors the engine return dict."""

    model_config = ConfigDict(extra="allow", protected_namespaces=())

    adjusted_collateral: Any = Field(default=None)
    collateral_value: Any = Field(default=None)
    total_haircut: Any = Field(default=None)


class CreditValuationAdjustmentCvaRequest(BaseModel):
    """Request parameters for credit_valuation_adjustment_cva()."""

    expected_exposure: list[float] | list[list[float]]
    time_steps: list[float] | list[list[float]]
    discount_factors: list[float] | list[list[float]]
    credit_spread: float
    recovery_rate: float = 0.4


class CreditValuationAdjustmentCvaResponse(BaseModel):
    """Result of credit_valuation_adjustment_cva(). Mirrors the engine return dict."""

    model_config = ConfigDict(extra="allow", protected_namespaces=())

    cva: Any = Field(default=None)
    hazard_rate: Any = Field(default=None)
    lgd: Any = Field(default=None)
    n_buckets: Any = Field(default=None)


class DebtValuationAdjustmentDvaRequest(BaseModel):
    """Request parameters for debt_valuation_adjustment_dva()."""

    expected_negative_exposure: list[float] | list[list[float]]
    time_steps: list[float] | list[list[float]]
    discount_factors: list[float] | list[list[float]]
    own_credit_spread: float
    own_recovery_rate: float = 0.4


class DebtValuationAdjustmentDvaResponse(BaseModel):
    """Result of debt_valuation_adjustment_dva(). Mirrors the engine return dict."""

    model_config = ConfigDict(extra="allow", protected_namespaces=())

    dva: Any = Field(default=None)
    hazard_rate: Any = Field(default=None)
    lgd: Any = Field(default=None)
    n_buckets: Any = Field(default=None)


class FundingValuationAdjustmentFvaRequest(BaseModel):
    """Request parameters for funding_valuation_adjustment_fva()."""

    expected_exposure: list[float] | list[list[float]]
    time_steps: list[float] | list[list[float]]
    discount_factors: list[float] | list[list[float]]
    funding_spread: float
    survival_probability: list[float] | list[list[float]] | None = None


class FundingValuationAdjustmentFvaResponse(BaseModel):
    """Result of funding_valuation_adjustment_fva(). Mirrors the engine return dict."""

    model_config = ConfigDict(extra="allow", protected_namespaces=())

    fva: Any = Field(default=None)
    n_buckets: Any = Field(default=None)


class CapitalValuationAdjustmentKvaRequest(BaseModel):
    """Request parameters for capital_valuation_adjustment_kva()."""

    capital_profile: list[float] | list[list[float]]
    time_steps: list[float] | list[list[float]]
    discount_factors: list[float] | list[list[float]]
    cost_of_capital: float = 0.1


class CapitalValuationAdjustmentKvaResponse(BaseModel):
    """Result of capital_valuation_adjustment_kva(). Mirrors the engine return dict."""

    model_config = ConfigDict(extra="allow", protected_namespaces=())

    kva: Any = Field(default=None)
    n_buckets: Any = Field(default=None)


class MarginValuationAdjustmentMvaRequest(BaseModel):
    """Request parameters for margin_valuation_adjustment_mva()."""

    initial_margin_profile: list[float] | list[list[float]]
    time_steps: list[float] | list[list[float]]
    discount_factors: list[float] | list[list[float]]
    margin_funding_spread: float


class MarginValuationAdjustmentMvaResponse(BaseModel):
    """Result of margin_valuation_adjustment_mva(). Mirrors the engine return dict."""

    model_config = ConfigDict(extra="allow", protected_namespaces=())

    mva: Any = Field(default=None)
    n_buckets: Any = Field(default=None)


class XvaAggregationRequest(BaseModel):
    """Request parameters for xva_aggregation()."""

    cva: float
    dva: float = 0.0
    fva: float = 0.0
    kva: float = 0.0
    mva: float = 0.0


class XvaAggregationResponse(BaseModel):
    """Result of xva_aggregation(). Mirrors the engine return dict."""

    model_config = ConfigDict(extra="allow", protected_namespaces=())

    components: Any = Field(default=None)
    total_xva: Any = Field(default=None)


class CvaSensitivityCvaGreeksRequest(BaseModel):
    """Request parameters for cva_sensitivity_cva_greeks()."""

    expected_exposure: list[float] | list[list[float]]
    time_steps: list[float] | list[list[float]]
    discount_factors: list[float] | list[list[float]]
    credit_spread: float
    recovery_rate: float = 0.4
    spread_bump: float = 0.0001


class CvaSensitivityCvaGreeksResponse(BaseModel):
    """Result of cva_sensitivity_cva_greeks(). Mirrors the engine return dict."""

    model_config = ConfigDict(extra="allow", protected_namespaces=())

    cs01: Any = Field(default=None)
    cva: Any = Field(default=None)
    exposure_delta: Any = Field(default=None)
    spread_bump: Any = Field(default=None)


class WrongWayRiskAdjustmentRequest(BaseModel):
    """Request parameters for wrong_way_risk_adjustment()."""

    base_cva: float
    correlation: float
    exposure_volatility: float = 0.3


class WrongWayRiskAdjustmentResponse(BaseModel):
    """Result of wrong_way_risk_adjustment(). Mirrors the engine return dict."""

    model_config = ConfigDict(extra="allow", protected_namespaces=())

    adjusted_cva: Any = Field(default=None)
    alpha: Any = Field(default=None)
    wwr_addon: Any = Field(default=None)


class CreditSpreadCurveBootstrapRequest(BaseModel):
    """Request parameters for credit_spread_curve_bootstrap()."""

    tenors: list[float] | list[list[float]]
    par_spreads: list[float] | list[list[float]]
    recovery_rate: float = 0.4


class CreditSpreadCurveBootstrapResponse(BaseModel):
    """Result of credit_spread_curve_bootstrap(). Mirrors the engine return dict."""

    model_config = ConfigDict(extra="allow", protected_namespaces=())

    default_prob: Any = Field(default=None)
    hazard_rates: Any = Field(default=None)
    lgd: Any = Field(default=None)
    survival: Any = Field(default=None)


class CdsPricingIsdaStandardRequest(BaseModel):
    """Request parameters for cds_pricing_isda_standard()."""

    payment_times: list[float] | list[list[float]]
    accrual_factors: list[float] | list[list[float]]
    discount_factors: list[float] | list[list[float]]
    hazard_rate: float
    contract_spread: float
    notional: float = 1.0
    recovery_rate: float = 0.4


class CdsPricingIsdaStandardResponse(BaseModel):
    """Result of cds_pricing_isda_standard(). Mirrors the engine return dict."""

    model_config = ConfigDict(extra="allow", protected_namespaces=())

    par_spread: Any = Field(default=None)
    premium_leg: Any = Field(default=None)
    protection_leg: Any = Field(default=None)
    pv: Any = Field(default=None)
    risky_annuity: Any = Field(default=None)


class CdsSpreadToPdConversionRequest(BaseModel):
    """Request parameters for cds_spread_to_pd_conversion()."""

    cds_spread: float
    maturity: float
    recovery_rate: float = 0.4


class CdsSpreadToPdConversionResponse(BaseModel):
    """Result of cds_spread_to_pd_conversion(). Mirrors the engine return dict."""

    model_config = ConfigDict(extra="allow", protected_namespaces=())

    annual_pd: Any = Field(default=None)
    hazard_rate: Any = Field(default=None)
    maturity: Any = Field(default=None)
    pd: Any = Field(default=None)


class CreditDefaultSwapVarRequest(BaseModel):
    """Request parameters for credit_default_swap_var()."""

    notional: float
    risky_annuity: float
    spread_volatility: float
    confidence_level: float = 0.99
    horizon_days: int = 1
    position: str = "long_protection"

    @field_validator("confidence_level")
    @classmethod
    def _confidence_range(cls, v: float) -> float:
        if v is not None and not (0.90 <= v <= 0.9999):
            raise ValueError(f"confidence_level {v} outside regulatory range [0.90, 0.9999]")
        return v


class CreditDefaultSwapVarResponse(BaseModel):
    """Result of credit_default_swap_var(). Mirrors the engine return dict."""

    model_config = ConfigDict(extra="allow", protected_namespaces=())

    confidence_level: Any = Field(default=None)
    pnl_sensitivity: Any = Field(default=None)
    spread_dv01: Any = Field(default=None)
    var: Any = Field(default=None)
    z_score: Any = Field(default=None)


class Ifrs9StageClassificationPdThresholdRequest(BaseModel):
    """Request parameters for ifrs_9_stage_classification_pd_threshold()."""

    pd_current: float
    pd_origination: float
    days_past_due: int = 0
    sicr_relative_threshold: float = 2.0
    sicr_absolute_threshold: float = 0.02


class Ifrs9StageClassificationPdThresholdResponse(BaseModel):
    """Result of ifrs_9_stage_classification_pd_threshold(). Mirrors the engine return dict."""

    model_config = ConfigDict(extra="allow", protected_namespaces=())

    reason: Any = Field(default=None)
    sicr: Any = Field(default=None)
    stage: Any = Field(default=None)


class Ifrs912MonthEclStage1Request(BaseModel):
    """Request parameters for ifrs_9_12_month_ecl_stage_1()."""

    pd_12m: float
    lgd: float
    ead: float
    discount_factor: float = 1.0


class Ifrs912MonthEclStage1Response(BaseModel):
    """Result of ifrs_9_12_month_ecl_stage_1(). Mirrors the engine return dict."""

    model_config = ConfigDict(extra="allow", protected_namespaces=())

    ecl: Any = Field(default=None)
    ecl_rate: Any = Field(default=None)
    stage: Any = Field(default=None)


class Ifrs9LifetimeEclStage23Request(BaseModel):
    """Request parameters for ifrs_9_lifetime_ecl_stage_2_3()."""

    marginal_pd: list[float] | list[list[float]]
    lgd: list[float] | list[list[float]]
    ead: list[float] | list[list[float]]
    discount_factors: list[float] | list[list[float]]
    stage: int = 2


class Ifrs9LifetimeEclStage23Response(BaseModel):
    """Result of ifrs_9_lifetime_ecl_stage_2_3(). Mirrors the engine return dict."""

    model_config = ConfigDict(extra="allow", protected_namespaces=())

    cumulative_pd: Any = Field(default=None)
    ecl: Any = Field(default=None)
    stage: Any = Field(default=None)


class Ifrs9ScenarioWeightedEclRequest(BaseModel):
    """Request parameters for ifrs_9_scenario_weighted_ecl()."""

    scenario_ecls: list[float] | list[list[float]]
    scenario_weights: list[float] | list[list[float]]


class Ifrs9ScenarioWeightedEclResponse(BaseModel):
    """Result of ifrs_9_scenario_weighted_ecl(). Mirrors the engine return dict."""

    model_config = ConfigDict(extra="allow", protected_namespaces=())

    contributions: Any = Field(default=None)
    ecl: Any = Field(default=None)
    weights: Any = Field(default=None)


class MacroeconomicOverlaysEclRequest(BaseModel):
    """Request parameters for macroeconomic_overlays_ecl()."""

    base_ecl: float
    macro_factors: list[float] | list[list[float]]
    sensitivities: list[float] | list[list[float]]
    management_overlay: float = 0.0


class MacroeconomicOverlaysEclResponse(BaseModel):
    """Result of macroeconomic_overlays_ecl(). Mirrors the engine return dict."""

    model_config = ConfigDict(extra="allow", protected_namespaces=())

    ecl_adjusted: Any = Field(default=None)
    macro_multiplier: Any = Field(default=None)
    f_model_ecl: Any = Field(default=None, alias="model_ecl")


class Ifrs9StagingCriteriaAssessmentRequest(BaseModel):
    """Request parameters for ifrs_9_staging_criteria_assessment()."""

    pd_current: float
    pd_origination: float
    days_past_due: int = 0
    forbearance: bool = False
    watchlist: bool = False
    sicr_relative_threshold: float = 2.0


class Ifrs9StagingCriteriaAssessmentResponse(BaseModel):
    """Result of ifrs_9_staging_criteria_assessment(). Mirrors the engine return dict."""

    model_config = ConfigDict(extra="allow", protected_namespaces=())

    quantitative_stage: Any = Field(default=None)
    stage: Any = Field(default=None)
    triggers: Any = Field(default=None)


class CreditPortfolioOptimisationRequest(BaseModel):
    """Request parameters for credit_portfolio_optimisation()."""

    expected_returns: list[float] | list[list[float]]
    expected_losses: list[float] | list[list[float]]
    max_weight: float = 1.0
    risk_aversion: float = 1.0


class CreditPortfolioOptimisationResponse(BaseModel):
    """Result of credit_portfolio_optimisation(). Mirrors the engine return dict."""

    model_config = ConfigDict(extra="allow", protected_namespaces=())

    portfolio_el: Any = Field(default=None)
    utility: Any = Field(default=None)
    weights: Any = Field(default=None)


class CreditStressTestingRequest(BaseModel):
    """Request parameters for credit_stress_testing()."""

    pd: list[float] | list[list[float]]
    lgd: list[float] | list[list[float]]
    ead: list[float] | list[list[float]]
    pd_shock_multiplier: float = 1.5
    lgd_shock_multiplier: float = 1.2


class CreditStressTestingResponse(BaseModel):
    """Result of credit_stress_testing(). Mirrors the engine return dict."""

    model_config = ConfigDict(extra="allow", protected_namespaces=())

    baseline_el: Any = Field(default=None)
    incremental_loss: Any = Field(default=None)
    stress_ratio: Any = Field(default=None)
    stressed_el: Any = Field(default=None)


class AltmanZScoreCreditScoringRequest(BaseModel):
    """Request parameters for altman_z_score_credit_scoring()."""

    working_capital: float
    retained_earnings: float
    ebit: float
    market_value_equity: float
    sales: float
    total_assets: float
    total_liabilities: float


class AltmanZScoreCreditScoringResponse(BaseModel):
    """Result of altman_z_score_credit_scoring(). Mirrors the engine return dict."""

    model_config = ConfigDict(extra="allow", protected_namespaces=())

    x1: Any = Field(default=None)
    x2: Any = Field(default=None)
    x3: Any = Field(default=None)
    x4: Any = Field(default=None)
    x5: Any = Field(default=None)
    z_score: Any = Field(default=None)
    zone: Any = Field(default=None)


class LogisticRegressionPdModelRequest(BaseModel):
    """Request parameters for logistic_regression_pd_model()."""

    features: list[float] | list[list[float]]
    defaults: list[float] | list[list[float]]
    max_iter: int = 100
    tol: float = 1e-08
    ridge: float = 1e-06


class LogisticRegressionPdModelResponse(BaseModel):
    """Result of logistic_regression_pd_model(). Mirrors the engine return dict."""

    model_config = ConfigDict(extra="allow", protected_namespaces=())

    coefficients: Any = Field(default=None)
    converged: Any = Field(default=None)
    fitted_pd: Any = Field(default=None)
    log_likelihood: Any = Field(default=None)
    n_iter: Any = Field(default=None)


class MachineLearningPdCalibrationRequest(BaseModel):
    """Request parameters for machine_learning_pd_calibration()."""

    raw_scores: list[float] | list[list[float]]
    defaults: list[float] | list[list[float]]
    max_iter: int = 100
    tol: float = 1e-08


class MachineLearningPdCalibrationResponse(BaseModel):
    """Result of machine_learning_pd_calibration(). Mirrors the engine return dict."""

    model_config = ConfigDict(extra="allow", protected_namespaces=())

    brier_score: Any = Field(default=None)
    calibrated_pd: Any = Field(default=None)
    converged: Any = Field(default=None)
    intercept: Any = Field(default=None)
    slope: Any = Field(default=None)


class ThroughTheCyclePdAdjustmentRequest(BaseModel):
    """Request parameters for through_the_cycle_pd_adjustment()."""

    pit_pd: float
    long_run_average_pd: float
    cyclicality: float = 0.5


class ThroughTheCyclePdAdjustmentResponse(BaseModel):
    """Result of through_the_cycle_pd_adjustment(). Mirrors the engine return dict."""

    model_config = ConfigDict(extra="allow", protected_namespaces=())

    cyclicality: Any = Field(default=None)
    long_run_average_pd: Any = Field(default=None)
    pit_pd: Any = Field(default=None)
    ttc_pd: Any = Field(default=None)


class PointInTimePdEstimationRequest(BaseModel):
    """Request parameters for point_in_time_pd_estimation()."""

    ttc_pd: float
    macro_index: float
    sensitivity: float = 1.0


class PointInTimePdEstimationResponse(BaseModel):
    """Result of point_in_time_pd_estimation(). Mirrors the engine return dict."""

    model_config = ConfigDict(extra="allow", protected_namespaces=())

    macro_index: Any = Field(default=None)
    pit_pd: Any = Field(default=None)
    sensitivity: Any = Field(default=None)
    ttc_pd: Any = Field(default=None)


class RatingsMigrationMatrixRequest(BaseModel):
    """Request parameters for ratings_migration_matrix()."""

    from_rating: list[float] | list[list[float]]
    to_rating: list[float] | list[list[float]]
    n_states: int


class RatingsMigrationMatrixResponse(BaseModel):
    """Result of ratings_migration_matrix(). Mirrors the engine return dict."""

    model_config = ConfigDict(extra="allow", protected_namespaces=())

    matrix: Any = Field(default=None)
    n_states: Any = Field(default=None)


class RetailScorecardPdModelRequest(BaseModel):
    """Request parameters for retail_scorecard_pd_model()."""

    feature_values: list[float] | list[list[float]]
    points_per_feature: list[float] | list[list[float]]
    base_points: float
    pdo: float = 50.0
    base_score: float = 600.0
    base_odds: float = 50.0


class RetailScorecardPdModelResponse(BaseModel):
    """Result of retail_scorecard_pd_model(). Mirrors the engine return dict."""

    model_config = ConfigDict(extra="allow", protected_namespaces=())

    odds: Any = Field(default=None)
    pd: Any = Field(default=None)
    score: Any = Field(default=None)


class CorporateCreditScoringModelRequest(BaseModel):
    """Request parameters for corporate_credit_scoring_model()."""

    factor_scores: list[float] | list[list[float]]
    factor_weights: list[float] | list[list[float]]
    pd_floor: float = 0.0003
    pd_anchor: float = 0.5


class CorporateCreditScoringModelResponse(BaseModel):
    """Result of corporate_credit_scoring_model(). Mirrors the engine return dict."""

    model_config = ConfigDict(extra="allow", protected_namespaces=())

    composite_score: Any = Field(default=None)
    pd: Any = Field(default=None)
    rating_bucket: Any = Field(default=None)


class SovereignCreditRiskAssessmentRequest(BaseModel):
    """Request parameters for sovereign_credit_risk_assessment()."""

    debt_to_gdp: float
    fiscal_balance_pct: float
    current_account_pct: float
    fx_reserves_months: float
    governance_score: float


class SovereignCreditRiskAssessmentResponse(BaseModel):
    """Result of sovereign_credit_risk_assessment(). Mirrors the engine return dict."""

    model_config = ConfigDict(extra="allow", protected_namespaces=())

    credit_score: Any = Field(default=None)
    pd: Any = Field(default=None)
    rating: Any = Field(default=None)


class SectorDefaultRateAnalysisRequest(BaseModel):
    """Request parameters for sector_default_rate_analysis()."""

    sector_defaults: list[float] | list[list[float]]
    sector_obligors: list[float] | list[list[float]]
    sector_names: list[str] | None = None


class SectorDefaultRateAnalysisResponse(BaseModel):
    """Result of sector_default_rate_analysis(). Mirrors the engine return dict."""

    model_config = ConfigDict(extra="allow", protected_namespaces=())

    lift: Any = Field(default=None)
    overall_rate: Any = Field(default=None)
    rates: Any = Field(default=None)
    riskiest_sector: Any = Field(default=None)

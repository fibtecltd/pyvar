"""api/routes/operational.py — auto-generated FastAPI routes for the operational domain.

Generated by scripts/gen_p3.py. Each route validates its request, converts
list payloads to NumPy arrays, calls the engine wrapper (the single source
of truth), and returns the result dict via OrjsonResponse. No business logic
lives here.
"""

from __future__ import annotations

import logging
from typing import Any

import numpy as np
from fastapi import APIRouter, Depends, HTTPException, status

from api.middleware.auth import TokenPayload, get_current_user
from api.responses import OrjsonResponse
from engine.oprisk_capital import (
    basel_standardised_measurement_sma as _e_basel_standardised_measurement_sma,
)
from engine.oprisk_capital import (
    diversification_benefit_oprisk as _e_diversification_benefit_oprisk,
)
from engine.oprisk_capital import insurance_offset_calculation as _e_insurance_offset_calculation
from engine.oprisk_capital import oprisk_capital_allocation as _e_oprisk_capital_allocation
from engine.oprisk_capital import oprisk_economic_capital as _e_oprisk_economic_capital
from engine.oprisk_capital import oprisk_stress_testing as _e_oprisk_stress_testing
from engine.oprisk_capital import tail_risk_scenario_oprisk as _e_tail_risk_scenario_oprisk
from engine.oprisk_governance import audit_finding_risk_scorer as _e_audit_finding_risk_scorer
from engine.oprisk_governance import (
    business_continuity_risk_score as _e_business_continuity_risk_score,
)
from engine.oprisk_governance import conduct_risk_metric as _e_conduct_risk_metric
from engine.oprisk_governance import cyber_risk_loss_estimation as _e_cyber_risk_loss_estimation
from engine.oprisk_governance import (
    escalation_threshold_calculation as _e_escalation_threshold_calculation,
)
from engine.oprisk_governance import it_risk_scoring as _e_it_risk_scoring
from engine.oprisk_governance import model_risk_assessment as _e_model_risk_assessment
from engine.oprisk_governance import near_miss_capture_framework as _e_near_miss_capture_framework
from engine.oprisk_governance import oprisk_heat_map_generator as _e_oprisk_heat_map_generator
from engine.oprisk_governance import regulatory_compliance_score as _e_regulatory_compliance_score
from engine.oprisk_governance import (
    risk_appetite_statement_oprisk as _e_risk_appetite_statement_oprisk,
)
from engine.oprisk_governance import root_cause_analysis_template as _e_root_cause_analysis_template
from engine.oprisk_governance import third_party_vendor_risk as _e_third_party_vendor_risk
from engine.oprisk_kri import key_risk_indicator_kri_library as _e_key_risk_indicator_kri_library
from engine.oprisk_kri import kri_threshold_breach_detection as _e_kri_threshold_breach_detection
from engine.oprisk_kri import kri_trend_analysis as _e_kri_trend_analysis
from engine.oprisk_lda import (
    advanced_measurement_approach_ama as _e_advanced_measurement_approach_ama,
)
from engine.oprisk_lda import compound_loss_distribution as _e_compound_loss_distribution
from engine.oprisk_lda import frequency_distribution_fitting as _e_frequency_distribution_fitting
from engine.oprisk_lda import loss_distribution_approach_lda as _e_loss_distribution_approach_lda
from engine.oprisk_lda import monte_carlo_oprisk_capital as _e_monte_carlo_oprisk_capital
from engine.oprisk_lda import operational_var_opvar as _e_operational_var_opvar
from engine.oprisk_lda import severity_distribution_fitting as _e_severity_distribution_fitting
from engine.oprisk_rcsa import business_environment_factor_bei as _e_business_environment_factor_bei
from engine.oprisk_rcsa import control_testing_effectiveness as _e_control_testing_effectiveness
from engine.oprisk_rcsa import external_loss_data_integration as _e_external_loss_data_integration
from engine.oprisk_rcsa import internal_control_factor_icf as _e_internal_control_factor_icf
from engine.oprisk_rcsa import loss_data_collection_framework as _e_loss_data_collection_framework
from engine.oprisk_rcsa import loss_event_classification_basel as _e_loss_event_classification_basel
from engine.oprisk_rcsa import rcsa_control_effectiveness as _e_rcsa_control_effectiveness
from engine.oprisk_rcsa import rcsa_inherent_risk_scoring as _e_rcsa_inherent_risk_scoring
from engine.oprisk_rcsa import rcsa_residual_risk_scoring as _e_rcsa_residual_risk_scoring
from engine.oprisk_rcsa import rcsa_risk_identification as _e_rcsa_risk_identification
from engine.oprisk_scenario import scenario_analysis_oprisk as _e_scenario_analysis_oprisk
from engine.oprisk_scenario import (
    scenario_expert_elicitation_model as _e_scenario_expert_elicitation_model,
)
from engine.oprisk_scenario import scenario_frequency_estimation as _e_scenario_frequency_estimation
from engine.oprisk_scenario import scenario_severity_estimation as _e_scenario_severity_estimation
from schemas.operational import (
    AdvancedMeasurementApproachAmaRequest,
    AdvancedMeasurementApproachAmaResponse,
    AuditFindingRiskScorerRequest,
    AuditFindingRiskScorerResponse,
    BaselStandardisedMeasurementSmaRequest,
    BaselStandardisedMeasurementSmaResponse,
    BusinessContinuityRiskScoreRequest,
    BusinessContinuityRiskScoreResponse,
    BusinessEnvironmentFactorBeiRequest,
    BusinessEnvironmentFactorBeiResponse,
    CompoundLossDistributionRequest,
    CompoundLossDistributionResponse,
    ConductRiskMetricRequest,
    ConductRiskMetricResponse,
    ControlTestingEffectivenessRequest,
    ControlTestingEffectivenessResponse,
    CyberRiskLossEstimationRequest,
    CyberRiskLossEstimationResponse,
    DiversificationBenefitOpriskRequest,
    DiversificationBenefitOpriskResponse,
    EscalationThresholdCalculationRequest,
    EscalationThresholdCalculationResponse,
    ExternalLossDataIntegrationRequest,
    ExternalLossDataIntegrationResponse,
    FrequencyDistributionFittingRequest,
    FrequencyDistributionFittingResponse,
    InsuranceOffsetCalculationRequest,
    InsuranceOffsetCalculationResponse,
    InternalControlFactorIcfRequest,
    InternalControlFactorIcfResponse,
    ItRiskScoringRequest,
    ItRiskScoringResponse,
    KeyRiskIndicatorKriLibraryRequest,
    KeyRiskIndicatorKriLibraryResponse,
    KriThresholdBreachDetectionRequest,
    KriThresholdBreachDetectionResponse,
    KriTrendAnalysisRequest,
    KriTrendAnalysisResponse,
    LossDataCollectionFrameworkRequest,
    LossDataCollectionFrameworkResponse,
    LossDistributionApproachLdaRequest,
    LossDistributionApproachLdaResponse,
    LossEventClassificationBaselRequest,
    LossEventClassificationBaselResponse,
    ModelRiskAssessmentRequest,
    ModelRiskAssessmentResponse,
    MonteCarloOpriskCapitalRequest,
    MonteCarloOpriskCapitalResponse,
    NearMissCaptureFrameworkRequest,
    NearMissCaptureFrameworkResponse,
    OperationalVarOpvarRequest,
    OperationalVarOpvarResponse,
    OpriskCapitalAllocationRequest,
    OpriskCapitalAllocationResponse,
    OpriskEconomicCapitalRequest,
    OpriskEconomicCapitalResponse,
    OpriskHeatMapGeneratorRequest,
    OpriskHeatMapGeneratorResponse,
    OpriskStressTestingRequest,
    OpriskStressTestingResponse,
    RcsaControlEffectivenessRequest,
    RcsaControlEffectivenessResponse,
    RcsaInherentRiskScoringRequest,
    RcsaInherentRiskScoringResponse,
    RcsaResidualRiskScoringRequest,
    RcsaResidualRiskScoringResponse,
    RcsaRiskIdentificationRequest,
    RcsaRiskIdentificationResponse,
    RegulatoryComplianceScoreRequest,
    RegulatoryComplianceScoreResponse,
    RiskAppetiteStatementOpriskRequest,
    RiskAppetiteStatementOpriskResponse,
    RootCauseAnalysisTemplateRequest,
    RootCauseAnalysisTemplateResponse,
    ScenarioAnalysisOpriskRequest,
    ScenarioAnalysisOpriskResponse,
    ScenarioExpertElicitationModelRequest,
    ScenarioExpertElicitationModelResponse,
    ScenarioFrequencyEstimationRequest,
    ScenarioFrequencyEstimationResponse,
    ScenarioSeverityEstimationRequest,
    ScenarioSeverityEstimationResponse,
    SeverityDistributionFittingRequest,
    SeverityDistributionFittingResponse,
    TailRiskScenarioOpriskRequest,
    TailRiskScenarioOpriskResponse,
    ThirdPartyVendorRiskRequest,
    ThirdPartyVendorRiskResponse,
)

logger = logging.getLogger(__name__)
router = APIRouter(prefix="/operational", tags=["operational"])

# Engine input errors → HTTP 422. Explicit tuple (no bare except) covers
# bad numeric domains, singular matrices, and shape/key mismatches.
_INPUT_ERRORS = (
    ValueError,
    TypeError,
    KeyError,
    IndexError,
    ArithmeticError,
    AssertionError,
    np.linalg.LinAlgError,
)


@router.post(
    "/frequency_distribution_fitting",
    response_model=FrequencyDistributionFittingResponse,
    summary="Frequency Distribution Fitting",
)
async def frequency_distribution_fitting(
    body: FrequencyDistributionFittingRequest,
    user: TokenPayload = Depends(get_current_user),
) -> OrjsonResponse:
    kwargs = body.model_dump()
    kwargs["annual_event_counts"] = np.asarray(kwargs["annual_event_counts"], dtype=np.float64)
    try:
        result: Any = _e_frequency_distribution_fitting(**kwargs)
    except _INPUT_ERRORS as exc:
        raise HTTPException(
            status_code=status.HTTP_422_UNPROCESSABLE_ENTITY,
            detail=str(exc),
        ) from exc
    if not isinstance(result, dict):
        result = {"result": result}
    return OrjsonResponse(content=result)


@router.post(
    "/severity_distribution_fitting",
    response_model=SeverityDistributionFittingResponse,
    summary="Severity Distribution Fitting",
)
async def severity_distribution_fitting(
    body: SeverityDistributionFittingRequest,
    user: TokenPayload = Depends(get_current_user),
) -> OrjsonResponse:
    kwargs = body.model_dump()
    kwargs["loss_amounts"] = np.asarray(kwargs["loss_amounts"], dtype=np.float64)
    try:
        result: Any = _e_severity_distribution_fitting(**kwargs)
    except _INPUT_ERRORS as exc:
        raise HTTPException(
            status_code=status.HTTP_422_UNPROCESSABLE_ENTITY,
            detail=str(exc),
        ) from exc
    if not isinstance(result, dict):
        result = {"result": result}
    return OrjsonResponse(content=result)


@router.post(
    "/compound_loss_distribution",
    response_model=CompoundLossDistributionResponse,
    summary="Compound Loss Distribution",
)
async def compound_loss_distribution(
    body: CompoundLossDistributionRequest,
    user: TokenPayload = Depends(get_current_user),
) -> OrjsonResponse:
    kwargs = body.model_dump()
    try:
        result: Any = _e_compound_loss_distribution(**kwargs)
    except _INPUT_ERRORS as exc:
        raise HTTPException(
            status_code=status.HTTP_422_UNPROCESSABLE_ENTITY,
            detail=str(exc),
        ) from exc
    if not isinstance(result, dict):
        result = {"result": result}
    return OrjsonResponse(content=result)


@router.post(
    "/monte_carlo_oprisk_capital",
    response_model=MonteCarloOpriskCapitalResponse,
    summary="Monte Carlo Oprisk Capital",
)
async def monte_carlo_oprisk_capital(
    body: MonteCarloOpriskCapitalRequest,
    user: TokenPayload = Depends(get_current_user),
) -> OrjsonResponse:
    kwargs = body.model_dump()
    try:
        result: Any = _e_monte_carlo_oprisk_capital(**kwargs)
    except _INPUT_ERRORS as exc:
        raise HTTPException(
            status_code=status.HTTP_422_UNPROCESSABLE_ENTITY,
            detail=str(exc),
        ) from exc
    if not isinstance(result, dict):
        result = {"result": result}
    return OrjsonResponse(content=result)


@router.post(
    "/operational_var_opvar",
    response_model=OperationalVarOpvarResponse,
    summary="Operational Var Opvar",
)
async def operational_var_opvar(
    body: OperationalVarOpvarRequest,
    user: TokenPayload = Depends(get_current_user),
) -> OrjsonResponse:
    kwargs = body.model_dump()
    kwargs["annual_losses"] = np.asarray(kwargs["annual_losses"], dtype=np.float64)
    try:
        result: Any = _e_operational_var_opvar(**kwargs)
    except _INPUT_ERRORS as exc:
        raise HTTPException(
            status_code=status.HTTP_422_UNPROCESSABLE_ENTITY,
            detail=str(exc),
        ) from exc
    if not isinstance(result, dict):
        result = {"result": result}
    return OrjsonResponse(content=result)


@router.post(
    "/loss_distribution_approach_lda",
    response_model=LossDistributionApproachLdaResponse,
    summary="Loss Distribution Approach Lda",
)
async def loss_distribution_approach_lda(
    body: LossDistributionApproachLdaRequest,
    user: TokenPayload = Depends(get_current_user),
) -> OrjsonResponse:
    kwargs = body.model_dump()
    kwargs["annual_event_counts"] = np.asarray(kwargs["annual_event_counts"], dtype=np.float64)
    kwargs["loss_amounts"] = np.asarray(kwargs["loss_amounts"], dtype=np.float64)
    try:
        result: Any = _e_loss_distribution_approach_lda(**kwargs)
    except _INPUT_ERRORS as exc:
        raise HTTPException(
            status_code=status.HTTP_422_UNPROCESSABLE_ENTITY,
            detail=str(exc),
        ) from exc
    if not isinstance(result, dict):
        result = {"result": result}
    return OrjsonResponse(content=result)


@router.post(
    "/advanced_measurement_approach_ama",
    response_model=AdvancedMeasurementApproachAmaResponse,
    summary="Advanced Measurement Approach Ama",
)
async def advanced_measurement_approach_ama(
    body: AdvancedMeasurementApproachAmaRequest,
    user: TokenPayload = Depends(get_current_user),
) -> OrjsonResponse:
    kwargs = body.model_dump()
    kwargs["annual_event_counts"] = np.asarray(kwargs["annual_event_counts"], dtype=np.float64)
    kwargs["loss_amounts"] = np.asarray(kwargs["loss_amounts"], dtype=np.float64)
    try:
        result: Any = _e_advanced_measurement_approach_ama(**kwargs)
    except _INPUT_ERRORS as exc:
        raise HTTPException(
            status_code=status.HTTP_422_UNPROCESSABLE_ENTITY,
            detail=str(exc),
        ) from exc
    if not isinstance(result, dict):
        result = {"result": result}
    return OrjsonResponse(content=result)


@router.post(
    "/basel_standardised_measurement_sma",
    response_model=BaselStandardisedMeasurementSmaResponse,
    summary="Basel Standardised Measurement Sma",
)
async def basel_standardised_measurement_sma(
    body: BaselStandardisedMeasurementSmaRequest,
    user: TokenPayload = Depends(get_current_user),
) -> OrjsonResponse:
    kwargs = body.model_dump()
    try:
        result: Any = _e_basel_standardised_measurement_sma(**kwargs)
    except _INPUT_ERRORS as exc:
        raise HTTPException(
            status_code=status.HTTP_422_UNPROCESSABLE_ENTITY,
            detail=str(exc),
        ) from exc
    if not isinstance(result, dict):
        result = {"result": result}
    return OrjsonResponse(content=result)


@router.post(
    "/oprisk_capital_allocation",
    response_model=OpriskCapitalAllocationResponse,
    summary="Oprisk Capital Allocation",
)
async def oprisk_capital_allocation(
    body: OpriskCapitalAllocationRequest,
    user: TokenPayload = Depends(get_current_user),
) -> OrjsonResponse:
    kwargs = body.model_dump()
    kwargs["business_line_risks"] = np.asarray(kwargs["business_line_risks"], dtype=np.float64)
    try:
        result: Any = _e_oprisk_capital_allocation(**kwargs)
    except _INPUT_ERRORS as exc:
        raise HTTPException(
            status_code=status.HTTP_422_UNPROCESSABLE_ENTITY,
            detail=str(exc),
        ) from exc
    if not isinstance(result, dict):
        result = {"result": result}
    return OrjsonResponse(content=result)


@router.post(
    "/diversification_benefit_oprisk",
    response_model=DiversificationBenefitOpriskResponse,
    summary="Diversification Benefit Oprisk",
)
async def diversification_benefit_oprisk(
    body: DiversificationBenefitOpriskRequest,
    user: TokenPayload = Depends(get_current_user),
) -> OrjsonResponse:
    kwargs = body.model_dump()
    kwargs["standalone_capitals"] = np.asarray(kwargs["standalone_capitals"], dtype=np.float64)
    try:
        result: Any = _e_diversification_benefit_oprisk(**kwargs)
    except _INPUT_ERRORS as exc:
        raise HTTPException(
            status_code=status.HTTP_422_UNPROCESSABLE_ENTITY,
            detail=str(exc),
        ) from exc
    if not isinstance(result, dict):
        result = {"result": result}
    return OrjsonResponse(content=result)


@router.post(
    "/oprisk_economic_capital",
    response_model=OpriskEconomicCapitalResponse,
    summary="Oprisk Economic Capital",
)
async def oprisk_economic_capital(
    body: OpriskEconomicCapitalRequest,
    user: TokenPayload = Depends(get_current_user),
) -> OrjsonResponse:
    kwargs = body.model_dump()
    try:
        result: Any = _e_oprisk_economic_capital(**kwargs)
    except _INPUT_ERRORS as exc:
        raise HTTPException(
            status_code=status.HTTP_422_UNPROCESSABLE_ENTITY,
            detail=str(exc),
        ) from exc
    if not isinstance(result, dict):
        result = {"result": result}
    return OrjsonResponse(content=result)


@router.post(
    "/insurance_offset_calculation",
    response_model=InsuranceOffsetCalculationResponse,
    summary="Insurance Offset Calculation",
)
async def insurance_offset_calculation(
    body: InsuranceOffsetCalculationRequest,
    user: TokenPayload = Depends(get_current_user),
) -> OrjsonResponse:
    kwargs = body.model_dump()
    try:
        result: Any = _e_insurance_offset_calculation(**kwargs)
    except _INPUT_ERRORS as exc:
        raise HTTPException(
            status_code=status.HTTP_422_UNPROCESSABLE_ENTITY,
            detail=str(exc),
        ) from exc
    if not isinstance(result, dict):
        result = {"result": result}
    return OrjsonResponse(content=result)


@router.post(
    "/tail_risk_scenario_oprisk",
    response_model=TailRiskScenarioOpriskResponse,
    summary="Tail Risk Scenario Oprisk",
)
async def tail_risk_scenario_oprisk(
    body: TailRiskScenarioOpriskRequest,
    user: TokenPayload = Depends(get_current_user),
) -> OrjsonResponse:
    kwargs = body.model_dump()
    kwargs["annual_losses"] = np.asarray(kwargs["annual_losses"], dtype=np.float64)
    try:
        result: Any = _e_tail_risk_scenario_oprisk(**kwargs)
    except _INPUT_ERRORS as exc:
        raise HTTPException(
            status_code=status.HTTP_422_UNPROCESSABLE_ENTITY,
            detail=str(exc),
        ) from exc
    if not isinstance(result, dict):
        result = {"result": result}
    return OrjsonResponse(content=result)


@router.post(
    "/oprisk_stress_testing",
    response_model=OpriskStressTestingResponse,
    summary="Oprisk Stress Testing",
)
async def oprisk_stress_testing(
    body: OpriskStressTestingRequest,
    user: TokenPayload = Depends(get_current_user),
) -> OrjsonResponse:
    kwargs = body.model_dump()
    try:
        result: Any = _e_oprisk_stress_testing(**kwargs)
    except _INPUT_ERRORS as exc:
        raise HTTPException(
            status_code=status.HTTP_422_UNPROCESSABLE_ENTITY,
            detail=str(exc),
        ) from exc
    if not isinstance(result, dict):
        result = {"result": result}
    return OrjsonResponse(content=result)


@router.post(
    "/rcsa_risk_identification",
    response_model=RcsaRiskIdentificationResponse,
    summary="Rcsa Risk Identification",
)
async def rcsa_risk_identification(
    body: RcsaRiskIdentificationRequest,
    user: TokenPayload = Depends(get_current_user),
) -> OrjsonResponse:
    kwargs = body.model_dump()
    try:
        result: Any = _e_rcsa_risk_identification(**kwargs)
    except _INPUT_ERRORS as exc:
        raise HTTPException(
            status_code=status.HTTP_422_UNPROCESSABLE_ENTITY,
            detail=str(exc),
        ) from exc
    if not isinstance(result, dict):
        result = {"result": result}
    return OrjsonResponse(content=result)


@router.post(
    "/rcsa_inherent_risk_scoring",
    response_model=RcsaInherentRiskScoringResponse,
    summary="Rcsa Inherent Risk Scoring",
)
async def rcsa_inherent_risk_scoring(
    body: RcsaInherentRiskScoringRequest,
    user: TokenPayload = Depends(get_current_user),
) -> OrjsonResponse:
    kwargs = body.model_dump()
    try:
        result: Any = _e_rcsa_inherent_risk_scoring(**kwargs)
    except _INPUT_ERRORS as exc:
        raise HTTPException(
            status_code=status.HTTP_422_UNPROCESSABLE_ENTITY,
            detail=str(exc),
        ) from exc
    if not isinstance(result, dict):
        result = {"result": result}
    return OrjsonResponse(content=result)


@router.post(
    "/rcsa_residual_risk_scoring",
    response_model=RcsaResidualRiskScoringResponse,
    summary="Rcsa Residual Risk Scoring",
)
async def rcsa_residual_risk_scoring(
    body: RcsaResidualRiskScoringRequest,
    user: TokenPayload = Depends(get_current_user),
) -> OrjsonResponse:
    kwargs = body.model_dump()
    try:
        result: Any = _e_rcsa_residual_risk_scoring(**kwargs)
    except _INPUT_ERRORS as exc:
        raise HTTPException(
            status_code=status.HTTP_422_UNPROCESSABLE_ENTITY,
            detail=str(exc),
        ) from exc
    if not isinstance(result, dict):
        result = {"result": result}
    return OrjsonResponse(content=result)


@router.post(
    "/rcsa_control_effectiveness",
    response_model=RcsaControlEffectivenessResponse,
    summary="Rcsa Control Effectiveness",
)
async def rcsa_control_effectiveness(
    body: RcsaControlEffectivenessRequest,
    user: TokenPayload = Depends(get_current_user),
) -> OrjsonResponse:
    kwargs = body.model_dump()
    try:
        result: Any = _e_rcsa_control_effectiveness(**kwargs)
    except _INPUT_ERRORS as exc:
        raise HTTPException(
            status_code=status.HTTP_422_UNPROCESSABLE_ENTITY,
            detail=str(exc),
        ) from exc
    if not isinstance(result, dict):
        result = {"result": result}
    return OrjsonResponse(content=result)


@router.post(
    "/control_testing_effectiveness",
    response_model=ControlTestingEffectivenessResponse,
    summary="Control Testing Effectiveness",
)
async def control_testing_effectiveness(
    body: ControlTestingEffectivenessRequest,
    user: TokenPayload = Depends(get_current_user),
) -> OrjsonResponse:
    kwargs = body.model_dump()
    try:
        result: Any = _e_control_testing_effectiveness(**kwargs)
    except _INPUT_ERRORS as exc:
        raise HTTPException(
            status_code=status.HTTP_422_UNPROCESSABLE_ENTITY,
            detail=str(exc),
        ) from exc
    if not isinstance(result, dict):
        result = {"result": result}
    return OrjsonResponse(content=result)


@router.post(
    "/business_environment_factor_bei",
    response_model=BusinessEnvironmentFactorBeiResponse,
    summary="Business Environment Factor Bei",
)
async def business_environment_factor_bei(
    body: BusinessEnvironmentFactorBeiRequest,
    user: TokenPayload = Depends(get_current_user),
) -> OrjsonResponse:
    kwargs = body.model_dump()
    kwargs["factor_scores"] = np.asarray(kwargs["factor_scores"], dtype=np.float64)
    try:
        result: Any = _e_business_environment_factor_bei(**kwargs)
    except _INPUT_ERRORS as exc:
        raise HTTPException(
            status_code=status.HTTP_422_UNPROCESSABLE_ENTITY,
            detail=str(exc),
        ) from exc
    if not isinstance(result, dict):
        result = {"result": result}
    return OrjsonResponse(content=result)


@router.post(
    "/internal_control_factor_icf",
    response_model=InternalControlFactorIcfResponse,
    summary="Internal Control Factor Icf",
)
async def internal_control_factor_icf(
    body: InternalControlFactorIcfRequest,
    user: TokenPayload = Depends(get_current_user),
) -> OrjsonResponse:
    kwargs = body.model_dump()
    kwargs["control_scores"] = np.asarray(kwargs["control_scores"], dtype=np.float64)
    try:
        result: Any = _e_internal_control_factor_icf(**kwargs)
    except _INPUT_ERRORS as exc:
        raise HTTPException(
            status_code=status.HTTP_422_UNPROCESSABLE_ENTITY,
            detail=str(exc),
        ) from exc
    if not isinstance(result, dict):
        result = {"result": result}
    return OrjsonResponse(content=result)


@router.post(
    "/loss_event_classification_basel",
    response_model=LossEventClassificationBaselResponse,
    summary="Loss Event Classification Basel",
)
async def loss_event_classification_basel(
    body: LossEventClassificationBaselRequest,
    user: TokenPayload = Depends(get_current_user),
) -> OrjsonResponse:
    kwargs = body.model_dump()
    try:
        result: Any = _e_loss_event_classification_basel(**kwargs)
    except _INPUT_ERRORS as exc:
        raise HTTPException(
            status_code=status.HTTP_422_UNPROCESSABLE_ENTITY,
            detail=str(exc),
        ) from exc
    if not isinstance(result, dict):
        result = {"result": result}
    return OrjsonResponse(content=result)


@router.post(
    "/loss_data_collection_framework",
    response_model=LossDataCollectionFrameworkResponse,
    summary="Loss Data Collection Framework",
)
async def loss_data_collection_framework(
    body: LossDataCollectionFrameworkRequest,
    user: TokenPayload = Depends(get_current_user),
) -> OrjsonResponse:
    kwargs = body.model_dump()
    try:
        result: Any = _e_loss_data_collection_framework(**kwargs)
    except _INPUT_ERRORS as exc:
        raise HTTPException(
            status_code=status.HTTP_422_UNPROCESSABLE_ENTITY,
            detail=str(exc),
        ) from exc
    if not isinstance(result, dict):
        result = {"result": result}
    return OrjsonResponse(content=result)


@router.post(
    "/external_loss_data_integration",
    response_model=ExternalLossDataIntegrationResponse,
    summary="External Loss Data Integration",
)
async def external_loss_data_integration(
    body: ExternalLossDataIntegrationRequest,
    user: TokenPayload = Depends(get_current_user),
) -> OrjsonResponse:
    kwargs = body.model_dump()
    kwargs["internal_losses"] = np.asarray(kwargs["internal_losses"], dtype=np.float64)
    kwargs["external_losses"] = np.asarray(kwargs["external_losses"], dtype=np.float64)
    try:
        result: Any = _e_external_loss_data_integration(**kwargs)
    except _INPUT_ERRORS as exc:
        raise HTTPException(
            status_code=status.HTTP_422_UNPROCESSABLE_ENTITY,
            detail=str(exc),
        ) from exc
    if not isinstance(result, dict):
        result = {"result": result}
    return OrjsonResponse(content=result)


@router.post(
    "/key_risk_indicator_kri_library",
    response_model=KeyRiskIndicatorKriLibraryResponse,
    summary="Key Risk Indicator Kri Library",
)
async def key_risk_indicator_kri_library(
    body: KeyRiskIndicatorKriLibraryRequest,
    user: TokenPayload = Depends(get_current_user),
) -> OrjsonResponse:
    kwargs = body.model_dump()
    try:
        result: Any = _e_key_risk_indicator_kri_library(**kwargs)
    except _INPUT_ERRORS as exc:
        raise HTTPException(
            status_code=status.HTTP_422_UNPROCESSABLE_ENTITY,
            detail=str(exc),
        ) from exc
    if not isinstance(result, dict):
        result = {"result": result}
    return OrjsonResponse(content=result)


@router.post(
    "/kri_threshold_breach_detection",
    response_model=KriThresholdBreachDetectionResponse,
    summary="Kri Threshold Breach Detection",
)
async def kri_threshold_breach_detection(
    body: KriThresholdBreachDetectionRequest,
    user: TokenPayload = Depends(get_current_user),
) -> OrjsonResponse:
    kwargs = body.model_dump()
    try:
        result: Any = _e_kri_threshold_breach_detection(**kwargs)
    except _INPUT_ERRORS as exc:
        raise HTTPException(
            status_code=status.HTTP_422_UNPROCESSABLE_ENTITY,
            detail=str(exc),
        ) from exc
    if not isinstance(result, dict):
        result = {"result": result}
    return OrjsonResponse(content=result)


@router.post(
    "/kri_trend_analysis",
    response_model=KriTrendAnalysisResponse,
    summary="Kri Trend Analysis",
)
async def kri_trend_analysis(
    body: KriTrendAnalysisRequest,
    user: TokenPayload = Depends(get_current_user),
) -> OrjsonResponse:
    kwargs = body.model_dump()
    kwargs["observations"] = np.asarray(kwargs["observations"], dtype=np.float64)
    try:
        result: Any = _e_kri_trend_analysis(**kwargs)
    except _INPUT_ERRORS as exc:
        raise HTTPException(
            status_code=status.HTTP_422_UNPROCESSABLE_ENTITY,
            detail=str(exc),
        ) from exc
    if not isinstance(result, dict):
        result = {"result": result}
    return OrjsonResponse(content=result)


@router.post(
    "/scenario_frequency_estimation",
    response_model=ScenarioFrequencyEstimationResponse,
    summary="Scenario Frequency Estimation",
)
async def scenario_frequency_estimation(
    body: ScenarioFrequencyEstimationRequest,
    user: TokenPayload = Depends(get_current_user),
) -> OrjsonResponse:
    kwargs = body.model_dump()
    try:
        result: Any = _e_scenario_frequency_estimation(**kwargs)
    except _INPUT_ERRORS as exc:
        raise HTTPException(
            status_code=status.HTTP_422_UNPROCESSABLE_ENTITY,
            detail=str(exc),
        ) from exc
    if not isinstance(result, dict):
        result = {"result": result}
    return OrjsonResponse(content=result)


@router.post(
    "/scenario_severity_estimation",
    response_model=ScenarioSeverityEstimationResponse,
    summary="Scenario Severity Estimation",
)
async def scenario_severity_estimation(
    body: ScenarioSeverityEstimationRequest,
    user: TokenPayload = Depends(get_current_user),
) -> OrjsonResponse:
    kwargs = body.model_dump()
    try:
        result: Any = _e_scenario_severity_estimation(**kwargs)
    except _INPUT_ERRORS as exc:
        raise HTTPException(
            status_code=status.HTTP_422_UNPROCESSABLE_ENTITY,
            detail=str(exc),
        ) from exc
    if not isinstance(result, dict):
        result = {"result": result}
    return OrjsonResponse(content=result)


@router.post(
    "/scenario_expert_elicitation_model",
    response_model=ScenarioExpertElicitationModelResponse,
    summary="Scenario Expert Elicitation Model",
)
async def scenario_expert_elicitation_model(
    body: ScenarioExpertElicitationModelRequest,
    user: TokenPayload = Depends(get_current_user),
) -> OrjsonResponse:
    kwargs = body.model_dump()
    kwargs["expert_estimates"] = np.asarray(kwargs["expert_estimates"], dtype=np.float64)
    if kwargs["expert_weights"] is not None:
        kwargs["expert_weights"] = np.asarray(kwargs["expert_weights"], dtype=np.float64)
    try:
        result: Any = _e_scenario_expert_elicitation_model(**kwargs)
    except _INPUT_ERRORS as exc:
        raise HTTPException(
            status_code=status.HTTP_422_UNPROCESSABLE_ENTITY,
            detail=str(exc),
        ) from exc
    if not isinstance(result, dict):
        result = {"result": result}
    return OrjsonResponse(content=result)


@router.post(
    "/scenario_analysis_oprisk",
    response_model=ScenarioAnalysisOpriskResponse,
    summary="Scenario Analysis Oprisk",
)
async def scenario_analysis_oprisk(
    body: ScenarioAnalysisOpriskRequest,
    user: TokenPayload = Depends(get_current_user),
) -> OrjsonResponse:
    kwargs = body.model_dump()
    try:
        result: Any = _e_scenario_analysis_oprisk(**kwargs)
    except _INPUT_ERRORS as exc:
        raise HTTPException(
            status_code=status.HTTP_422_UNPROCESSABLE_ENTITY,
            detail=str(exc),
        ) from exc
    if not isinstance(result, dict):
        result = {"result": result}
    return OrjsonResponse(content=result)


@router.post(
    "/cyber_risk_loss_estimation",
    response_model=CyberRiskLossEstimationResponse,
    summary="Cyber Risk Loss Estimation",
)
async def cyber_risk_loss_estimation(
    body: CyberRiskLossEstimationRequest,
    user: TokenPayload = Depends(get_current_user),
) -> OrjsonResponse:
    kwargs = body.model_dump()
    try:
        result: Any = _e_cyber_risk_loss_estimation(**kwargs)
    except _INPUT_ERRORS as exc:
        raise HTTPException(
            status_code=status.HTTP_422_UNPROCESSABLE_ENTITY,
            detail=str(exc),
        ) from exc
    if not isinstance(result, dict):
        result = {"result": result}
    return OrjsonResponse(content=result)


@router.post(
    "/conduct_risk_metric",
    response_model=ConductRiskMetricResponse,
    summary="Conduct Risk Metric",
)
async def conduct_risk_metric(
    body: ConductRiskMetricRequest,
    user: TokenPayload = Depends(get_current_user),
) -> OrjsonResponse:
    kwargs = body.model_dump()
    try:
        result: Any = _e_conduct_risk_metric(**kwargs)
    except _INPUT_ERRORS as exc:
        raise HTTPException(
            status_code=status.HTTP_422_UNPROCESSABLE_ENTITY,
            detail=str(exc),
        ) from exc
    if not isinstance(result, dict):
        result = {"result": result}
    return OrjsonResponse(content=result)


@router.post(
    "/model_risk_assessment",
    response_model=ModelRiskAssessmentResponse,
    summary="Model Risk Assessment",
)
async def model_risk_assessment(
    body: ModelRiskAssessmentRequest,
    user: TokenPayload = Depends(get_current_user),
) -> OrjsonResponse:
    kwargs = body.model_dump()
    try:
        result: Any = _e_model_risk_assessment(**kwargs)
    except _INPUT_ERRORS as exc:
        raise HTTPException(
            status_code=status.HTTP_422_UNPROCESSABLE_ENTITY,
            detail=str(exc),
        ) from exc
    if not isinstance(result, dict):
        result = {"result": result}
    return OrjsonResponse(content=result)


@router.post(
    "/it_risk_scoring",
    response_model=ItRiskScoringResponse,
    summary="It Risk Scoring",
)
async def it_risk_scoring(
    body: ItRiskScoringRequest,
    user: TokenPayload = Depends(get_current_user),
) -> OrjsonResponse:
    kwargs = body.model_dump()
    try:
        result: Any = _e_it_risk_scoring(**kwargs)
    except _INPUT_ERRORS as exc:
        raise HTTPException(
            status_code=status.HTTP_422_UNPROCESSABLE_ENTITY,
            detail=str(exc),
        ) from exc
    if not isinstance(result, dict):
        result = {"result": result}
    return OrjsonResponse(content=result)


@router.post(
    "/third_party_vendor_risk",
    response_model=ThirdPartyVendorRiskResponse,
    summary="Third Party Vendor Risk",
)
async def third_party_vendor_risk(
    body: ThirdPartyVendorRiskRequest,
    user: TokenPayload = Depends(get_current_user),
) -> OrjsonResponse:
    kwargs = body.model_dump()
    try:
        result: Any = _e_third_party_vendor_risk(**kwargs)
    except _INPUT_ERRORS as exc:
        raise HTTPException(
            status_code=status.HTTP_422_UNPROCESSABLE_ENTITY,
            detail=str(exc),
        ) from exc
    if not isinstance(result, dict):
        result = {"result": result}
    return OrjsonResponse(content=result)


@router.post(
    "/business_continuity_risk_score",
    response_model=BusinessContinuityRiskScoreResponse,
    summary="Business Continuity Risk Score",
)
async def business_continuity_risk_score(
    body: BusinessContinuityRiskScoreRequest,
    user: TokenPayload = Depends(get_current_user),
) -> OrjsonResponse:
    kwargs = body.model_dump()
    try:
        result: Any = _e_business_continuity_risk_score(**kwargs)
    except _INPUT_ERRORS as exc:
        raise HTTPException(
            status_code=status.HTTP_422_UNPROCESSABLE_ENTITY,
            detail=str(exc),
        ) from exc
    if not isinstance(result, dict):
        result = {"result": result}
    return OrjsonResponse(content=result)


@router.post(
    "/near_miss_capture_framework",
    response_model=NearMissCaptureFrameworkResponse,
    summary="Near Miss Capture Framework",
)
async def near_miss_capture_framework(
    body: NearMissCaptureFrameworkRequest,
    user: TokenPayload = Depends(get_current_user),
) -> OrjsonResponse:
    kwargs = body.model_dump()
    try:
        result: Any = _e_near_miss_capture_framework(**kwargs)
    except _INPUT_ERRORS as exc:
        raise HTTPException(
            status_code=status.HTTP_422_UNPROCESSABLE_ENTITY,
            detail=str(exc),
        ) from exc
    if not isinstance(result, dict):
        result = {"result": result}
    return OrjsonResponse(content=result)


@router.post(
    "/root_cause_analysis_template",
    response_model=RootCauseAnalysisTemplateResponse,
    summary="Root Cause Analysis Template",
)
async def root_cause_analysis_template(
    body: RootCauseAnalysisTemplateRequest,
    user: TokenPayload = Depends(get_current_user),
) -> OrjsonResponse:
    kwargs = body.model_dump()
    try:
        result: Any = _e_root_cause_analysis_template(**kwargs)
    except _INPUT_ERRORS as exc:
        raise HTTPException(
            status_code=status.HTTP_422_UNPROCESSABLE_ENTITY,
            detail=str(exc),
        ) from exc
    if not isinstance(result, dict):
        result = {"result": result}
    return OrjsonResponse(content=result)


@router.post(
    "/risk_appetite_statement_oprisk",
    response_model=RiskAppetiteStatementOpriskResponse,
    summary="Risk Appetite Statement Oprisk",
)
async def risk_appetite_statement_oprisk(
    body: RiskAppetiteStatementOpriskRequest,
    user: TokenPayload = Depends(get_current_user),
) -> OrjsonResponse:
    kwargs = body.model_dump()
    try:
        result: Any = _e_risk_appetite_statement_oprisk(**kwargs)
    except _INPUT_ERRORS as exc:
        raise HTTPException(
            status_code=status.HTTP_422_UNPROCESSABLE_ENTITY,
            detail=str(exc),
        ) from exc
    if not isinstance(result, dict):
        result = {"result": result}
    return OrjsonResponse(content=result)


@router.post(
    "/escalation_threshold_calculation",
    response_model=EscalationThresholdCalculationResponse,
    summary="Escalation Threshold Calculation",
)
async def escalation_threshold_calculation(
    body: EscalationThresholdCalculationRequest,
    user: TokenPayload = Depends(get_current_user),
) -> OrjsonResponse:
    kwargs = body.model_dump()
    try:
        result: Any = _e_escalation_threshold_calculation(**kwargs)
    except _INPUT_ERRORS as exc:
        raise HTTPException(
            status_code=status.HTTP_422_UNPROCESSABLE_ENTITY,
            detail=str(exc),
        ) from exc
    if not isinstance(result, dict):
        result = {"result": result}
    return OrjsonResponse(content=result)


@router.post(
    "/oprisk_heat_map_generator",
    response_model=OpriskHeatMapGeneratorResponse,
    summary="Oprisk Heat Map Generator",
)
async def oprisk_heat_map_generator(
    body: OpriskHeatMapGeneratorRequest,
    user: TokenPayload = Depends(get_current_user),
) -> OrjsonResponse:
    kwargs = body.model_dump()
    kwargs["likelihoods"] = np.asarray(kwargs["likelihoods"], dtype=np.float64)
    kwargs["impacts"] = np.asarray(kwargs["impacts"], dtype=np.float64)
    try:
        result: Any = _e_oprisk_heat_map_generator(**kwargs)
    except _INPUT_ERRORS as exc:
        raise HTTPException(
            status_code=status.HTTP_422_UNPROCESSABLE_ENTITY,
            detail=str(exc),
        ) from exc
    if not isinstance(result, dict):
        result = {"result": result}
    return OrjsonResponse(content=result)


@router.post(
    "/regulatory_compliance_score",
    response_model=RegulatoryComplianceScoreResponse,
    summary="Regulatory Compliance Score",
)
async def regulatory_compliance_score(
    body: RegulatoryComplianceScoreRequest,
    user: TokenPayload = Depends(get_current_user),
) -> OrjsonResponse:
    kwargs = body.model_dump()
    kwargs["requirements_met"] = np.asarray(kwargs["requirements_met"], dtype=np.float64)
    if kwargs["weights"] is not None:
        kwargs["weights"] = np.asarray(kwargs["weights"], dtype=np.float64)
    try:
        result: Any = _e_regulatory_compliance_score(**kwargs)
    except _INPUT_ERRORS as exc:
        raise HTTPException(
            status_code=status.HTTP_422_UNPROCESSABLE_ENTITY,
            detail=str(exc),
        ) from exc
    if not isinstance(result, dict):
        result = {"result": result}
    return OrjsonResponse(content=result)


@router.post(
    "/audit_finding_risk_scorer",
    response_model=AuditFindingRiskScorerResponse,
    summary="Audit Finding Risk Scorer",
)
async def audit_finding_risk_scorer(
    body: AuditFindingRiskScorerRequest,
    user: TokenPayload = Depends(get_current_user),
) -> OrjsonResponse:
    kwargs = body.model_dump()
    try:
        result: Any = _e_audit_finding_risk_scorer(**kwargs)
    except _INPUT_ERRORS as exc:
        raise HTTPException(
            status_code=status.HTTP_422_UNPROCESSABLE_ENTITY,
            detail=str(exc),
        ) from exc
    if not isinstance(result, dict):
        result = {"result": result}
    return OrjsonResponse(content=result)

"""api/routes/derivatives.py — auto-generated FastAPI routes for the derivatives domain.

Generated by scripts/gen_p3.py. Each route validates its request, converts
list payloads to NumPy arrays, calls the engine wrapper (the single source
of truth), and returns the result dict via OrjsonResponse. No business logic
lives here.
"""

from __future__ import annotations

import logging
from typing import Any

import numpy as np
from fastapi import APIRouter, Depends, HTTPException, status

from api.middleware.auth import TokenPayload, get_current_user
from api.responses import OrjsonResponse
from engine.deriv_bond_analytics import asset_swap_spread as _e_asset_swap_spread
from engine.deriv_bond_analytics import convexity as _e_convexity
from engine.deriv_bond_analytics import duration_macaulay as _e_duration_macaulay
from engine.deriv_bond_analytics import dv01_pvbp as _e_dv01_pvbp
from engine.deriv_bond_analytics import effective_duration as _e_effective_duration
from engine.deriv_bond_analytics import modified_duration as _e_modified_duration
from engine.deriv_bond_analytics import oas_option_adjusted_spread as _e_oas_option_adjusted_spread
from engine.deriv_bond_analytics import yield_to_call as _e_yield_to_call
from engine.deriv_bond_analytics import yield_to_maturity as _e_yield_to_maturity
from engine.deriv_bond_analytics import z_spread_calculator as _e_z_spread_calculator
from engine.deriv_bonds import bond_pricer_fixed_coupon as _e_bond_pricer_fixed_coupon
from engine.deriv_bonds import bond_pricer_floating_rate as _e_bond_pricer_floating_rate
from engine.deriv_bonds import callable_bond_pricer as _e_callable_bond_pricer
from engine.deriv_bonds import convertible_bond_pricer as _e_convertible_bond_pricer
from engine.deriv_bonds import inflation_linked_bond_pricer as _e_inflation_linked_bond_pricer
from engine.deriv_bonds import puttable_bond_pricer as _e_puttable_bond_pricer
from engine.deriv_bonds import zero_coupon_bond_pricer as _e_zero_coupon_bond_pricer
from engine.deriv_curves import bootstrap_yield_curve as _e_bootstrap_yield_curve
from engine.deriv_curves import forward_rate_agreement_fra as _e_forward_rate_agreement_fra
from engine.deriv_curves import interest_rate_swap_irs_pricer as _e_interest_rate_swap_irs_pricer
from engine.deriv_curves import nelson_siegel_curve_fit as _e_nelson_siegel_curve_fit
from engine.deriv_curves import nelson_siegel_svensson_curve as _e_nelson_siegel_svensson_curve
from engine.deriv_curves import ois_curve_sonia_sofr as _e_ois_curve_sonia_sofr
from engine.deriv_curves import swap_rate_curve as _e_swap_rate_curve
from engine.deriv_fx import fx_forward_pricer as _e_fx_forward_pricer
from engine.deriv_fx import (
    fx_option_pricer_garman_kohlhagen as _e_fx_option_pricer_garman_kohlhagen,
)
from engine.deriv_options_exotic import american_option_lsm as _e_american_option_lsm
from engine.deriv_options_exotic import asian_option_pricer as _e_asian_option_pricer
from engine.deriv_options_exotic import barrier_option_pricer as _e_barrier_option_pricer
from engine.deriv_options_exotic import basket_option_pricer as _e_basket_option_pricer
from engine.deriv_options_exotic import bermudan_option_pricer as _e_bermudan_option_pricer
from engine.deriv_options_exotic import chooser_option_pricer as _e_chooser_option_pricer
from engine.deriv_options_exotic import compound_option_pricer as _e_compound_option_pricer
from engine.deriv_options_exotic import digital_option_pricer as _e_digital_option_pricer
from engine.deriv_options_exotic import lookback_option_pricer as _e_lookback_option_pricer
from engine.deriv_options_exotic import rainbow_option_pricer as _e_rainbow_option_pricer
from engine.deriv_options_exotic import (
    spread_option_kirk_approximation as _e_spread_option_kirk_approximation,
)
from engine.deriv_options_vanilla import (
    binomial_tree_option_pricer as _e_binomial_tree_option_pricer,
)
from engine.deriv_options_vanilla import (
    black_scholes_european_option as _e_black_scholes_european_option,
)
from engine.deriv_options_vanilla import black_scholes_greeks as _e_black_scholes_greeks
from engine.deriv_options_vanilla import monte_carlo_option_pricer as _e_monte_carlo_option_pricer
from engine.deriv_options_vanilla import (
    trinomial_tree_option_pricer as _e_trinomial_tree_option_pricer,
)
from engine.deriv_rates import cap_floor_pricer as _e_cap_floor_pricer
from engine.deriv_rates import caplet_floorlet_pricer_black as _e_caplet_floorlet_pricer_black
from engine.deriv_rates import credit_default_swap_cds_pricer as _e_credit_default_swap_cds_pricer
from engine.deriv_rates import cross_currency_swap_pricer as _e_cross_currency_swap_pricer
from engine.deriv_rates import equity_swap_pricer as _e_equity_swap_pricer
from engine.deriv_rates import overnight_index_swap_ois as _e_overnight_index_swap_ois
from engine.deriv_rates import swaption_pricer_black as _e_swaption_pricer_black
from engine.deriv_rates import swaption_pricer_sabr as _e_swaption_pricer_sabr
from engine.deriv_rates import total_return_swap_trs as _e_total_return_swap_trs
from engine.deriv_short_rate import cox_ingersoll_ross_model as _e_cox_ingersoll_ross_model
from engine.deriv_short_rate import hull_white_short_rate_model as _e_hull_white_short_rate_model
from engine.deriv_short_rate import lmm_bgm_rate_model as _e_lmm_bgm_rate_model
from engine.deriv_short_rate import vasicek_interest_rate_model as _e_vasicek_interest_rate_model
from engine.deriv_stoch_vol import displaced_diffusion_model as _e_displaced_diffusion_model
from engine.deriv_stoch_vol import (
    heston_stochastic_volatility_model as _e_heston_stochastic_volatility_model,
)
from engine.deriv_stoch_vol import local_volatility_dupire_model as _e_local_volatility_dupire_model
from engine.deriv_stoch_vol import normal_inverse_gaussian_model as _e_normal_inverse_gaussian_model
from engine.deriv_stoch_vol import (
    rough_volatility_rbergomi_model as _e_rough_volatility_rbergomi_model,
)
from engine.deriv_stoch_vol import sabr_volatility_model as _e_sabr_volatility_model
from engine.deriv_stoch_vol import variance_gamma_model as _e_variance_gamma_model
from schemas.derivatives import (
    AmericanOptionLsmRequest,
    AmericanOptionLsmResponse,
    AsianOptionPricerRequest,
    AsianOptionPricerResponse,
    AssetSwapSpreadRequest,
    AssetSwapSpreadResponse,
    BarrierOptionPricerRequest,
    BarrierOptionPricerResponse,
    BasketOptionPricerRequest,
    BasketOptionPricerResponse,
    BermudanOptionPricerRequest,
    BermudanOptionPricerResponse,
    BinomialTreeOptionPricerRequest,
    BinomialTreeOptionPricerResponse,
    BlackScholesEuropeanOptionRequest,
    BlackScholesEuropeanOptionResponse,
    BlackScholesGreeksRequest,
    BlackScholesGreeksResponse,
    BondPricerFixedCouponRequest,
    BondPricerFixedCouponResponse,
    BondPricerFloatingRateRequest,
    BondPricerFloatingRateResponse,
    BootstrapYieldCurveRequest,
    BootstrapYieldCurveResponse,
    CallableBondPricerRequest,
    CallableBondPricerResponse,
    CapFloorPricerRequest,
    CapFloorPricerResponse,
    CapletFloorletPricerBlackRequest,
    CapletFloorletPricerBlackResponse,
    ChooserOptionPricerRequest,
    ChooserOptionPricerResponse,
    CompoundOptionPricerRequest,
    CompoundOptionPricerResponse,
    ConvertibleBondPricerRequest,
    ConvertibleBondPricerResponse,
    ConvexityRequest,
    ConvexityResponse,
    CoxIngersollRossModelRequest,
    CoxIngersollRossModelResponse,
    CreditDefaultSwapCdsPricerRequest,
    CreditDefaultSwapCdsPricerResponse,
    CrossCurrencySwapPricerRequest,
    CrossCurrencySwapPricerResponse,
    DigitalOptionPricerRequest,
    DigitalOptionPricerResponse,
    DisplacedDiffusionModelRequest,
    DisplacedDiffusionModelResponse,
    DurationMacaulayRequest,
    DurationMacaulayResponse,
    Dv01PvbpRequest,
    Dv01PvbpResponse,
    EffectiveDurationRequest,
    EffectiveDurationResponse,
    EquitySwapPricerRequest,
    EquitySwapPricerResponse,
    ForwardRateAgreementFraRequest,
    ForwardRateAgreementFraResponse,
    FxForwardPricerRequest,
    FxForwardPricerResponse,
    FxOptionPricerGarmanKohlhagenRequest,
    FxOptionPricerGarmanKohlhagenResponse,
    HestonStochasticVolatilityModelRequest,
    HestonStochasticVolatilityModelResponse,
    HullWhiteShortRateModelRequest,
    HullWhiteShortRateModelResponse,
    InflationLinkedBondPricerRequest,
    InflationLinkedBondPricerResponse,
    InterestRateSwapIrsPricerRequest,
    InterestRateSwapIrsPricerResponse,
    LmmBgmRateModelRequest,
    LmmBgmRateModelResponse,
    LocalVolatilityDupireModelRequest,
    LocalVolatilityDupireModelResponse,
    LookbackOptionPricerRequest,
    LookbackOptionPricerResponse,
    ModifiedDurationRequest,
    ModifiedDurationResponse,
    MonteCarloOptionPricerRequest,
    MonteCarloOptionPricerResponse,
    NelsonSiegelCurveFitRequest,
    NelsonSiegelCurveFitResponse,
    NelsonSiegelSvenssonCurveRequest,
    NelsonSiegelSvenssonCurveResponse,
    NormalInverseGaussianModelRequest,
    NormalInverseGaussianModelResponse,
    OasOptionAdjustedSpreadRequest,
    OasOptionAdjustedSpreadResponse,
    OisCurveSoniaSofrRequest,
    OisCurveSoniaSofrResponse,
    OvernightIndexSwapOisRequest,
    OvernightIndexSwapOisResponse,
    PuttableBondPricerRequest,
    PuttableBondPricerResponse,
    RainbowOptionPricerRequest,
    RainbowOptionPricerResponse,
    RoughVolatilityRbergomiModelRequest,
    RoughVolatilityRbergomiModelResponse,
    SabrVolatilityModelRequest,
    SabrVolatilityModelResponse,
    SpreadOptionKirkApproximationRequest,
    SpreadOptionKirkApproximationResponse,
    SwapRateCurveRequest,
    SwapRateCurveResponse,
    SwaptionPricerBlackRequest,
    SwaptionPricerBlackResponse,
    SwaptionPricerSabrRequest,
    SwaptionPricerSabrResponse,
    TotalReturnSwapTrsRequest,
    TotalReturnSwapTrsResponse,
    TrinomialTreeOptionPricerRequest,
    TrinomialTreeOptionPricerResponse,
    VarianceGammaModelRequest,
    VarianceGammaModelResponse,
    VasicekInterestRateModelRequest,
    VasicekInterestRateModelResponse,
    YieldToCallRequest,
    YieldToCallResponse,
    YieldToMaturityRequest,
    YieldToMaturityResponse,
    ZeroCouponBondPricerRequest,
    ZeroCouponBondPricerResponse,
    ZSpreadCalculatorRequest,
    ZSpreadCalculatorResponse,
)

logger = logging.getLogger(__name__)
router = APIRouter(prefix="/derivatives", tags=["derivatives"])

# Engine input errors → HTTP 422. Explicit tuple (no bare except) covers
# bad numeric domains, singular matrices, and shape/key mismatches.
_INPUT_ERRORS = (
    ValueError,
    TypeError,
    KeyError,
    IndexError,
    ArithmeticError,
    AssertionError,
    np.linalg.LinAlgError,
)


@router.post(
    "/black_scholes_european_option",
    response_model=BlackScholesEuropeanOptionResponse,
    summary="Black Scholes European Option",
)
async def black_scholes_european_option(
    body: BlackScholesEuropeanOptionRequest,
    user: TokenPayload = Depends(get_current_user),
) -> OrjsonResponse:
    kwargs = body.model_dump()
    try:
        result: Any = _e_black_scholes_european_option(**kwargs)
    except _INPUT_ERRORS as exc:
        raise HTTPException(
            status_code=status.HTTP_422_UNPROCESSABLE_ENTITY,
            detail=str(exc),
        ) from exc
    if not isinstance(result, dict):
        result = {"result": result}
    return OrjsonResponse(content=result)


@router.post(
    "/black_scholes_greeks",
    response_model=BlackScholesGreeksResponse,
    summary="Black Scholes Greeks",
)
async def black_scholes_greeks(
    body: BlackScholesGreeksRequest,
    user: TokenPayload = Depends(get_current_user),
) -> OrjsonResponse:
    kwargs = body.model_dump()
    try:
        result: Any = _e_black_scholes_greeks(**kwargs)
    except _INPUT_ERRORS as exc:
        raise HTTPException(
            status_code=status.HTTP_422_UNPROCESSABLE_ENTITY,
            detail=str(exc),
        ) from exc
    if not isinstance(result, dict):
        result = {"result": result}
    return OrjsonResponse(content=result)


@router.post(
    "/binomial_tree_option_pricer",
    response_model=BinomialTreeOptionPricerResponse,
    summary="Binomial Tree Option Pricer",
)
async def binomial_tree_option_pricer(
    body: BinomialTreeOptionPricerRequest,
    user: TokenPayload = Depends(get_current_user),
) -> OrjsonResponse:
    kwargs = body.model_dump()
    try:
        result: Any = _e_binomial_tree_option_pricer(**kwargs)
    except _INPUT_ERRORS as exc:
        raise HTTPException(
            status_code=status.HTTP_422_UNPROCESSABLE_ENTITY,
            detail=str(exc),
        ) from exc
    if not isinstance(result, dict):
        result = {"result": result}
    return OrjsonResponse(content=result)


@router.post(
    "/trinomial_tree_option_pricer",
    response_model=TrinomialTreeOptionPricerResponse,
    summary="Trinomial Tree Option Pricer",
)
async def trinomial_tree_option_pricer(
    body: TrinomialTreeOptionPricerRequest,
    user: TokenPayload = Depends(get_current_user),
) -> OrjsonResponse:
    kwargs = body.model_dump()
    try:
        result: Any = _e_trinomial_tree_option_pricer(**kwargs)
    except _INPUT_ERRORS as exc:
        raise HTTPException(
            status_code=status.HTTP_422_UNPROCESSABLE_ENTITY,
            detail=str(exc),
        ) from exc
    if not isinstance(result, dict):
        result = {"result": result}
    return OrjsonResponse(content=result)


@router.post(
    "/monte_carlo_option_pricer",
    response_model=MonteCarloOptionPricerResponse,
    summary="Monte Carlo Option Pricer",
)
async def monte_carlo_option_pricer(
    body: MonteCarloOptionPricerRequest,
    user: TokenPayload = Depends(get_current_user),
) -> OrjsonResponse:
    if body.n_simulations > user.max_simulations:
        raise HTTPException(
            status_code=status.HTTP_403_FORBIDDEN,
            detail=(
                f"Your {user.tier!r} tier allows a maximum of "
                f"{user.max_simulations:,} simulations. "
                f"Requested: {body.n_simulations:,}."
            ),
        )
    kwargs = body.model_dump()
    try:
        result: Any = _e_monte_carlo_option_pricer(**kwargs)
    except _INPUT_ERRORS as exc:
        raise HTTPException(
            status_code=status.HTTP_422_UNPROCESSABLE_ENTITY,
            detail=str(exc),
        ) from exc
    if not isinstance(result, dict):
        result = {"result": result}
    return OrjsonResponse(content=result)


@router.post(
    "/digital_option_pricer",
    response_model=DigitalOptionPricerResponse,
    summary="Digital Option Pricer",
)
async def digital_option_pricer(
    body: DigitalOptionPricerRequest,
    user: TokenPayload = Depends(get_current_user),
) -> OrjsonResponse:
    kwargs = body.model_dump()
    try:
        result: Any = _e_digital_option_pricer(**kwargs)
    except _INPUT_ERRORS as exc:
        raise HTTPException(
            status_code=status.HTTP_422_UNPROCESSABLE_ENTITY,
            detail=str(exc),
        ) from exc
    if not isinstance(result, dict):
        result = {"result": result}
    return OrjsonResponse(content=result)


@router.post(
    "/barrier_option_pricer",
    response_model=BarrierOptionPricerResponse,
    summary="Barrier Option Pricer",
)
async def barrier_option_pricer(
    body: BarrierOptionPricerRequest,
    user: TokenPayload = Depends(get_current_user),
) -> OrjsonResponse:
    kwargs = body.model_dump()
    try:
        result: Any = _e_barrier_option_pricer(**kwargs)
    except _INPUT_ERRORS as exc:
        raise HTTPException(
            status_code=status.HTTP_422_UNPROCESSABLE_ENTITY,
            detail=str(exc),
        ) from exc
    if not isinstance(result, dict):
        result = {"result": result}
    return OrjsonResponse(content=result)


@router.post(
    "/asian_option_pricer",
    response_model=AsianOptionPricerResponse,
    summary="Asian Option Pricer",
)
async def asian_option_pricer(
    body: AsianOptionPricerRequest,
    user: TokenPayload = Depends(get_current_user),
) -> OrjsonResponse:
    if body.n_simulations > user.max_simulations:
        raise HTTPException(
            status_code=status.HTTP_403_FORBIDDEN,
            detail=(
                f"Your {user.tier!r} tier allows a maximum of "
                f"{user.max_simulations:,} simulations. "
                f"Requested: {body.n_simulations:,}."
            ),
        )
    kwargs = body.model_dump()
    try:
        result: Any = _e_asian_option_pricer(**kwargs)
    except _INPUT_ERRORS as exc:
        raise HTTPException(
            status_code=status.HTTP_422_UNPROCESSABLE_ENTITY,
            detail=str(exc),
        ) from exc
    if not isinstance(result, dict):
        result = {"result": result}
    return OrjsonResponse(content=result)


@router.post(
    "/lookback_option_pricer",
    response_model=LookbackOptionPricerResponse,
    summary="Lookback Option Pricer",
)
async def lookback_option_pricer(
    body: LookbackOptionPricerRequest,
    user: TokenPayload = Depends(get_current_user),
) -> OrjsonResponse:
    if body.n_simulations > user.max_simulations:
        raise HTTPException(
            status_code=status.HTTP_403_FORBIDDEN,
            detail=(
                f"Your {user.tier!r} tier allows a maximum of "
                f"{user.max_simulations:,} simulations. "
                f"Requested: {body.n_simulations:,}."
            ),
        )
    kwargs = body.model_dump()
    try:
        result: Any = _e_lookback_option_pricer(**kwargs)
    except _INPUT_ERRORS as exc:
        raise HTTPException(
            status_code=status.HTTP_422_UNPROCESSABLE_ENTITY,
            detail=str(exc),
        ) from exc
    if not isinstance(result, dict):
        result = {"result": result}
    return OrjsonResponse(content=result)


@router.post(
    "/american_option_lsm",
    response_model=AmericanOptionLsmResponse,
    summary="American Option Lsm",
)
async def american_option_lsm(
    body: AmericanOptionLsmRequest,
    user: TokenPayload = Depends(get_current_user),
) -> OrjsonResponse:
    if body.n_simulations > user.max_simulations:
        raise HTTPException(
            status_code=status.HTTP_403_FORBIDDEN,
            detail=(
                f"Your {user.tier!r} tier allows a maximum of "
                f"{user.max_simulations:,} simulations. "
                f"Requested: {body.n_simulations:,}."
            ),
        )
    kwargs = body.model_dump()
    try:
        result: Any = _e_american_option_lsm(**kwargs)
    except _INPUT_ERRORS as exc:
        raise HTTPException(
            status_code=status.HTTP_422_UNPROCESSABLE_ENTITY,
            detail=str(exc),
        ) from exc
    if not isinstance(result, dict):
        result = {"result": result}
    return OrjsonResponse(content=result)


@router.post(
    "/bermudan_option_pricer",
    response_model=BermudanOptionPricerResponse,
    summary="Bermudan Option Pricer",
)
async def bermudan_option_pricer(
    body: BermudanOptionPricerRequest,
    user: TokenPayload = Depends(get_current_user),
) -> OrjsonResponse:
    if body.n_simulations > user.max_simulations:
        raise HTTPException(
            status_code=status.HTTP_403_FORBIDDEN,
            detail=(
                f"Your {user.tier!r} tier allows a maximum of "
                f"{user.max_simulations:,} simulations. "
                f"Requested: {body.n_simulations:,}."
            ),
        )
    kwargs = body.model_dump()
    try:
        result: Any = _e_bermudan_option_pricer(**kwargs)
    except _INPUT_ERRORS as exc:
        raise HTTPException(
            status_code=status.HTTP_422_UNPROCESSABLE_ENTITY,
            detail=str(exc),
        ) from exc
    if not isinstance(result, dict):
        result = {"result": result}
    return OrjsonResponse(content=result)


@router.post(
    "/rainbow_option_pricer",
    response_model=RainbowOptionPricerResponse,
    summary="Rainbow Option Pricer",
)
async def rainbow_option_pricer(
    body: RainbowOptionPricerRequest,
    user: TokenPayload = Depends(get_current_user),
) -> OrjsonResponse:
    if body.n_simulations > user.max_simulations:
        raise HTTPException(
            status_code=status.HTTP_403_FORBIDDEN,
            detail=(
                f"Your {user.tier!r} tier allows a maximum of "
                f"{user.max_simulations:,} simulations. "
                f"Requested: {body.n_simulations:,}."
            ),
        )
    kwargs = body.model_dump()
    kwargs["spots"] = np.asarray(kwargs["spots"], dtype=np.float64)
    kwargs["sigmas"] = np.asarray(kwargs["sigmas"], dtype=np.float64)
    kwargs["correlation"] = np.asarray(kwargs["correlation"], dtype=np.float64)
    try:
        result: Any = _e_rainbow_option_pricer(**kwargs)
    except _INPUT_ERRORS as exc:
        raise HTTPException(
            status_code=status.HTTP_422_UNPROCESSABLE_ENTITY,
            detail=str(exc),
        ) from exc
    if not isinstance(result, dict):
        result = {"result": result}
    return OrjsonResponse(content=result)


@router.post(
    "/basket_option_pricer",
    response_model=BasketOptionPricerResponse,
    summary="Basket Option Pricer",
)
async def basket_option_pricer(
    body: BasketOptionPricerRequest,
    user: TokenPayload = Depends(get_current_user),
) -> OrjsonResponse:
    if body.n_simulations > user.max_simulations:
        raise HTTPException(
            status_code=status.HTTP_403_FORBIDDEN,
            detail=(
                f"Your {user.tier!r} tier allows a maximum of "
                f"{user.max_simulations:,} simulations. "
                f"Requested: {body.n_simulations:,}."
            ),
        )
    kwargs = body.model_dump()
    kwargs["spots"] = np.asarray(kwargs["spots"], dtype=np.float64)
    kwargs["weights"] = np.asarray(kwargs["weights"], dtype=np.float64)
    kwargs["sigmas"] = np.asarray(kwargs["sigmas"], dtype=np.float64)
    kwargs["correlation"] = np.asarray(kwargs["correlation"], dtype=np.float64)
    try:
        result: Any = _e_basket_option_pricer(**kwargs)
    except _INPUT_ERRORS as exc:
        raise HTTPException(
            status_code=status.HTTP_422_UNPROCESSABLE_ENTITY,
            detail=str(exc),
        ) from exc
    if not isinstance(result, dict):
        result = {"result": result}
    return OrjsonResponse(content=result)


@router.post(
    "/spread_option_kirk_approximation",
    response_model=SpreadOptionKirkApproximationResponse,
    summary="Spread Option Kirk Approximation",
)
async def spread_option_kirk_approximation(
    body: SpreadOptionKirkApproximationRequest,
    user: TokenPayload = Depends(get_current_user),
) -> OrjsonResponse:
    kwargs = body.model_dump()
    try:
        result: Any = _e_spread_option_kirk_approximation(**kwargs)
    except _INPUT_ERRORS as exc:
        raise HTTPException(
            status_code=status.HTTP_422_UNPROCESSABLE_ENTITY,
            detail=str(exc),
        ) from exc
    if not isinstance(result, dict):
        result = {"result": result}
    return OrjsonResponse(content=result)


@router.post(
    "/compound_option_pricer",
    response_model=CompoundOptionPricerResponse,
    summary="Compound Option Pricer",
)
async def compound_option_pricer(
    body: CompoundOptionPricerRequest,
    user: TokenPayload = Depends(get_current_user),
) -> OrjsonResponse:
    if body.n_simulations > user.max_simulations:
        raise HTTPException(
            status_code=status.HTTP_403_FORBIDDEN,
            detail=(
                f"Your {user.tier!r} tier allows a maximum of "
                f"{user.max_simulations:,} simulations. "
                f"Requested: {body.n_simulations:,}."
            ),
        )
    kwargs = body.model_dump()
    try:
        result: Any = _e_compound_option_pricer(**kwargs)
    except _INPUT_ERRORS as exc:
        raise HTTPException(
            status_code=status.HTTP_422_UNPROCESSABLE_ENTITY,
            detail=str(exc),
        ) from exc
    if not isinstance(result, dict):
        result = {"result": result}
    return OrjsonResponse(content=result)


@router.post(
    "/chooser_option_pricer",
    response_model=ChooserOptionPricerResponse,
    summary="Chooser Option Pricer",
)
async def chooser_option_pricer(
    body: ChooserOptionPricerRequest,
    user: TokenPayload = Depends(get_current_user),
) -> OrjsonResponse:
    kwargs = body.model_dump()
    try:
        result: Any = _e_chooser_option_pricer(**kwargs)
    except _INPUT_ERRORS as exc:
        raise HTTPException(
            status_code=status.HTTP_422_UNPROCESSABLE_ENTITY,
            detail=str(exc),
        ) from exc
    if not isinstance(result, dict):
        result = {"result": result}
    return OrjsonResponse(content=result)


@router.post(
    "/bond_pricer_fixed_coupon",
    response_model=BondPricerFixedCouponResponse,
    summary="Bond Pricer Fixed Coupon",
)
async def bond_pricer_fixed_coupon(
    body: BondPricerFixedCouponRequest,
    user: TokenPayload = Depends(get_current_user),
) -> OrjsonResponse:
    kwargs = body.model_dump()
    try:
        result: Any = _e_bond_pricer_fixed_coupon(**kwargs)
    except _INPUT_ERRORS as exc:
        raise HTTPException(
            status_code=status.HTTP_422_UNPROCESSABLE_ENTITY,
            detail=str(exc),
        ) from exc
    if not isinstance(result, dict):
        result = {"result": result}
    return OrjsonResponse(content=result)


@router.post(
    "/bond_pricer_floating_rate",
    response_model=BondPricerFloatingRateResponse,
    summary="Bond Pricer Floating Rate",
)
async def bond_pricer_floating_rate(
    body: BondPricerFloatingRateRequest,
    user: TokenPayload = Depends(get_current_user),
) -> OrjsonResponse:
    kwargs = body.model_dump()
    kwargs["reference_rates"] = np.asarray(kwargs["reference_rates"], dtype=np.float64)
    kwargs["discount_rates"] = np.asarray(kwargs["discount_rates"], dtype=np.float64)
    try:
        result: Any = _e_bond_pricer_floating_rate(**kwargs)
    except _INPUT_ERRORS as exc:
        raise HTTPException(
            status_code=status.HTTP_422_UNPROCESSABLE_ENTITY,
            detail=str(exc),
        ) from exc
    if not isinstance(result, dict):
        result = {"result": result}
    return OrjsonResponse(content=result)


@router.post(
    "/zero_coupon_bond_pricer",
    response_model=ZeroCouponBondPricerResponse,
    summary="Zero Coupon Bond Pricer",
)
async def zero_coupon_bond_pricer(
    body: ZeroCouponBondPricerRequest,
    user: TokenPayload = Depends(get_current_user),
) -> OrjsonResponse:
    kwargs = body.model_dump()
    try:
        result: Any = _e_zero_coupon_bond_pricer(**kwargs)
    except _INPUT_ERRORS as exc:
        raise HTTPException(
            status_code=status.HTTP_422_UNPROCESSABLE_ENTITY,
            detail=str(exc),
        ) from exc
    if not isinstance(result, dict):
        result = {"result": result}
    return OrjsonResponse(content=result)


@router.post(
    "/inflation_linked_bond_pricer",
    response_model=InflationLinkedBondPricerResponse,
    summary="Inflation Linked Bond Pricer",
)
async def inflation_linked_bond_pricer(
    body: InflationLinkedBondPricerRequest,
    user: TokenPayload = Depends(get_current_user),
) -> OrjsonResponse:
    kwargs = body.model_dump()
    try:
        result: Any = _e_inflation_linked_bond_pricer(**kwargs)
    except _INPUT_ERRORS as exc:
        raise HTTPException(
            status_code=status.HTTP_422_UNPROCESSABLE_ENTITY,
            detail=str(exc),
        ) from exc
    if not isinstance(result, dict):
        result = {"result": result}
    return OrjsonResponse(content=result)


@router.post(
    "/callable_bond_pricer",
    response_model=CallableBondPricerResponse,
    summary="Callable Bond Pricer",
)
async def callable_bond_pricer(
    body: CallableBondPricerRequest,
    user: TokenPayload = Depends(get_current_user),
) -> OrjsonResponse:
    kwargs = body.model_dump()
    try:
        result: Any = _e_callable_bond_pricer(**kwargs)
    except _INPUT_ERRORS as exc:
        raise HTTPException(
            status_code=status.HTTP_422_UNPROCESSABLE_ENTITY,
            detail=str(exc),
        ) from exc
    if not isinstance(result, dict):
        result = {"result": result}
    return OrjsonResponse(content=result)


@router.post(
    "/puttable_bond_pricer",
    response_model=PuttableBondPricerResponse,
    summary="Puttable Bond Pricer",
)
async def puttable_bond_pricer(
    body: PuttableBondPricerRequest,
    user: TokenPayload = Depends(get_current_user),
) -> OrjsonResponse:
    kwargs = body.model_dump()
    try:
        result: Any = _e_puttable_bond_pricer(**kwargs)
    except _INPUT_ERRORS as exc:
        raise HTTPException(
            status_code=status.HTTP_422_UNPROCESSABLE_ENTITY,
            detail=str(exc),
        ) from exc
    if not isinstance(result, dict):
        result = {"result": result}
    return OrjsonResponse(content=result)


@router.post(
    "/convertible_bond_pricer",
    response_model=ConvertibleBondPricerResponse,
    summary="Convertible Bond Pricer",
)
async def convertible_bond_pricer(
    body: ConvertibleBondPricerRequest,
    user: TokenPayload = Depends(get_current_user),
) -> OrjsonResponse:
    kwargs = body.model_dump()
    try:
        result: Any = _e_convertible_bond_pricer(**kwargs)
    except _INPUT_ERRORS as exc:
        raise HTTPException(
            status_code=status.HTTP_422_UNPROCESSABLE_ENTITY,
            detail=str(exc),
        ) from exc
    if not isinstance(result, dict):
        result = {"result": result}
    return OrjsonResponse(content=result)


@router.post(
    "/duration_macaulay",
    response_model=DurationMacaulayResponse,
    summary="Duration Macaulay",
)
async def duration_macaulay(
    body: DurationMacaulayRequest,
    user: TokenPayload = Depends(get_current_user),
) -> OrjsonResponse:
    kwargs = body.model_dump()
    kwargs["cashflows"] = np.asarray(kwargs["cashflows"], dtype=np.float64)
    kwargs["times"] = np.asarray(kwargs["times"], dtype=np.float64)
    try:
        result: Any = _e_duration_macaulay(**kwargs)
    except _INPUT_ERRORS as exc:
        raise HTTPException(
            status_code=status.HTTP_422_UNPROCESSABLE_ENTITY,
            detail=str(exc),
        ) from exc
    if not isinstance(result, dict):
        result = {"result": result}
    return OrjsonResponse(content=result)


@router.post(
    "/modified_duration",
    response_model=ModifiedDurationResponse,
    summary="Modified Duration",
)
async def modified_duration(
    body: ModifiedDurationRequest,
    user: TokenPayload = Depends(get_current_user),
) -> OrjsonResponse:
    kwargs = body.model_dump()
    kwargs["cashflows"] = np.asarray(kwargs["cashflows"], dtype=np.float64)
    kwargs["times"] = np.asarray(kwargs["times"], dtype=np.float64)
    try:
        result: Any = _e_modified_duration(**kwargs)
    except _INPUT_ERRORS as exc:
        raise HTTPException(
            status_code=status.HTTP_422_UNPROCESSABLE_ENTITY,
            detail=str(exc),
        ) from exc
    if not isinstance(result, dict):
        result = {"result": result}
    return OrjsonResponse(content=result)


@router.post(
    "/effective_duration",
    response_model=EffectiveDurationResponse,
    summary="Effective Duration",
)
async def effective_duration(
    body: EffectiveDurationRequest,
    user: TokenPayload = Depends(get_current_user),
) -> OrjsonResponse:
    kwargs = body.model_dump()
    try:
        result: Any = _e_effective_duration(**kwargs)
    except _INPUT_ERRORS as exc:
        raise HTTPException(
            status_code=status.HTTP_422_UNPROCESSABLE_ENTITY,
            detail=str(exc),
        ) from exc
    if not isinstance(result, dict):
        result = {"result": result}
    return OrjsonResponse(content=result)


@router.post(
    "/convexity",
    response_model=ConvexityResponse,
    summary="Convexity",
)
async def convexity(
    body: ConvexityRequest,
    user: TokenPayload = Depends(get_current_user),
) -> OrjsonResponse:
    kwargs = body.model_dump()
    kwargs["cashflows"] = np.asarray(kwargs["cashflows"], dtype=np.float64)
    kwargs["times"] = np.asarray(kwargs["times"], dtype=np.float64)
    try:
        result: Any = _e_convexity(**kwargs)
    except _INPUT_ERRORS as exc:
        raise HTTPException(
            status_code=status.HTTP_422_UNPROCESSABLE_ENTITY,
            detail=str(exc),
        ) from exc
    if not isinstance(result, dict):
        result = {"result": result}
    return OrjsonResponse(content=result)


@router.post(
    "/dv01_pvbp",
    response_model=Dv01PvbpResponse,
    summary="Dv01 Pvbp",
)
async def dv01_pvbp(
    body: Dv01PvbpRequest,
    user: TokenPayload = Depends(get_current_user),
) -> OrjsonResponse:
    kwargs = body.model_dump()
    kwargs["cashflows"] = np.asarray(kwargs["cashflows"], dtype=np.float64)
    kwargs["times"] = np.asarray(kwargs["times"], dtype=np.float64)
    try:
        result: Any = _e_dv01_pvbp(**kwargs)
    except _INPUT_ERRORS as exc:
        raise HTTPException(
            status_code=status.HTTP_422_UNPROCESSABLE_ENTITY,
            detail=str(exc),
        ) from exc
    if not isinstance(result, dict):
        result = {"result": result}
    return OrjsonResponse(content=result)


@router.post(
    "/yield_to_maturity",
    response_model=YieldToMaturityResponse,
    summary="Yield To Maturity",
)
async def yield_to_maturity(
    body: YieldToMaturityRequest,
    user: TokenPayload = Depends(get_current_user),
) -> OrjsonResponse:
    kwargs = body.model_dump()
    kwargs["cashflows"] = np.asarray(kwargs["cashflows"], dtype=np.float64)
    kwargs["times"] = np.asarray(kwargs["times"], dtype=np.float64)
    try:
        result: Any = _e_yield_to_maturity(**kwargs)
    except _INPUT_ERRORS as exc:
        raise HTTPException(
            status_code=status.HTTP_422_UNPROCESSABLE_ENTITY,
            detail=str(exc),
        ) from exc
    if not isinstance(result, dict):
        result = {"result": result}
    return OrjsonResponse(content=result)


@router.post(
    "/yield_to_call",
    response_model=YieldToCallResponse,
    summary="Yield To Call",
)
async def yield_to_call(
    body: YieldToCallRequest,
    user: TokenPayload = Depends(get_current_user),
) -> OrjsonResponse:
    kwargs = body.model_dump()
    try:
        result: Any = _e_yield_to_call(**kwargs)
    except _INPUT_ERRORS as exc:
        raise HTTPException(
            status_code=status.HTTP_422_UNPROCESSABLE_ENTITY,
            detail=str(exc),
        ) from exc
    if not isinstance(result, dict):
        result = {"result": result}
    return OrjsonResponse(content=result)


@router.post(
    "/z_spread_calculator",
    response_model=ZSpreadCalculatorResponse,
    summary="Z Spread Calculator",
)
async def z_spread_calculator(
    body: ZSpreadCalculatorRequest,
    user: TokenPayload = Depends(get_current_user),
) -> OrjsonResponse:
    kwargs = body.model_dump()
    kwargs["cashflows"] = np.asarray(kwargs["cashflows"], dtype=np.float64)
    kwargs["times"] = np.asarray(kwargs["times"], dtype=np.float64)
    kwargs["zero_rates"] = np.asarray(kwargs["zero_rates"], dtype=np.float64)
    try:
        result: Any = _e_z_spread_calculator(**kwargs)
    except _INPUT_ERRORS as exc:
        raise HTTPException(
            status_code=status.HTTP_422_UNPROCESSABLE_ENTITY,
            detail=str(exc),
        ) from exc
    if not isinstance(result, dict):
        result = {"result": result}
    return OrjsonResponse(content=result)


@router.post(
    "/asset_swap_spread",
    response_model=AssetSwapSpreadResponse,
    summary="Asset Swap Spread",
)
async def asset_swap_spread(
    body: AssetSwapSpreadRequest,
    user: TokenPayload = Depends(get_current_user),
) -> OrjsonResponse:
    kwargs = body.model_dump()
    kwargs["cashflows"] = np.asarray(kwargs["cashflows"], dtype=np.float64)
    kwargs["times"] = np.asarray(kwargs["times"], dtype=np.float64)
    kwargs["swap_rates"] = np.asarray(kwargs["swap_rates"], dtype=np.float64)
    try:
        result: Any = _e_asset_swap_spread(**kwargs)
    except _INPUT_ERRORS as exc:
        raise HTTPException(
            status_code=status.HTTP_422_UNPROCESSABLE_ENTITY,
            detail=str(exc),
        ) from exc
    if not isinstance(result, dict):
        result = {"result": result}
    return OrjsonResponse(content=result)


@router.post(
    "/oas_option_adjusted_spread",
    response_model=OasOptionAdjustedSpreadResponse,
    summary="Oas Option Adjusted Spread",
)
async def oas_option_adjusted_spread(
    body: OasOptionAdjustedSpreadRequest,
    user: TokenPayload = Depends(get_current_user),
) -> OrjsonResponse:
    kwargs = body.model_dump()
    try:
        result: Any = _e_oas_option_adjusted_spread(**kwargs)
    except _INPUT_ERRORS as exc:
        raise HTTPException(
            status_code=status.HTTP_422_UNPROCESSABLE_ENTITY,
            detail=str(exc),
        ) from exc
    if not isinstance(result, dict):
        result = {"result": result}
    return OrjsonResponse(content=result)


@router.post(
    "/nelson_siegel_curve_fit",
    response_model=NelsonSiegelCurveFitResponse,
    summary="Nelson Siegel Curve Fit",
)
async def nelson_siegel_curve_fit(
    body: NelsonSiegelCurveFitRequest,
    user: TokenPayload = Depends(get_current_user),
) -> OrjsonResponse:
    kwargs = body.model_dump()
    kwargs["maturities"] = np.asarray(kwargs["maturities"], dtype=np.float64)
    kwargs["yields"] = np.asarray(kwargs["yields"], dtype=np.float64)
    try:
        result: Any = _e_nelson_siegel_curve_fit(**kwargs)
    except _INPUT_ERRORS as exc:
        raise HTTPException(
            status_code=status.HTTP_422_UNPROCESSABLE_ENTITY,
            detail=str(exc),
        ) from exc
    if not isinstance(result, dict):
        result = {"result": result}
    return OrjsonResponse(content=result)


@router.post(
    "/nelson_siegel_svensson_curve",
    response_model=NelsonSiegelSvenssonCurveResponse,
    summary="Nelson Siegel Svensson Curve",
)
async def nelson_siegel_svensson_curve(
    body: NelsonSiegelSvenssonCurveRequest,
    user: TokenPayload = Depends(get_current_user),
) -> OrjsonResponse:
    kwargs = body.model_dump()
    kwargs["maturities"] = np.asarray(kwargs["maturities"], dtype=np.float64)
    kwargs["yields"] = np.asarray(kwargs["yields"], dtype=np.float64)
    try:
        result: Any = _e_nelson_siegel_svensson_curve(**kwargs)
    except _INPUT_ERRORS as exc:
        raise HTTPException(
            status_code=status.HTTP_422_UNPROCESSABLE_ENTITY,
            detail=str(exc),
        ) from exc
    if not isinstance(result, dict):
        result = {"result": result}
    return OrjsonResponse(content=result)


@router.post(
    "/bootstrap_yield_curve",
    response_model=BootstrapYieldCurveResponse,
    summary="Bootstrap Yield Curve",
)
async def bootstrap_yield_curve(
    body: BootstrapYieldCurveRequest,
    user: TokenPayload = Depends(get_current_user),
) -> OrjsonResponse:
    kwargs = body.model_dump()
    kwargs["par_rates"] = np.asarray(kwargs["par_rates"], dtype=np.float64)
    kwargs["maturities"] = np.asarray(kwargs["maturities"], dtype=np.float64)
    try:
        result: Any = _e_bootstrap_yield_curve(**kwargs)
    except _INPUT_ERRORS as exc:
        raise HTTPException(
            status_code=status.HTTP_422_UNPROCESSABLE_ENTITY,
            detail=str(exc),
        ) from exc
    if not isinstance(result, dict):
        result = {"result": result}
    return OrjsonResponse(content=result)


@router.post(
    "/swap_rate_curve",
    response_model=SwapRateCurveResponse,
    summary="Swap Rate Curve",
)
async def swap_rate_curve(
    body: SwapRateCurveRequest,
    user: TokenPayload = Depends(get_current_user),
) -> OrjsonResponse:
    kwargs = body.model_dump()
    kwargs["discount_factors"] = np.asarray(kwargs["discount_factors"], dtype=np.float64)
    kwargs["maturities"] = np.asarray(kwargs["maturities"], dtype=np.float64)
    try:
        result: Any = _e_swap_rate_curve(**kwargs)
    except _INPUT_ERRORS as exc:
        raise HTTPException(
            status_code=status.HTTP_422_UNPROCESSABLE_ENTITY,
            detail=str(exc),
        ) from exc
    if not isinstance(result, dict):
        result = {"result": result}
    return OrjsonResponse(content=result)


@router.post(
    "/ois_curve_sonia_sofr",
    response_model=OisCurveSoniaSofrResponse,
    summary="Ois Curve Sonia Sofr",
)
async def ois_curve_sonia_sofr(
    body: OisCurveSoniaSofrRequest,
    user: TokenPayload = Depends(get_current_user),
) -> OrjsonResponse:
    kwargs = body.model_dump()
    kwargs["ois_rates"] = np.asarray(kwargs["ois_rates"], dtype=np.float64)
    kwargs["maturities"] = np.asarray(kwargs["maturities"], dtype=np.float64)
    try:
        result: Any = _e_ois_curve_sonia_sofr(**kwargs)
    except _INPUT_ERRORS as exc:
        raise HTTPException(
            status_code=status.HTTP_422_UNPROCESSABLE_ENTITY,
            detail=str(exc),
        ) from exc
    if not isinstance(result, dict):
        result = {"result": result}
    return OrjsonResponse(content=result)


@router.post(
    "/forward_rate_agreement_fra",
    response_model=ForwardRateAgreementFraResponse,
    summary="Forward Rate Agreement Fra",
)
async def forward_rate_agreement_fra(
    body: ForwardRateAgreementFraRequest,
    user: TokenPayload = Depends(get_current_user),
) -> OrjsonResponse:
    kwargs = body.model_dump()
    try:
        result: Any = _e_forward_rate_agreement_fra(**kwargs)
    except _INPUT_ERRORS as exc:
        raise HTTPException(
            status_code=status.HTTP_422_UNPROCESSABLE_ENTITY,
            detail=str(exc),
        ) from exc
    if not isinstance(result, dict):
        result = {"result": result}
    return OrjsonResponse(content=result)


@router.post(
    "/interest_rate_swap_irs_pricer",
    response_model=InterestRateSwapIrsPricerResponse,
    summary="Interest Rate Swap Irs Pricer",
)
async def interest_rate_swap_irs_pricer(
    body: InterestRateSwapIrsPricerRequest,
    user: TokenPayload = Depends(get_current_user),
) -> OrjsonResponse:
    kwargs = body.model_dump()
    kwargs["forward_rates"] = np.asarray(kwargs["forward_rates"], dtype=np.float64)
    kwargs["discount_factors"] = np.asarray(kwargs["discount_factors"], dtype=np.float64)
    kwargs["accruals"] = np.asarray(kwargs["accruals"], dtype=np.float64)
    try:
        result: Any = _e_interest_rate_swap_irs_pricer(**kwargs)
    except _INPUT_ERRORS as exc:
        raise HTTPException(
            status_code=status.HTTP_422_UNPROCESSABLE_ENTITY,
            detail=str(exc),
        ) from exc
    if not isinstance(result, dict):
        result = {"result": result}
    return OrjsonResponse(content=result)


@router.post(
    "/cross_currency_swap_pricer",
    response_model=CrossCurrencySwapPricerResponse,
    summary="Cross Currency Swap Pricer",
)
async def cross_currency_swap_pricer(
    body: CrossCurrencySwapPricerRequest,
    user: TokenPayload = Depends(get_current_user),
) -> OrjsonResponse:
    kwargs = body.model_dump()
    kwargs["df_dom"] = np.asarray(kwargs["df_dom"], dtype=np.float64)
    kwargs["df_for"] = np.asarray(kwargs["df_for"], dtype=np.float64)
    kwargs["accruals"] = np.asarray(kwargs["accruals"], dtype=np.float64)
    try:
        result: Any = _e_cross_currency_swap_pricer(**kwargs)
    except _INPUT_ERRORS as exc:
        raise HTTPException(
            status_code=status.HTTP_422_UNPROCESSABLE_ENTITY,
            detail=str(exc),
        ) from exc
    if not isinstance(result, dict):
        result = {"result": result}
    return OrjsonResponse(content=result)


@router.post(
    "/overnight_index_swap_ois",
    response_model=OvernightIndexSwapOisResponse,
    summary="Overnight Index Swap Ois",
)
async def overnight_index_swap_ois(
    body: OvernightIndexSwapOisRequest,
    user: TokenPayload = Depends(get_current_user),
) -> OrjsonResponse:
    kwargs = body.model_dump()
    try:
        result: Any = _e_overnight_index_swap_ois(**kwargs)
    except _INPUT_ERRORS as exc:
        raise HTTPException(
            status_code=status.HTTP_422_UNPROCESSABLE_ENTITY,
            detail=str(exc),
        ) from exc
    if not isinstance(result, dict):
        result = {"result": result}
    return OrjsonResponse(content=result)


@router.post(
    "/total_return_swap_trs",
    response_model=TotalReturnSwapTrsResponse,
    summary="Total Return Swap Trs",
)
async def total_return_swap_trs(
    body: TotalReturnSwapTrsRequest,
    user: TokenPayload = Depends(get_current_user),
) -> OrjsonResponse:
    kwargs = body.model_dump()
    try:
        result: Any = _e_total_return_swap_trs(**kwargs)
    except _INPUT_ERRORS as exc:
        raise HTTPException(
            status_code=status.HTTP_422_UNPROCESSABLE_ENTITY,
            detail=str(exc),
        ) from exc
    if not isinstance(result, dict):
        result = {"result": result}
    return OrjsonResponse(content=result)


@router.post(
    "/credit_default_swap_cds_pricer",
    response_model=CreditDefaultSwapCdsPricerResponse,
    summary="Credit Default Swap Cds Pricer",
)
async def credit_default_swap_cds_pricer(
    body: CreditDefaultSwapCdsPricerRequest,
    user: TokenPayload = Depends(get_current_user),
) -> OrjsonResponse:
    kwargs = body.model_dump()
    try:
        result: Any = _e_credit_default_swap_cds_pricer(**kwargs)
    except _INPUT_ERRORS as exc:
        raise HTTPException(
            status_code=status.HTTP_422_UNPROCESSABLE_ENTITY,
            detail=str(exc),
        ) from exc
    if not isinstance(result, dict):
        result = {"result": result}
    return OrjsonResponse(content=result)


@router.post(
    "/equity_swap_pricer",
    response_model=EquitySwapPricerResponse,
    summary="Equity Swap Pricer",
)
async def equity_swap_pricer(
    body: EquitySwapPricerRequest,
    user: TokenPayload = Depends(get_current_user),
) -> OrjsonResponse:
    kwargs = body.model_dump()
    try:
        result: Any = _e_equity_swap_pricer(**kwargs)
    except _INPUT_ERRORS as exc:
        raise HTTPException(
            status_code=status.HTTP_422_UNPROCESSABLE_ENTITY,
            detail=str(exc),
        ) from exc
    if not isinstance(result, dict):
        result = {"result": result}
    return OrjsonResponse(content=result)


@router.post(
    "/caplet_floorlet_pricer_black",
    response_model=CapletFloorletPricerBlackResponse,
    summary="Caplet Floorlet Pricer Black",
)
async def caplet_floorlet_pricer_black(
    body: CapletFloorletPricerBlackRequest,
    user: TokenPayload = Depends(get_current_user),
) -> OrjsonResponse:
    kwargs = body.model_dump()
    try:
        result: Any = _e_caplet_floorlet_pricer_black(**kwargs)
    except _INPUT_ERRORS as exc:
        raise HTTPException(
            status_code=status.HTTP_422_UNPROCESSABLE_ENTITY,
            detail=str(exc),
        ) from exc
    if not isinstance(result, dict):
        result = {"result": result}
    return OrjsonResponse(content=result)


@router.post(
    "/cap_floor_pricer",
    response_model=CapFloorPricerResponse,
    summary="Cap Floor Pricer",
)
async def cap_floor_pricer(
    body: CapFloorPricerRequest,
    user: TokenPayload = Depends(get_current_user),
) -> OrjsonResponse:
    kwargs = body.model_dump()
    kwargs["forward_rates"] = np.asarray(kwargs["forward_rates"], dtype=np.float64)
    kwargs["vols"] = np.asarray(kwargs["vols"], dtype=np.float64)
    kwargs["expiries"] = np.asarray(kwargs["expiries"], dtype=np.float64)
    kwargs["accruals"] = np.asarray(kwargs["accruals"], dtype=np.float64)
    kwargs["discount_factors"] = np.asarray(kwargs["discount_factors"], dtype=np.float64)
    try:
        result: Any = _e_cap_floor_pricer(**kwargs)
    except _INPUT_ERRORS as exc:
        raise HTTPException(
            status_code=status.HTTP_422_UNPROCESSABLE_ENTITY,
            detail=str(exc),
        ) from exc
    if not isinstance(result, dict):
        result = {"result": result}
    return OrjsonResponse(content=result)


@router.post(
    "/swaption_pricer_black",
    response_model=SwaptionPricerBlackResponse,
    summary="Swaption Pricer Black",
)
async def swaption_pricer_black(
    body: SwaptionPricerBlackRequest,
    user: TokenPayload = Depends(get_current_user),
) -> OrjsonResponse:
    kwargs = body.model_dump()
    try:
        result: Any = _e_swaption_pricer_black(**kwargs)
    except _INPUT_ERRORS as exc:
        raise HTTPException(
            status_code=status.HTTP_422_UNPROCESSABLE_ENTITY,
            detail=str(exc),
        ) from exc
    if not isinstance(result, dict):
        result = {"result": result}
    return OrjsonResponse(content=result)


@router.post(
    "/swaption_pricer_sabr",
    response_model=SwaptionPricerSabrResponse,
    summary="Swaption Pricer Sabr",
)
async def swaption_pricer_sabr(
    body: SwaptionPricerSabrRequest,
    user: TokenPayload = Depends(get_current_user),
) -> OrjsonResponse:
    kwargs = body.model_dump()
    try:
        result: Any = _e_swaption_pricer_sabr(**kwargs)
    except _INPUT_ERRORS as exc:
        raise HTTPException(
            status_code=status.HTTP_422_UNPROCESSABLE_ENTITY,
            detail=str(exc),
        ) from exc
    if not isinstance(result, dict):
        result = {"result": result}
    return OrjsonResponse(content=result)


@router.post(
    "/vasicek_interest_rate_model",
    response_model=VasicekInterestRateModelResponse,
    summary="Vasicek Interest Rate Model",
)
async def vasicek_interest_rate_model(
    body: VasicekInterestRateModelRequest,
    user: TokenPayload = Depends(get_current_user),
) -> OrjsonResponse:
    if body.n_simulations > user.max_simulations:
        raise HTTPException(
            status_code=status.HTTP_403_FORBIDDEN,
            detail=(
                f"Your {user.tier!r} tier allows a maximum of "
                f"{user.max_simulations:,} simulations. "
                f"Requested: {body.n_simulations:,}."
            ),
        )
    kwargs = body.model_dump()
    try:
        result: Any = _e_vasicek_interest_rate_model(**kwargs)
    except _INPUT_ERRORS as exc:
        raise HTTPException(
            status_code=status.HTTP_422_UNPROCESSABLE_ENTITY,
            detail=str(exc),
        ) from exc
    if not isinstance(result, dict):
        result = {"result": result}
    return OrjsonResponse(content=result)


@router.post(
    "/cox_ingersoll_ross_model",
    response_model=CoxIngersollRossModelResponse,
    summary="Cox Ingersoll Ross Model",
)
async def cox_ingersoll_ross_model(
    body: CoxIngersollRossModelRequest,
    user: TokenPayload = Depends(get_current_user),
) -> OrjsonResponse:
    if body.n_simulations > user.max_simulations:
        raise HTTPException(
            status_code=status.HTTP_403_FORBIDDEN,
            detail=(
                f"Your {user.tier!r} tier allows a maximum of "
                f"{user.max_simulations:,} simulations. "
                f"Requested: {body.n_simulations:,}."
            ),
        )
    kwargs = body.model_dump()
    try:
        result: Any = _e_cox_ingersoll_ross_model(**kwargs)
    except _INPUT_ERRORS as exc:
        raise HTTPException(
            status_code=status.HTTP_422_UNPROCESSABLE_ENTITY,
            detail=str(exc),
        ) from exc
    if not isinstance(result, dict):
        result = {"result": result}
    return OrjsonResponse(content=result)


@router.post(
    "/hull_white_short_rate_model",
    response_model=HullWhiteShortRateModelResponse,
    summary="Hull White Short Rate Model",
)
async def hull_white_short_rate_model(
    body: HullWhiteShortRateModelRequest,
    user: TokenPayload = Depends(get_current_user),
) -> OrjsonResponse:
    if body.n_simulations > user.max_simulations:
        raise HTTPException(
            status_code=status.HTTP_403_FORBIDDEN,
            detail=(
                f"Your {user.tier!r} tier allows a maximum of "
                f"{user.max_simulations:,} simulations. "
                f"Requested: {body.n_simulations:,}."
            ),
        )
    kwargs = body.model_dump()
    try:
        result: Any = _e_hull_white_short_rate_model(**kwargs)
    except _INPUT_ERRORS as exc:
        raise HTTPException(
            status_code=status.HTTP_422_UNPROCESSABLE_ENTITY,
            detail=str(exc),
        ) from exc
    if not isinstance(result, dict):
        result = {"result": result}
    return OrjsonResponse(content=result)


@router.post(
    "/lmm_bgm_rate_model",
    response_model=LmmBgmRateModelResponse,
    summary="Lmm Bgm Rate Model",
)
async def lmm_bgm_rate_model(
    body: LmmBgmRateModelRequest,
    user: TokenPayload = Depends(get_current_user),
) -> OrjsonResponse:
    if body.n_simulations > user.max_simulations:
        raise HTTPException(
            status_code=status.HTTP_403_FORBIDDEN,
            detail=(
                f"Your {user.tier!r} tier allows a maximum of "
                f"{user.max_simulations:,} simulations. "
                f"Requested: {body.n_simulations:,}."
            ),
        )
    kwargs = body.model_dump()
    kwargs["forward_rates"] = np.asarray(kwargs["forward_rates"], dtype=np.float64)
    kwargs["vols"] = np.asarray(kwargs["vols"], dtype=np.float64)
    try:
        result: Any = _e_lmm_bgm_rate_model(**kwargs)
    except _INPUT_ERRORS as exc:
        raise HTTPException(
            status_code=status.HTTP_422_UNPROCESSABLE_ENTITY,
            detail=str(exc),
        ) from exc
    if not isinstance(result, dict):
        result = {"result": result}
    return OrjsonResponse(content=result)


@router.post(
    "/heston_stochastic_volatility_model",
    response_model=HestonStochasticVolatilityModelResponse,
    summary="Heston Stochastic Volatility Model",
)
async def heston_stochastic_volatility_model(
    body: HestonStochasticVolatilityModelRequest,
    user: TokenPayload = Depends(get_current_user),
) -> OrjsonResponse:
    kwargs = body.model_dump()
    try:
        result: Any = _e_heston_stochastic_volatility_model(**kwargs)
    except _INPUT_ERRORS as exc:
        raise HTTPException(
            status_code=status.HTTP_422_UNPROCESSABLE_ENTITY,
            detail=str(exc),
        ) from exc
    if not isinstance(result, dict):
        result = {"result": result}
    return OrjsonResponse(content=result)


@router.post(
    "/sabr_volatility_model",
    response_model=SabrVolatilityModelResponse,
    summary="Sabr Volatility Model",
)
async def sabr_volatility_model(
    body: SabrVolatilityModelRequest,
    user: TokenPayload = Depends(get_current_user),
) -> OrjsonResponse:
    kwargs = body.model_dump()
    try:
        result: Any = _e_sabr_volatility_model(**kwargs)
    except _INPUT_ERRORS as exc:
        raise HTTPException(
            status_code=status.HTTP_422_UNPROCESSABLE_ENTITY,
            detail=str(exc),
        ) from exc
    if not isinstance(result, dict):
        result = {"result": result}
    return OrjsonResponse(content=result)


@router.post(
    "/local_volatility_dupire_model",
    response_model=LocalVolatilityDupireModelResponse,
    summary="Local Volatility Dupire Model",
)
async def local_volatility_dupire_model(
    body: LocalVolatilityDupireModelRequest,
    user: TokenPayload = Depends(get_current_user),
) -> OrjsonResponse:
    kwargs = body.model_dump()
    kwargs["strikes"] = np.asarray(kwargs["strikes"], dtype=np.float64)
    kwargs["maturities"] = np.asarray(kwargs["maturities"], dtype=np.float64)
    kwargs["call_surface"] = np.asarray(kwargs["call_surface"], dtype=np.float64)
    try:
        result: Any = _e_local_volatility_dupire_model(**kwargs)
    except _INPUT_ERRORS as exc:
        raise HTTPException(
            status_code=status.HTTP_422_UNPROCESSABLE_ENTITY,
            detail=str(exc),
        ) from exc
    if not isinstance(result, dict):
        result = {"result": result}
    return OrjsonResponse(content=result)


@router.post(
    "/rough_volatility_rbergomi_model",
    response_model=RoughVolatilityRbergomiModelResponse,
    summary="Rough Volatility Rbergomi Model",
)
async def rough_volatility_rbergomi_model(
    body: RoughVolatilityRbergomiModelRequest,
    user: TokenPayload = Depends(get_current_user),
) -> OrjsonResponse:
    if body.n_simulations > user.max_simulations:
        raise HTTPException(
            status_code=status.HTTP_403_FORBIDDEN,
            detail=(
                f"Your {user.tier!r} tier allows a maximum of "
                f"{user.max_simulations:,} simulations. "
                f"Requested: {body.n_simulations:,}."
            ),
        )
    kwargs = body.model_dump()
    try:
        result: Any = _e_rough_volatility_rbergomi_model(**kwargs)
    except _INPUT_ERRORS as exc:
        raise HTTPException(
            status_code=status.HTTP_422_UNPROCESSABLE_ENTITY,
            detail=str(exc),
        ) from exc
    if not isinstance(result, dict):
        result = {"result": result}
    return OrjsonResponse(content=result)


@router.post(
    "/variance_gamma_model",
    response_model=VarianceGammaModelResponse,
    summary="Variance Gamma Model",
)
async def variance_gamma_model(
    body: VarianceGammaModelRequest,
    user: TokenPayload = Depends(get_current_user),
) -> OrjsonResponse:
    if body.n_simulations > user.max_simulations:
        raise HTTPException(
            status_code=status.HTTP_403_FORBIDDEN,
            detail=(
                f"Your {user.tier!r} tier allows a maximum of "
                f"{user.max_simulations:,} simulations. "
                f"Requested: {body.n_simulations:,}."
            ),
        )
    kwargs = body.model_dump()
    try:
        result: Any = _e_variance_gamma_model(**kwargs)
    except _INPUT_ERRORS as exc:
        raise HTTPException(
            status_code=status.HTTP_422_UNPROCESSABLE_ENTITY,
            detail=str(exc),
        ) from exc
    if not isinstance(result, dict):
        result = {"result": result}
    return OrjsonResponse(content=result)


@router.post(
    "/normal_inverse_gaussian_model",
    response_model=NormalInverseGaussianModelResponse,
    summary="Normal Inverse Gaussian Model",
)
async def normal_inverse_gaussian_model(
    body: NormalInverseGaussianModelRequest,
    user: TokenPayload = Depends(get_current_user),
) -> OrjsonResponse:
    if body.n_simulations > user.max_simulations:
        raise HTTPException(
            status_code=status.HTTP_403_FORBIDDEN,
            detail=(
                f"Your {user.tier!r} tier allows a maximum of "
                f"{user.max_simulations:,} simulations. "
                f"Requested: {body.n_simulations:,}."
            ),
        )
    kwargs = body.model_dump()
    try:
        result: Any = _e_normal_inverse_gaussian_model(**kwargs)
    except _INPUT_ERRORS as exc:
        raise HTTPException(
            status_code=status.HTTP_422_UNPROCESSABLE_ENTITY,
            detail=str(exc),
        ) from exc
    if not isinstance(result, dict):
        result = {"result": result}
    return OrjsonResponse(content=result)


@router.post(
    "/displaced_diffusion_model",
    response_model=DisplacedDiffusionModelResponse,
    summary="Displaced Diffusion Model",
)
async def displaced_diffusion_model(
    body: DisplacedDiffusionModelRequest,
    user: TokenPayload = Depends(get_current_user),
) -> OrjsonResponse:
    kwargs = body.model_dump()
    try:
        result: Any = _e_displaced_diffusion_model(**kwargs)
    except _INPUT_ERRORS as exc:
        raise HTTPException(
            status_code=status.HTTP_422_UNPROCESSABLE_ENTITY,
            detail=str(exc),
        ) from exc
    if not isinstance(result, dict):
        result = {"result": result}
    return OrjsonResponse(content=result)


@router.post(
    "/fx_forward_pricer",
    response_model=FxForwardPricerResponse,
    summary="Fx Forward Pricer",
)
async def fx_forward_pricer(
    body: FxForwardPricerRequest,
    user: TokenPayload = Depends(get_current_user),
) -> OrjsonResponse:
    kwargs = body.model_dump()
    try:
        result: Any = _e_fx_forward_pricer(**kwargs)
    except _INPUT_ERRORS as exc:
        raise HTTPException(
            status_code=status.HTTP_422_UNPROCESSABLE_ENTITY,
            detail=str(exc),
        ) from exc
    if not isinstance(result, dict):
        result = {"result": result}
    return OrjsonResponse(content=result)


@router.post(
    "/fx_option_pricer_garman_kohlhagen",
    response_model=FxOptionPricerGarmanKohlhagenResponse,
    summary="Fx Option Pricer Garman Kohlhagen",
)
async def fx_option_pricer_garman_kohlhagen(
    body: FxOptionPricerGarmanKohlhagenRequest,
    user: TokenPayload = Depends(get_current_user),
) -> OrjsonResponse:
    kwargs = body.model_dump()
    try:
        result: Any = _e_fx_option_pricer_garman_kohlhagen(**kwargs)
    except _INPUT_ERRORS as exc:
        raise HTTPException(
            status_code=status.HTTP_422_UNPROCESSABLE_ENTITY,
            detail=str(exc),
        ) from exc
    if not isinstance(result, dict):
        result = {"result": result}
    return OrjsonResponse(content=result)

"""api/routes/credit_risk.py — auto-generated FastAPI routes for the credit-risk domain.

Generated by scripts/gen_p3.py. Each route validates its request, converts
list payloads to NumPy arrays, calls the engine wrapper (the single source
of truth), and returns the result dict via OrjsonResponse. No business logic
lives here.
"""

from __future__ import annotations

import logging
from typing import Any

import numpy as np
from fastapi import APIRouter, Depends, HTTPException, status

from api.middleware.auth import TokenPayload, get_current_user
from api.responses import OrjsonResponse
from engine.credit_capital import (
    basel_standardised_approach_rwa as _e_basel_standardised_approach_rwa,
)
from engine.credit_capital import irb_advanced_approach_capital as _e_irb_advanced_approach_capital
from engine.credit_capital import (
    irb_foundation_approach_capital as _e_irb_foundation_approach_capital,
)
from engine.credit_capital import maturity_adjustment_basel_irb as _e_maturity_adjustment_basel_irb
from engine.credit_capital import sme_correlation_factor_basel as _e_sme_correlation_factor_basel
from engine.credit_ccr import collateral_haircut_calculation as _e_collateral_haircut_calculation
from engine.credit_ccr import (
    counterparty_credit_risk_ccr_exposure as _e_counterparty_credit_risk_ccr_exposure,
)
from engine.credit_ccr import current_exposure_method_cem as _e_current_exposure_method_cem
from engine.credit_ccr import effective_epe_regulatory as _e_effective_epe_regulatory
from engine.credit_ccr import expected_positive_exposure_epe as _e_expected_positive_exposure_epe
from engine.credit_ccr import potential_future_exposure_pfe as _e_potential_future_exposure_pfe
from engine.credit_ccr import (
    standardised_approach_ccr_sa_ccr as _e_standardised_approach_ccr_sa_ccr,
)
from engine.credit_cds import cds_pricing_isda_standard as _e_cds_pricing_isda_standard
from engine.credit_cds import cds_spread_to_pd_conversion as _e_cds_spread_to_pd_conversion
from engine.credit_cds import credit_default_swap_var as _e_credit_default_swap_var
from engine.credit_cds import credit_spread_curve_bootstrap as _e_credit_spread_curve_bootstrap
from engine.credit_ifrs9 import credit_portfolio_optimisation as _e_credit_portfolio_optimisation
from engine.credit_ifrs9 import credit_stress_testing as _e_credit_stress_testing
from engine.credit_ifrs9 import ifrs_9_12_month_ecl_stage_1 as _e_ifrs_9_12_month_ecl_stage_1
from engine.credit_ifrs9 import ifrs_9_lifetime_ecl_stage_2_3 as _e_ifrs_9_lifetime_ecl_stage_2_3
from engine.credit_ifrs9 import ifrs_9_scenario_weighted_ecl as _e_ifrs_9_scenario_weighted_ecl
from engine.credit_ifrs9 import (
    ifrs_9_stage_classification_pd_threshold as _e_ifrs_9_stage_classification_pd_threshold,
)
from engine.credit_ifrs9 import (
    ifrs_9_staging_criteria_assessment as _e_ifrs_9_staging_criteria_assessment,
)
from engine.credit_ifrs9 import macroeconomic_overlays_ecl as _e_macroeconomic_overlays_ecl
from engine.credit_pd_lgd import downturn_lgd_adjustment as _e_downturn_lgd_adjustment
from engine.credit_pd_lgd import expected_loss_el_computation as _e_expected_loss_el_computation
from engine.credit_pd_lgd import (
    exposure_at_default_ead_calculator as _e_exposure_at_default_ead_calculator,
)
from engine.credit_pd_lgd import loss_given_default_lgd_model as _e_loss_given_default_lgd_model
from engine.credit_pd_lgd import (
    probability_of_default_pd_estimation as _e_probability_of_default_pd_estimation,
)
from engine.credit_pd_lgd import recovery_rate_estimation as _e_recovery_rate_estimation
from engine.credit_pd_lgd import unexpected_loss_ul_computation as _e_unexpected_loss_ul_computation
from engine.credit_scoring import altman_z_score_credit_scoring as _e_altman_z_score_credit_scoring
from engine.credit_scoring import (
    corporate_credit_scoring_model as _e_corporate_credit_scoring_model,
)
from engine.credit_scoring import logistic_regression_pd_model as _e_logistic_regression_pd_model
from engine.credit_scoring import (
    machine_learning_pd_calibration as _e_machine_learning_pd_calibration,
)
from engine.credit_scoring import point_in_time_pd_estimation as _e_point_in_time_pd_estimation
from engine.credit_scoring import ratings_migration_matrix as _e_ratings_migration_matrix
from engine.credit_scoring import retail_scorecard_pd_model as _e_retail_scorecard_pd_model
from engine.credit_scoring import sector_default_rate_analysis as _e_sector_default_rate_analysis
from engine.credit_scoring import (
    sovereign_credit_risk_assessment as _e_sovereign_credit_risk_assessment,
)
from engine.credit_scoring import (
    through_the_cycle_pd_adjustment as _e_through_the_cycle_pd_adjustment,
)
from engine.credit_var import credit_concentration_risk_hhi as _e_credit_concentration_risk_hhi
from engine.credit_var import credit_var_analytical_vasicek as _e_credit_var_analytical_vasicek
from engine.credit_var import credit_var_monte_carlo as _e_credit_var_monte_carlo
from engine.credit_var import creditmetrics_portfolio_model as _e_creditmetrics_portfolio_model
from engine.credit_var import default_correlation_matrix as _e_default_correlation_matrix
from engine.credit_var import kmv_merton_distance_to_default as _e_kmv_merton_distance_to_default
from engine.credit_xva import (
    capital_valuation_adjustment_kva as _e_capital_valuation_adjustment_kva,
)
from engine.credit_xva import credit_valuation_adjustment_cva as _e_credit_valuation_adjustment_cva
from engine.credit_xva import cva_sensitivity_cva_greeks as _e_cva_sensitivity_cva_greeks
from engine.credit_xva import debt_valuation_adjustment_dva as _e_debt_valuation_adjustment_dva
from engine.credit_xva import (
    funding_valuation_adjustment_fva as _e_funding_valuation_adjustment_fva,
)
from engine.credit_xva import margin_valuation_adjustment_mva as _e_margin_valuation_adjustment_mva
from engine.credit_xva import wrong_way_risk_adjustment as _e_wrong_way_risk_adjustment
from engine.credit_xva import xva_aggregation as _e_xva_aggregation
from schemas.credit_risk import (
    AltmanZScoreCreditScoringRequest,
    AltmanZScoreCreditScoringResponse,
    BaselStandardisedApproachRwaRequest,
    BaselStandardisedApproachRwaResponse,
    CapitalValuationAdjustmentKvaRequest,
    CapitalValuationAdjustmentKvaResponse,
    CdsPricingIsdaStandardRequest,
    CdsPricingIsdaStandardResponse,
    CdsSpreadToPdConversionRequest,
    CdsSpreadToPdConversionResponse,
    CollateralHaircutCalculationRequest,
    CollateralHaircutCalculationResponse,
    CorporateCreditScoringModelRequest,
    CorporateCreditScoringModelResponse,
    CounterpartyCreditRiskCcrExposureRequest,
    CounterpartyCreditRiskCcrExposureResponse,
    CreditConcentrationRiskHhiRequest,
    CreditConcentrationRiskHhiResponse,
    CreditDefaultSwapVarRequest,
    CreditDefaultSwapVarResponse,
    CreditmetricsPortfolioModelRequest,
    CreditmetricsPortfolioModelResponse,
    CreditPortfolioOptimisationRequest,
    CreditPortfolioOptimisationResponse,
    CreditSpreadCurveBootstrapRequest,
    CreditSpreadCurveBootstrapResponse,
    CreditStressTestingRequest,
    CreditStressTestingResponse,
    CreditValuationAdjustmentCvaRequest,
    CreditValuationAdjustmentCvaResponse,
    CreditVarAnalyticalVasicekRequest,
    CreditVarAnalyticalVasicekResponse,
    CreditVarMonteCarloRequest,
    CreditVarMonteCarloResponse,
    CurrentExposureMethodCemRequest,
    CurrentExposureMethodCemResponse,
    CvaSensitivityCvaGreeksRequest,
    CvaSensitivityCvaGreeksResponse,
    DebtValuationAdjustmentDvaRequest,
    DebtValuationAdjustmentDvaResponse,
    DefaultCorrelationMatrixRequest,
    DefaultCorrelationMatrixResponse,
    DownturnLgdAdjustmentRequest,
    DownturnLgdAdjustmentResponse,
    EffectiveEpeRegulatoryRequest,
    EffectiveEpeRegulatoryResponse,
    ExpectedLossElComputationRequest,
    ExpectedLossElComputationResponse,
    ExpectedPositiveExposureEpeRequest,
    ExpectedPositiveExposureEpeResponse,
    ExposureAtDefaultEadCalculatorRequest,
    ExposureAtDefaultEadCalculatorResponse,
    FundingValuationAdjustmentFvaRequest,
    FundingValuationAdjustmentFvaResponse,
    Ifrs9LifetimeEclStage23Request,
    Ifrs9LifetimeEclStage23Response,
    Ifrs9ScenarioWeightedEclRequest,
    Ifrs9ScenarioWeightedEclResponse,
    Ifrs9StageClassificationPdThresholdRequest,
    Ifrs9StageClassificationPdThresholdResponse,
    Ifrs9StagingCriteriaAssessmentRequest,
    Ifrs9StagingCriteriaAssessmentResponse,
    Ifrs912MonthEclStage1Request,
    Ifrs912MonthEclStage1Response,
    IrbAdvancedApproachCapitalRequest,
    IrbAdvancedApproachCapitalResponse,
    IrbFoundationApproachCapitalRequest,
    IrbFoundationApproachCapitalResponse,
    KmvMertonDistanceToDefaultRequest,
    KmvMertonDistanceToDefaultResponse,
    LogisticRegressionPdModelRequest,
    LogisticRegressionPdModelResponse,
    LossGivenDefaultLgdModelRequest,
    LossGivenDefaultLgdModelResponse,
    MachineLearningPdCalibrationRequest,
    MachineLearningPdCalibrationResponse,
    MacroeconomicOverlaysEclRequest,
    MacroeconomicOverlaysEclResponse,
    MarginValuationAdjustmentMvaRequest,
    MarginValuationAdjustmentMvaResponse,
    MaturityAdjustmentBaselIrbRequest,
    MaturityAdjustmentBaselIrbResponse,
    PointInTimePdEstimationRequest,
    PointInTimePdEstimationResponse,
    PotentialFutureExposurePfeRequest,
    PotentialFutureExposurePfeResponse,
    ProbabilityOfDefaultPdEstimationRequest,
    ProbabilityOfDefaultPdEstimationResponse,
    RatingsMigrationMatrixRequest,
    RatingsMigrationMatrixResponse,
    RecoveryRateEstimationRequest,
    RecoveryRateEstimationResponse,
    RetailScorecardPdModelRequest,
    RetailScorecardPdModelResponse,
    SectorDefaultRateAnalysisRequest,
    SectorDefaultRateAnalysisResponse,
    SmeCorrelationFactorBaselRequest,
    SmeCorrelationFactorBaselResponse,
    SovereignCreditRiskAssessmentRequest,
    SovereignCreditRiskAssessmentResponse,
    StandardisedApproachCcrSaCcrRequest,
    StandardisedApproachCcrSaCcrResponse,
    ThroughTheCyclePdAdjustmentRequest,
    ThroughTheCyclePdAdjustmentResponse,
    UnexpectedLossUlComputationRequest,
    UnexpectedLossUlComputationResponse,
    WrongWayRiskAdjustmentRequest,
    WrongWayRiskAdjustmentResponse,
    XvaAggregationRequest,
    XvaAggregationResponse,
)

logger = logging.getLogger(__name__)
router = APIRouter(prefix="/credit-risk", tags=["credit-risk"])

# Engine input errors → HTTP 422. Explicit tuple (no bare except) covers
# bad numeric domains, singular matrices, and shape/key mismatches.
_INPUT_ERRORS = (
    ValueError,
    TypeError,
    KeyError,
    IndexError,
    ArithmeticError,
    AssertionError,
    np.linalg.LinAlgError,
)


@router.post(
    "/probability_of_default_pd_estimation",
    response_model=ProbabilityOfDefaultPdEstimationResponse,
    summary="Probability Of Default Pd Estimation",
)
async def probability_of_default_pd_estimation(
    body: ProbabilityOfDefaultPdEstimationRequest,
    user: TokenPayload = Depends(get_current_user),
) -> OrjsonResponse:
    kwargs = body.model_dump()
    kwargs["n_defaults"] = np.asarray(kwargs["n_defaults"], dtype=np.float64)
    kwargs["n_obligors"] = np.asarray(kwargs["n_obligors"], dtype=np.float64)
    try:
        result: Any = _e_probability_of_default_pd_estimation(**kwargs)
    except _INPUT_ERRORS as exc:
        raise HTTPException(
            status_code=status.HTTP_422_UNPROCESSABLE_ENTITY,
            detail=str(exc),
        ) from exc
    if not isinstance(result, dict):
        result = {"result": result}
    return OrjsonResponse(content=result)


@router.post(
    "/loss_given_default_lgd_model",
    response_model=LossGivenDefaultLgdModelResponse,
    summary="Loss Given Default Lgd Model",
)
async def loss_given_default_lgd_model(
    body: LossGivenDefaultLgdModelRequest,
    user: TokenPayload = Depends(get_current_user),
) -> OrjsonResponse:
    kwargs = body.model_dump()
    kwargs["recovery_amounts"] = np.asarray(kwargs["recovery_amounts"], dtype=np.float64)
    kwargs["exposure_amounts"] = np.asarray(kwargs["exposure_amounts"], dtype=np.float64)
    try:
        result: Any = _e_loss_given_default_lgd_model(**kwargs)
    except _INPUT_ERRORS as exc:
        raise HTTPException(
            status_code=status.HTTP_422_UNPROCESSABLE_ENTITY,
            detail=str(exc),
        ) from exc
    if not isinstance(result, dict):
        result = {"result": result}
    return OrjsonResponse(content=result)


@router.post(
    "/exposure_at_default_ead_calculator",
    response_model=ExposureAtDefaultEadCalculatorResponse,
    summary="Exposure At Default Ead Calculator",
)
async def exposure_at_default_ead_calculator(
    body: ExposureAtDefaultEadCalculatorRequest,
    user: TokenPayload = Depends(get_current_user),
) -> OrjsonResponse:
    kwargs = body.model_dump()
    try:
        result: Any = _e_exposure_at_default_ead_calculator(**kwargs)
    except _INPUT_ERRORS as exc:
        raise HTTPException(
            status_code=status.HTTP_422_UNPROCESSABLE_ENTITY,
            detail=str(exc),
        ) from exc
    if not isinstance(result, dict):
        result = {"result": result}
    return OrjsonResponse(content=result)


@router.post(
    "/expected_loss_el_computation",
    response_model=ExpectedLossElComputationResponse,
    summary="Expected Loss El Computation",
)
async def expected_loss_el_computation(
    body: ExpectedLossElComputationRequest,
    user: TokenPayload = Depends(get_current_user),
) -> OrjsonResponse:
    kwargs = body.model_dump()
    try:
        result: Any = _e_expected_loss_el_computation(**kwargs)
    except _INPUT_ERRORS as exc:
        raise HTTPException(
            status_code=status.HTTP_422_UNPROCESSABLE_ENTITY,
            detail=str(exc),
        ) from exc
    if not isinstance(result, dict):
        result = {"result": result}
    return OrjsonResponse(content=result)


@router.post(
    "/unexpected_loss_ul_computation",
    response_model=UnexpectedLossUlComputationResponse,
    summary="Unexpected Loss Ul Computation",
)
async def unexpected_loss_ul_computation(
    body: UnexpectedLossUlComputationRequest,
    user: TokenPayload = Depends(get_current_user),
) -> OrjsonResponse:
    kwargs = body.model_dump()
    kwargs["pd"] = np.asarray(kwargs["pd"], dtype=np.float64)
    kwargs["lgd"] = np.asarray(kwargs["lgd"], dtype=np.float64)
    kwargs["ead"] = np.asarray(kwargs["ead"], dtype=np.float64)
    try:
        result: Any = _e_unexpected_loss_ul_computation(**kwargs)
    except _INPUT_ERRORS as exc:
        raise HTTPException(
            status_code=status.HTTP_422_UNPROCESSABLE_ENTITY,
            detail=str(exc),
        ) from exc
    if not isinstance(result, dict):
        result = {"result": result}
    return OrjsonResponse(content=result)


@router.post(
    "/recovery_rate_estimation",
    response_model=RecoveryRateEstimationResponse,
    summary="Recovery Rate Estimation",
)
async def recovery_rate_estimation(
    body: RecoveryRateEstimationRequest,
    user: TokenPayload = Depends(get_current_user),
) -> OrjsonResponse:
    kwargs = body.model_dump()
    kwargs["recovery_amounts"] = np.asarray(kwargs["recovery_amounts"], dtype=np.float64)
    kwargs["exposure_amounts"] = np.asarray(kwargs["exposure_amounts"], dtype=np.float64)
    if kwargs["discount_factors"] is not None:
        kwargs["discount_factors"] = np.asarray(kwargs["discount_factors"], dtype=np.float64)
    try:
        result: Any = _e_recovery_rate_estimation(**kwargs)
    except _INPUT_ERRORS as exc:
        raise HTTPException(
            status_code=status.HTTP_422_UNPROCESSABLE_ENTITY,
            detail=str(exc),
        ) from exc
    if not isinstance(result, dict):
        result = {"result": result}
    return OrjsonResponse(content=result)


@router.post(
    "/downturn_lgd_adjustment",
    response_model=DownturnLgdAdjustmentResponse,
    summary="Downturn Lgd Adjustment",
)
async def downturn_lgd_adjustment(
    body: DownturnLgdAdjustmentRequest,
    user: TokenPayload = Depends(get_current_user),
) -> OrjsonResponse:
    kwargs = body.model_dump()
    try:
        result: Any = _e_downturn_lgd_adjustment(**kwargs)
    except _INPUT_ERRORS as exc:
        raise HTTPException(
            status_code=status.HTTP_422_UNPROCESSABLE_ENTITY,
            detail=str(exc),
        ) from exc
    if not isinstance(result, dict):
        result = {"result": result}
    return OrjsonResponse(content=result)


@router.post(
    "/irb_foundation_approach_capital",
    response_model=IrbFoundationApproachCapitalResponse,
    summary="Irb Foundation Approach Capital",
)
async def irb_foundation_approach_capital(
    body: IrbFoundationApproachCapitalRequest,
    user: TokenPayload = Depends(get_current_user),
) -> OrjsonResponse:
    kwargs = body.model_dump()
    try:
        result: Any = _e_irb_foundation_approach_capital(**kwargs)
    except _INPUT_ERRORS as exc:
        raise HTTPException(
            status_code=status.HTTP_422_UNPROCESSABLE_ENTITY,
            detail=str(exc),
        ) from exc
    if not isinstance(result, dict):
        result = {"result": result}
    return OrjsonResponse(content=result)


@router.post(
    "/irb_advanced_approach_capital",
    response_model=IrbAdvancedApproachCapitalResponse,
    summary="Irb Advanced Approach Capital",
)
async def irb_advanced_approach_capital(
    body: IrbAdvancedApproachCapitalRequest,
    user: TokenPayload = Depends(get_current_user),
) -> OrjsonResponse:
    kwargs = body.model_dump()
    try:
        result: Any = _e_irb_advanced_approach_capital(**kwargs)
    except _INPUT_ERRORS as exc:
        raise HTTPException(
            status_code=status.HTTP_422_UNPROCESSABLE_ENTITY,
            detail=str(exc),
        ) from exc
    if not isinstance(result, dict):
        result = {"result": result}
    return OrjsonResponse(content=result)


@router.post(
    "/basel_standardised_approach_rwa",
    response_model=BaselStandardisedApproachRwaResponse,
    summary="Basel Standardised Approach Rwa",
)
async def basel_standardised_approach_rwa(
    body: BaselStandardisedApproachRwaRequest,
    user: TokenPayload = Depends(get_current_user),
) -> OrjsonResponse:
    kwargs = body.model_dump()
    try:
        result: Any = _e_basel_standardised_approach_rwa(**kwargs)
    except _INPUT_ERRORS as exc:
        raise HTTPException(
            status_code=status.HTTP_422_UNPROCESSABLE_ENTITY,
            detail=str(exc),
        ) from exc
    if not isinstance(result, dict):
        result = {"result": result}
    return OrjsonResponse(content=result)


@router.post(
    "/maturity_adjustment_basel_irb",
    response_model=MaturityAdjustmentBaselIrbResponse,
    summary="Maturity Adjustment Basel Irb",
)
async def maturity_adjustment_basel_irb(
    body: MaturityAdjustmentBaselIrbRequest,
    user: TokenPayload = Depends(get_current_user),
) -> OrjsonResponse:
    kwargs = body.model_dump()
    try:
        result: Any = _e_maturity_adjustment_basel_irb(**kwargs)
    except _INPUT_ERRORS as exc:
        raise HTTPException(
            status_code=status.HTTP_422_UNPROCESSABLE_ENTITY,
            detail=str(exc),
        ) from exc
    if not isinstance(result, dict):
        result = {"result": result}
    return OrjsonResponse(content=result)


@router.post(
    "/sme_correlation_factor_basel",
    response_model=SmeCorrelationFactorBaselResponse,
    summary="Sme Correlation Factor Basel",
)
async def sme_correlation_factor_basel(
    body: SmeCorrelationFactorBaselRequest,
    user: TokenPayload = Depends(get_current_user),
) -> OrjsonResponse:
    kwargs = body.model_dump()
    try:
        result: Any = _e_sme_correlation_factor_basel(**kwargs)
    except _INPUT_ERRORS as exc:
        raise HTTPException(
            status_code=status.HTTP_422_UNPROCESSABLE_ENTITY,
            detail=str(exc),
        ) from exc
    if not isinstance(result, dict):
        result = {"result": result}
    return OrjsonResponse(content=result)


@router.post(
    "/credit_var_monte_carlo",
    response_model=CreditVarMonteCarloResponse,
    summary="Credit Var Monte Carlo",
)
async def credit_var_monte_carlo(
    body: CreditVarMonteCarloRequest,
    user: TokenPayload = Depends(get_current_user),
) -> OrjsonResponse:
    if body.n_simulations > user.max_simulations:
        raise HTTPException(
            status_code=status.HTTP_403_FORBIDDEN,
            detail=(
                f"Your {user.tier!r} tier allows a maximum of "
                f"{user.max_simulations:,} simulations. "
                f"Requested: {body.n_simulations:,}."
            ),
        )
    kwargs = body.model_dump()
    kwargs["pd"] = np.asarray(kwargs["pd"], dtype=np.float64)
    kwargs["lgd"] = np.asarray(kwargs["lgd"], dtype=np.float64)
    kwargs["ead"] = np.asarray(kwargs["ead"], dtype=np.float64)
    if isinstance(kwargs["asset_correlation"], list):
        kwargs["asset_correlation"] = np.asarray(kwargs["asset_correlation"], dtype=np.float64)
    try:
        result: Any = _e_credit_var_monte_carlo(**kwargs)
    except _INPUT_ERRORS as exc:
        raise HTTPException(
            status_code=status.HTTP_422_UNPROCESSABLE_ENTITY,
            detail=str(exc),
        ) from exc
    if not isinstance(result, dict):
        result = {"result": result}
    return OrjsonResponse(content=result)


@router.post(
    "/credit_var_analytical_vasicek",
    response_model=CreditVarAnalyticalVasicekResponse,
    summary="Credit Var Analytical Vasicek",
)
async def credit_var_analytical_vasicek(
    body: CreditVarAnalyticalVasicekRequest,
    user: TokenPayload = Depends(get_current_user),
) -> OrjsonResponse:
    kwargs = body.model_dump()
    try:
        result: Any = _e_credit_var_analytical_vasicek(**kwargs)
    except _INPUT_ERRORS as exc:
        raise HTTPException(
            status_code=status.HTTP_422_UNPROCESSABLE_ENTITY,
            detail=str(exc),
        ) from exc
    if not isinstance(result, dict):
        result = {"result": result}
    return OrjsonResponse(content=result)


@router.post(
    "/creditmetrics_portfolio_model",
    response_model=CreditmetricsPortfolioModelResponse,
    summary="Creditmetrics Portfolio Model",
)
async def creditmetrics_portfolio_model(
    body: CreditmetricsPortfolioModelRequest,
    user: TokenPayload = Depends(get_current_user),
) -> OrjsonResponse:
    if body.n_simulations > user.max_simulations:
        raise HTTPException(
            status_code=status.HTTP_403_FORBIDDEN,
            detail=(
                f"Your {user.tier!r} tier allows a maximum of "
                f"{user.max_simulations:,} simulations. "
                f"Requested: {body.n_simulations:,}."
            ),
        )
    kwargs = body.model_dump()
    kwargs["exposures"] = np.asarray(kwargs["exposures"], dtype=np.float64)
    kwargs["pd"] = np.asarray(kwargs["pd"], dtype=np.float64)
    kwargs["lgd"] = np.asarray(kwargs["lgd"], dtype=np.float64)
    try:
        result: Any = _e_creditmetrics_portfolio_model(**kwargs)
    except _INPUT_ERRORS as exc:
        raise HTTPException(
            status_code=status.HTTP_422_UNPROCESSABLE_ENTITY,
            detail=str(exc),
        ) from exc
    if not isinstance(result, dict):
        result = {"result": result}
    return OrjsonResponse(content=result)


@router.post(
    "/kmv_merton_distance_to_default",
    response_model=KmvMertonDistanceToDefaultResponse,
    summary="Kmv Merton Distance To Default",
)
async def kmv_merton_distance_to_default(
    body: KmvMertonDistanceToDefaultRequest,
    user: TokenPayload = Depends(get_current_user),
) -> OrjsonResponse:
    kwargs = body.model_dump()
    try:
        result: Any = _e_kmv_merton_distance_to_default(**kwargs)
    except _INPUT_ERRORS as exc:
        raise HTTPException(
            status_code=status.HTTP_422_UNPROCESSABLE_ENTITY,
            detail=str(exc),
        ) from exc
    if not isinstance(result, dict):
        result = {"result": result}
    return OrjsonResponse(content=result)


@router.post(
    "/default_correlation_matrix",
    response_model=DefaultCorrelationMatrixResponse,
    summary="Default Correlation Matrix",
)
async def default_correlation_matrix(
    body: DefaultCorrelationMatrixRequest,
    user: TokenPayload = Depends(get_current_user),
) -> OrjsonResponse:
    kwargs = body.model_dump()
    kwargs["pd"] = np.asarray(kwargs["pd"], dtype=np.float64)
    kwargs["asset_correlation"] = np.asarray(kwargs["asset_correlation"], dtype=np.float64)
    try:
        result: Any = _e_default_correlation_matrix(**kwargs)
    except _INPUT_ERRORS as exc:
        raise HTTPException(
            status_code=status.HTTP_422_UNPROCESSABLE_ENTITY,
            detail=str(exc),
        ) from exc
    if not isinstance(result, dict):
        result = {"result": result}
    return OrjsonResponse(content=result)


@router.post(
    "/credit_concentration_risk_hhi",
    response_model=CreditConcentrationRiskHhiResponse,
    summary="Credit Concentration Risk Hhi",
)
async def credit_concentration_risk_hhi(
    body: CreditConcentrationRiskHhiRequest,
    user: TokenPayload = Depends(get_current_user),
) -> OrjsonResponse:
    kwargs = body.model_dump()
    kwargs["exposures"] = np.asarray(kwargs["exposures"], dtype=np.float64)
    try:
        result: Any = _e_credit_concentration_risk_hhi(**kwargs)
    except _INPUT_ERRORS as exc:
        raise HTTPException(
            status_code=status.HTTP_422_UNPROCESSABLE_ENTITY,
            detail=str(exc),
        ) from exc
    if not isinstance(result, dict):
        result = {"result": result}
    return OrjsonResponse(content=result)


@router.post(
    "/counterparty_credit_risk_ccr_exposure",
    response_model=CounterpartyCreditRiskCcrExposureResponse,
    summary="Counterparty Credit Risk Ccr Exposure",
)
async def counterparty_credit_risk_ccr_exposure(
    body: CounterpartyCreditRiskCcrExposureRequest,
    user: TokenPayload = Depends(get_current_user),
) -> OrjsonResponse:
    kwargs = body.model_dump()
    try:
        result: Any = _e_counterparty_credit_risk_ccr_exposure(**kwargs)
    except _INPUT_ERRORS as exc:
        raise HTTPException(
            status_code=status.HTTP_422_UNPROCESSABLE_ENTITY,
            detail=str(exc),
        ) from exc
    if not isinstance(result, dict):
        result = {"result": result}
    return OrjsonResponse(content=result)


@router.post(
    "/current_exposure_method_cem",
    response_model=CurrentExposureMethodCemResponse,
    summary="Current Exposure Method Cem",
)
async def current_exposure_method_cem(
    body: CurrentExposureMethodCemRequest,
    user: TokenPayload = Depends(get_current_user),
) -> OrjsonResponse:
    kwargs = body.model_dump()
    try:
        result: Any = _e_current_exposure_method_cem(**kwargs)
    except _INPUT_ERRORS as exc:
        raise HTTPException(
            status_code=status.HTTP_422_UNPROCESSABLE_ENTITY,
            detail=str(exc),
        ) from exc
    if not isinstance(result, dict):
        result = {"result": result}
    return OrjsonResponse(content=result)


@router.post(
    "/standardised_approach_ccr_sa_ccr",
    response_model=StandardisedApproachCcrSaCcrResponse,
    summary="Standardised Approach Ccr Sa Ccr",
)
async def standardised_approach_ccr_sa_ccr(
    body: StandardisedApproachCcrSaCcrRequest,
    user: TokenPayload = Depends(get_current_user),
) -> OrjsonResponse:
    kwargs = body.model_dump()
    try:
        result: Any = _e_standardised_approach_ccr_sa_ccr(**kwargs)
    except _INPUT_ERRORS as exc:
        raise HTTPException(
            status_code=status.HTTP_422_UNPROCESSABLE_ENTITY,
            detail=str(exc),
        ) from exc
    if not isinstance(result, dict):
        result = {"result": result}
    return OrjsonResponse(content=result)


@router.post(
    "/potential_future_exposure_pfe",
    response_model=PotentialFutureExposurePfeResponse,
    summary="Potential Future Exposure Pfe",
)
async def potential_future_exposure_pfe(
    body: PotentialFutureExposurePfeRequest,
    user: TokenPayload = Depends(get_current_user),
) -> OrjsonResponse:
    kwargs = body.model_dump()
    kwargs["time_steps"] = np.asarray(kwargs["time_steps"], dtype=np.float64)
    try:
        result: Any = _e_potential_future_exposure_pfe(**kwargs)
    except _INPUT_ERRORS as exc:
        raise HTTPException(
            status_code=status.HTTP_422_UNPROCESSABLE_ENTITY,
            detail=str(exc),
        ) from exc
    if not isinstance(result, dict):
        result = {"result": result}
    return OrjsonResponse(content=result)


@router.post(
    "/expected_positive_exposure_epe",
    response_model=ExpectedPositiveExposureEpeResponse,
    summary="Expected Positive Exposure Epe",
)
async def expected_positive_exposure_epe(
    body: ExpectedPositiveExposureEpeRequest,
    user: TokenPayload = Depends(get_current_user),
) -> OrjsonResponse:
    kwargs = body.model_dump()
    kwargs["expected_exposure"] = np.asarray(kwargs["expected_exposure"], dtype=np.float64)
    kwargs["time_steps"] = np.asarray(kwargs["time_steps"], dtype=np.float64)
    try:
        result: Any = _e_expected_positive_exposure_epe(**kwargs)
    except _INPUT_ERRORS as exc:
        raise HTTPException(
            status_code=status.HTTP_422_UNPROCESSABLE_ENTITY,
            detail=str(exc),
        ) from exc
    if not isinstance(result, dict):
        result = {"result": result}
    return OrjsonResponse(content=result)


@router.post(
    "/effective_epe_regulatory",
    response_model=EffectiveEpeRegulatoryResponse,
    summary="Effective Epe Regulatory",
)
async def effective_epe_regulatory(
    body: EffectiveEpeRegulatoryRequest,
    user: TokenPayload = Depends(get_current_user),
) -> OrjsonResponse:
    kwargs = body.model_dump()
    kwargs["expected_exposure"] = np.asarray(kwargs["expected_exposure"], dtype=np.float64)
    kwargs["time_steps"] = np.asarray(kwargs["time_steps"], dtype=np.float64)
    try:
        result: Any = _e_effective_epe_regulatory(**kwargs)
    except _INPUT_ERRORS as exc:
        raise HTTPException(
            status_code=status.HTTP_422_UNPROCESSABLE_ENTITY,
            detail=str(exc),
        ) from exc
    if not isinstance(result, dict):
        result = {"result": result}
    return OrjsonResponse(content=result)


@router.post(
    "/collateral_haircut_calculation",
    response_model=CollateralHaircutCalculationResponse,
    summary="Collateral Haircut Calculation",
)
async def collateral_haircut_calculation(
    body: CollateralHaircutCalculationRequest,
    user: TokenPayload = Depends(get_current_user),
) -> OrjsonResponse:
    kwargs = body.model_dump()
    try:
        result: Any = _e_collateral_haircut_calculation(**kwargs)
    except _INPUT_ERRORS as exc:
        raise HTTPException(
            status_code=status.HTTP_422_UNPROCESSABLE_ENTITY,
            detail=str(exc),
        ) from exc
    if not isinstance(result, dict):
        result = {"result": result}
    return OrjsonResponse(content=result)


@router.post(
    "/credit_valuation_adjustment_cva",
    response_model=CreditValuationAdjustmentCvaResponse,
    summary="Credit Valuation Adjustment Cva",
)
async def credit_valuation_adjustment_cva(
    body: CreditValuationAdjustmentCvaRequest,
    user: TokenPayload = Depends(get_current_user),
) -> OrjsonResponse:
    kwargs = body.model_dump()
    kwargs["expected_exposure"] = np.asarray(kwargs["expected_exposure"], dtype=np.float64)
    kwargs["time_steps"] = np.asarray(kwargs["time_steps"], dtype=np.float64)
    kwargs["discount_factors"] = np.asarray(kwargs["discount_factors"], dtype=np.float64)
    try:
        result: Any = _e_credit_valuation_adjustment_cva(**kwargs)
    except _INPUT_ERRORS as exc:
        raise HTTPException(
            status_code=status.HTTP_422_UNPROCESSABLE_ENTITY,
            detail=str(exc),
        ) from exc
    if not isinstance(result, dict):
        result = {"result": result}
    return OrjsonResponse(content=result)


@router.post(
    "/debt_valuation_adjustment_dva",
    response_model=DebtValuationAdjustmentDvaResponse,
    summary="Debt Valuation Adjustment Dva",
)
async def debt_valuation_adjustment_dva(
    body: DebtValuationAdjustmentDvaRequest,
    user: TokenPayload = Depends(get_current_user),
) -> OrjsonResponse:
    kwargs = body.model_dump()
    kwargs["expected_negative_exposure"] = np.asarray(
        kwargs["expected_negative_exposure"], dtype=np.float64
    )
    kwargs["time_steps"] = np.asarray(kwargs["time_steps"], dtype=np.float64)
    kwargs["discount_factors"] = np.asarray(kwargs["discount_factors"], dtype=np.float64)
    try:
        result: Any = _e_debt_valuation_adjustment_dva(**kwargs)
    except _INPUT_ERRORS as exc:
        raise HTTPException(
            status_code=status.HTTP_422_UNPROCESSABLE_ENTITY,
            detail=str(exc),
        ) from exc
    if not isinstance(result, dict):
        result = {"result": result}
    return OrjsonResponse(content=result)


@router.post(
    "/funding_valuation_adjustment_fva",
    response_model=FundingValuationAdjustmentFvaResponse,
    summary="Funding Valuation Adjustment Fva",
)
async def funding_valuation_adjustment_fva(
    body: FundingValuationAdjustmentFvaRequest,
    user: TokenPayload = Depends(get_current_user),
) -> OrjsonResponse:
    kwargs = body.model_dump()
    kwargs["expected_exposure"] = np.asarray(kwargs["expected_exposure"], dtype=np.float64)
    kwargs["time_steps"] = np.asarray(kwargs["time_steps"], dtype=np.float64)
    kwargs["discount_factors"] = np.asarray(kwargs["discount_factors"], dtype=np.float64)
    if kwargs["survival_probability"] is not None:
        kwargs["survival_probability"] = np.asarray(
            kwargs["survival_probability"], dtype=np.float64
        )
    try:
        result: Any = _e_funding_valuation_adjustment_fva(**kwargs)
    except _INPUT_ERRORS as exc:
        raise HTTPException(
            status_code=status.HTTP_422_UNPROCESSABLE_ENTITY,
            detail=str(exc),
        ) from exc
    if not isinstance(result, dict):
        result = {"result": result}
    return OrjsonResponse(content=result)


@router.post(
    "/capital_valuation_adjustment_kva",
    response_model=CapitalValuationAdjustmentKvaResponse,
    summary="Capital Valuation Adjustment Kva",
)
async def capital_valuation_adjustment_kva(
    body: CapitalValuationAdjustmentKvaRequest,
    user: TokenPayload = Depends(get_current_user),
) -> OrjsonResponse:
    kwargs = body.model_dump()
    kwargs["capital_profile"] = np.asarray(kwargs["capital_profile"], dtype=np.float64)
    kwargs["time_steps"] = np.asarray(kwargs["time_steps"], dtype=np.float64)
    kwargs["discount_factors"] = np.asarray(kwargs["discount_factors"], dtype=np.float64)
    try:
        result: Any = _e_capital_valuation_adjustment_kva(**kwargs)
    except _INPUT_ERRORS as exc:
        raise HTTPException(
            status_code=status.HTTP_422_UNPROCESSABLE_ENTITY,
            detail=str(exc),
        ) from exc
    if not isinstance(result, dict):
        result = {"result": result}
    return OrjsonResponse(content=result)


@router.post(
    "/margin_valuation_adjustment_mva",
    response_model=MarginValuationAdjustmentMvaResponse,
    summary="Margin Valuation Adjustment Mva",
)
async def margin_valuation_adjustment_mva(
    body: MarginValuationAdjustmentMvaRequest,
    user: TokenPayload = Depends(get_current_user),
) -> OrjsonResponse:
    kwargs = body.model_dump()
    kwargs["initial_margin_profile"] = np.asarray(
        kwargs["initial_margin_profile"], dtype=np.float64
    )
    kwargs["time_steps"] = np.asarray(kwargs["time_steps"], dtype=np.float64)
    kwargs["discount_factors"] = np.asarray(kwargs["discount_factors"], dtype=np.float64)
    try:
        result: Any = _e_margin_valuation_adjustment_mva(**kwargs)
    except _INPUT_ERRORS as exc:
        raise HTTPException(
            status_code=status.HTTP_422_UNPROCESSABLE_ENTITY,
            detail=str(exc),
        ) from exc
    if not isinstance(result, dict):
        result = {"result": result}
    return OrjsonResponse(content=result)


@router.post(
    "/xva_aggregation",
    response_model=XvaAggregationResponse,
    summary="Xva Aggregation",
)
async def xva_aggregation(
    body: XvaAggregationRequest,
    user: TokenPayload = Depends(get_current_user),
) -> OrjsonResponse:
    kwargs = body.model_dump()
    try:
        result: Any = _e_xva_aggregation(**kwargs)
    except _INPUT_ERRORS as exc:
        raise HTTPException(
            status_code=status.HTTP_422_UNPROCESSABLE_ENTITY,
            detail=str(exc),
        ) from exc
    if not isinstance(result, dict):
        result = {"result": result}
    return OrjsonResponse(content=result)


@router.post(
    "/cva_sensitivity_cva_greeks",
    response_model=CvaSensitivityCvaGreeksResponse,
    summary="Cva Sensitivity Cva Greeks",
)
async def cva_sensitivity_cva_greeks(
    body: CvaSensitivityCvaGreeksRequest,
    user: TokenPayload = Depends(get_current_user),
) -> OrjsonResponse:
    kwargs = body.model_dump()
    kwargs["expected_exposure"] = np.asarray(kwargs["expected_exposure"], dtype=np.float64)
    kwargs["time_steps"] = np.asarray(kwargs["time_steps"], dtype=np.float64)
    kwargs["discount_factors"] = np.asarray(kwargs["discount_factors"], dtype=np.float64)
    try:
        result: Any = _e_cva_sensitivity_cva_greeks(**kwargs)
    except _INPUT_ERRORS as exc:
        raise HTTPException(
            status_code=status.HTTP_422_UNPROCESSABLE_ENTITY,
            detail=str(exc),
        ) from exc
    if not isinstance(result, dict):
        result = {"result": result}
    return OrjsonResponse(content=result)


@router.post(
    "/wrong_way_risk_adjustment",
    response_model=WrongWayRiskAdjustmentResponse,
    summary="Wrong Way Risk Adjustment",
)
async def wrong_way_risk_adjustment(
    body: WrongWayRiskAdjustmentRequest,
    user: TokenPayload = Depends(get_current_user),
) -> OrjsonResponse:
    kwargs = body.model_dump()
    try:
        result: Any = _e_wrong_way_risk_adjustment(**kwargs)
    except _INPUT_ERRORS as exc:
        raise HTTPException(
            status_code=status.HTTP_422_UNPROCESSABLE_ENTITY,
            detail=str(exc),
        ) from exc
    if not isinstance(result, dict):
        result = {"result": result}
    return OrjsonResponse(content=result)


@router.post(
    "/credit_spread_curve_bootstrap",
    response_model=CreditSpreadCurveBootstrapResponse,
    summary="Credit Spread Curve Bootstrap",
)
async def credit_spread_curve_bootstrap(
    body: CreditSpreadCurveBootstrapRequest,
    user: TokenPayload = Depends(get_current_user),
) -> OrjsonResponse:
    kwargs = body.model_dump()
    kwargs["tenors"] = np.asarray(kwargs["tenors"], dtype=np.float64)
    kwargs["par_spreads"] = np.asarray(kwargs["par_spreads"], dtype=np.float64)
    try:
        result: Any = _e_credit_spread_curve_bootstrap(**kwargs)
    except _INPUT_ERRORS as exc:
        raise HTTPException(
            status_code=status.HTTP_422_UNPROCESSABLE_ENTITY,
            detail=str(exc),
        ) from exc
    if not isinstance(result, dict):
        result = {"result": result}
    return OrjsonResponse(content=result)


@router.post(
    "/cds_pricing_isda_standard",
    response_model=CdsPricingIsdaStandardResponse,
    summary="Cds Pricing Isda Standard",
)
async def cds_pricing_isda_standard(
    body: CdsPricingIsdaStandardRequest,
    user: TokenPayload = Depends(get_current_user),
) -> OrjsonResponse:
    kwargs = body.model_dump()
    kwargs["payment_times"] = np.asarray(kwargs["payment_times"], dtype=np.float64)
    kwargs["accrual_factors"] = np.asarray(kwargs["accrual_factors"], dtype=np.float64)
    kwargs["discount_factors"] = np.asarray(kwargs["discount_factors"], dtype=np.float64)
    try:
        result: Any = _e_cds_pricing_isda_standard(**kwargs)
    except _INPUT_ERRORS as exc:
        raise HTTPException(
            status_code=status.HTTP_422_UNPROCESSABLE_ENTITY,
            detail=str(exc),
        ) from exc
    if not isinstance(result, dict):
        result = {"result": result}
    return OrjsonResponse(content=result)


@router.post(
    "/cds_spread_to_pd_conversion",
    response_model=CdsSpreadToPdConversionResponse,
    summary="Cds Spread To Pd Conversion",
)
async def cds_spread_to_pd_conversion(
    body: CdsSpreadToPdConversionRequest,
    user: TokenPayload = Depends(get_current_user),
) -> OrjsonResponse:
    kwargs = body.model_dump()
    try:
        result: Any = _e_cds_spread_to_pd_conversion(**kwargs)
    except _INPUT_ERRORS as exc:
        raise HTTPException(
            status_code=status.HTTP_422_UNPROCESSABLE_ENTITY,
            detail=str(exc),
        ) from exc
    if not isinstance(result, dict):
        result = {"result": result}
    return OrjsonResponse(content=result)


@router.post(
    "/credit_default_swap_var",
    response_model=CreditDefaultSwapVarResponse,
    summary="Credit Default Swap Var",
)
async def credit_default_swap_var(
    body: CreditDefaultSwapVarRequest,
    user: TokenPayload = Depends(get_current_user),
) -> OrjsonResponse:
    kwargs = body.model_dump()
    try:
        result: Any = _e_credit_default_swap_var(**kwargs)
    except _INPUT_ERRORS as exc:
        raise HTTPException(
            status_code=status.HTTP_422_UNPROCESSABLE_ENTITY,
            detail=str(exc),
        ) from exc
    if not isinstance(result, dict):
        result = {"result": result}
    return OrjsonResponse(content=result)


@router.post(
    "/ifrs_9_stage_classification_pd_threshold",
    response_model=Ifrs9StageClassificationPdThresholdResponse,
    summary="Ifrs 9 Stage Classification Pd Threshold",
)
async def ifrs_9_stage_classification_pd_threshold(
    body: Ifrs9StageClassificationPdThresholdRequest,
    user: TokenPayload = Depends(get_current_user),
) -> OrjsonResponse:
    kwargs = body.model_dump()
    try:
        result: Any = _e_ifrs_9_stage_classification_pd_threshold(**kwargs)
    except _INPUT_ERRORS as exc:
        raise HTTPException(
            status_code=status.HTTP_422_UNPROCESSABLE_ENTITY,
            detail=str(exc),
        ) from exc
    if not isinstance(result, dict):
        result = {"result": result}
    return OrjsonResponse(content=result)


@router.post(
    "/ifrs_9_12_month_ecl_stage_1",
    response_model=Ifrs912MonthEclStage1Response,
    summary="Ifrs 9 12 Month Ecl Stage 1",
)
async def ifrs_9_12_month_ecl_stage_1(
    body: Ifrs912MonthEclStage1Request,
    user: TokenPayload = Depends(get_current_user),
) -> OrjsonResponse:
    kwargs = body.model_dump()
    try:
        result: Any = _e_ifrs_9_12_month_ecl_stage_1(**kwargs)
    except _INPUT_ERRORS as exc:
        raise HTTPException(
            status_code=status.HTTP_422_UNPROCESSABLE_ENTITY,
            detail=str(exc),
        ) from exc
    if not isinstance(result, dict):
        result = {"result": result}
    return OrjsonResponse(content=result)


@router.post(
    "/ifrs_9_lifetime_ecl_stage_2_3",
    response_model=Ifrs9LifetimeEclStage23Response,
    summary="Ifrs 9 Lifetime Ecl Stage 2 3",
)
async def ifrs_9_lifetime_ecl_stage_2_3(
    body: Ifrs9LifetimeEclStage23Request,
    user: TokenPayload = Depends(get_current_user),
) -> OrjsonResponse:
    kwargs = body.model_dump()
    kwargs["marginal_pd"] = np.asarray(kwargs["marginal_pd"], dtype=np.float64)
    kwargs["lgd"] = np.asarray(kwargs["lgd"], dtype=np.float64)
    kwargs["ead"] = np.asarray(kwargs["ead"], dtype=np.float64)
    kwargs["discount_factors"] = np.asarray(kwargs["discount_factors"], dtype=np.float64)
    try:
        result: Any = _e_ifrs_9_lifetime_ecl_stage_2_3(**kwargs)
    except _INPUT_ERRORS as exc:
        raise HTTPException(
            status_code=status.HTTP_422_UNPROCESSABLE_ENTITY,
            detail=str(exc),
        ) from exc
    if not isinstance(result, dict):
        result = {"result": result}
    return OrjsonResponse(content=result)


@router.post(
    "/ifrs_9_scenario_weighted_ecl",
    response_model=Ifrs9ScenarioWeightedEclResponse,
    summary="Ifrs 9 Scenario Weighted Ecl",
)
async def ifrs_9_scenario_weighted_ecl(
    body: Ifrs9ScenarioWeightedEclRequest,
    user: TokenPayload = Depends(get_current_user),
) -> OrjsonResponse:
    kwargs = body.model_dump()
    kwargs["scenario_ecls"] = np.asarray(kwargs["scenario_ecls"], dtype=np.float64)
    kwargs["scenario_weights"] = np.asarray(kwargs["scenario_weights"], dtype=np.float64)
    try:
        result: Any = _e_ifrs_9_scenario_weighted_ecl(**kwargs)
    except _INPUT_ERRORS as exc:
        raise HTTPException(
            status_code=status.HTTP_422_UNPROCESSABLE_ENTITY,
            detail=str(exc),
        ) from exc
    if not isinstance(result, dict):
        result = {"result": result}
    return OrjsonResponse(content=result)


@router.post(
    "/macroeconomic_overlays_ecl",
    response_model=MacroeconomicOverlaysEclResponse,
    summary="Macroeconomic Overlays Ecl",
)
async def macroeconomic_overlays_ecl(
    body: MacroeconomicOverlaysEclRequest,
    user: TokenPayload = Depends(get_current_user),
) -> OrjsonResponse:
    kwargs = body.model_dump()
    kwargs["macro_factors"] = np.asarray(kwargs["macro_factors"], dtype=np.float64)
    kwargs["sensitivities"] = np.asarray(kwargs["sensitivities"], dtype=np.float64)
    try:
        result: Any = _e_macroeconomic_overlays_ecl(**kwargs)
    except _INPUT_ERRORS as exc:
        raise HTTPException(
            status_code=status.HTTP_422_UNPROCESSABLE_ENTITY,
            detail=str(exc),
        ) from exc
    if not isinstance(result, dict):
        result = {"result": result}
    return OrjsonResponse(content=result)


@router.post(
    "/ifrs_9_staging_criteria_assessment",
    response_model=Ifrs9StagingCriteriaAssessmentResponse,
    summary="Ifrs 9 Staging Criteria Assessment",
)
async def ifrs_9_staging_criteria_assessment(
    body: Ifrs9StagingCriteriaAssessmentRequest,
    user: TokenPayload = Depends(get_current_user),
) -> OrjsonResponse:
    kwargs = body.model_dump()
    try:
        result: Any = _e_ifrs_9_staging_criteria_assessment(**kwargs)
    except _INPUT_ERRORS as exc:
        raise HTTPException(
            status_code=status.HTTP_422_UNPROCESSABLE_ENTITY,
            detail=str(exc),
        ) from exc
    if not isinstance(result, dict):
        result = {"result": result}
    return OrjsonResponse(content=result)


@router.post(
    "/credit_portfolio_optimisation",
    response_model=CreditPortfolioOptimisationResponse,
    summary="Credit Portfolio Optimisation",
)
async def credit_portfolio_optimisation(
    body: CreditPortfolioOptimisationRequest,
    user: TokenPayload = Depends(get_current_user),
) -> OrjsonResponse:
    kwargs = body.model_dump()
    kwargs["expected_returns"] = np.asarray(kwargs["expected_returns"], dtype=np.float64)
    kwargs["expected_losses"] = np.asarray(kwargs["expected_losses"], dtype=np.float64)
    try:
        result: Any = _e_credit_portfolio_optimisation(**kwargs)
    except _INPUT_ERRORS as exc:
        raise HTTPException(
            status_code=status.HTTP_422_UNPROCESSABLE_ENTITY,
            detail=str(exc),
        ) from exc
    if not isinstance(result, dict):
        result = {"result": result}
    return OrjsonResponse(content=result)


@router.post(
    "/credit_stress_testing",
    response_model=CreditStressTestingResponse,
    summary="Credit Stress Testing",
)
async def credit_stress_testing(
    body: CreditStressTestingRequest,
    user: TokenPayload = Depends(get_current_user),
) -> OrjsonResponse:
    kwargs = body.model_dump()
    kwargs["pd"] = np.asarray(kwargs["pd"], dtype=np.float64)
    kwargs["lgd"] = np.asarray(kwargs["lgd"], dtype=np.float64)
    kwargs["ead"] = np.asarray(kwargs["ead"], dtype=np.float64)
    try:
        result: Any = _e_credit_stress_testing(**kwargs)
    except _INPUT_ERRORS as exc:
        raise HTTPException(
            status_code=status.HTTP_422_UNPROCESSABLE_ENTITY,
            detail=str(exc),
        ) from exc
    if not isinstance(result, dict):
        result = {"result": result}
    return OrjsonResponse(content=result)


@router.post(
    "/altman_z_score_credit_scoring",
    response_model=AltmanZScoreCreditScoringResponse,
    summary="Altman Z Score Credit Scoring",
)
async def altman_z_score_credit_scoring(
    body: AltmanZScoreCreditScoringRequest,
    user: TokenPayload = Depends(get_current_user),
) -> OrjsonResponse:
    kwargs = body.model_dump()
    try:
        result: Any = _e_altman_z_score_credit_scoring(**kwargs)
    except _INPUT_ERRORS as exc:
        raise HTTPException(
            status_code=status.HTTP_422_UNPROCESSABLE_ENTITY,
            detail=str(exc),
        ) from exc
    if not isinstance(result, dict):
        result = {"result": result}
    return OrjsonResponse(content=result)


@router.post(
    "/logistic_regression_pd_model",
    response_model=LogisticRegressionPdModelResponse,
    summary="Logistic Regression Pd Model",
)
async def logistic_regression_pd_model(
    body: LogisticRegressionPdModelRequest,
    user: TokenPayload = Depends(get_current_user),
) -> OrjsonResponse:
    kwargs = body.model_dump()
    kwargs["features"] = np.asarray(kwargs["features"], dtype=np.float64)
    kwargs["defaults"] = np.asarray(kwargs["defaults"], dtype=np.float64)
    try:
        result: Any = _e_logistic_regression_pd_model(**kwargs)
    except _INPUT_ERRORS as exc:
        raise HTTPException(
            status_code=status.HTTP_422_UNPROCESSABLE_ENTITY,
            detail=str(exc),
        ) from exc
    if not isinstance(result, dict):
        result = {"result": result}
    return OrjsonResponse(content=result)


@router.post(
    "/machine_learning_pd_calibration",
    response_model=MachineLearningPdCalibrationResponse,
    summary="Machine Learning Pd Calibration",
)
async def machine_learning_pd_calibration(
    body: MachineLearningPdCalibrationRequest,
    user: TokenPayload = Depends(get_current_user),
) -> OrjsonResponse:
    kwargs = body.model_dump()
    kwargs["raw_scores"] = np.asarray(kwargs["raw_scores"], dtype=np.float64)
    kwargs["defaults"] = np.asarray(kwargs["defaults"], dtype=np.float64)
    try:
        result: Any = _e_machine_learning_pd_calibration(**kwargs)
    except _INPUT_ERRORS as exc:
        raise HTTPException(
            status_code=status.HTTP_422_UNPROCESSABLE_ENTITY,
            detail=str(exc),
        ) from exc
    if not isinstance(result, dict):
        result = {"result": result}
    return OrjsonResponse(content=result)


@router.post(
    "/through_the_cycle_pd_adjustment",
    response_model=ThroughTheCyclePdAdjustmentResponse,
    summary="Through The Cycle Pd Adjustment",
)
async def through_the_cycle_pd_adjustment(
    body: ThroughTheCyclePdAdjustmentRequest,
    user: TokenPayload = Depends(get_current_user),
) -> OrjsonResponse:
    kwargs = body.model_dump()
    try:
        result: Any = _e_through_the_cycle_pd_adjustment(**kwargs)
    except _INPUT_ERRORS as exc:
        raise HTTPException(
            status_code=status.HTTP_422_UNPROCESSABLE_ENTITY,
            detail=str(exc),
        ) from exc
    if not isinstance(result, dict):
        result = {"result": result}
    return OrjsonResponse(content=result)


@router.post(
    "/point_in_time_pd_estimation",
    response_model=PointInTimePdEstimationResponse,
    summary="Point In Time Pd Estimation",
)
async def point_in_time_pd_estimation(
    body: PointInTimePdEstimationRequest,
    user: TokenPayload = Depends(get_current_user),
) -> OrjsonResponse:
    kwargs = body.model_dump()
    try:
        result: Any = _e_point_in_time_pd_estimation(**kwargs)
    except _INPUT_ERRORS as exc:
        raise HTTPException(
            status_code=status.HTTP_422_UNPROCESSABLE_ENTITY,
            detail=str(exc),
        ) from exc
    if not isinstance(result, dict):
        result = {"result": result}
    return OrjsonResponse(content=result)


@router.post(
    "/ratings_migration_matrix",
    response_model=RatingsMigrationMatrixResponse,
    summary="Ratings Migration Matrix",
)
async def ratings_migration_matrix(
    body: RatingsMigrationMatrixRequest,
    user: TokenPayload = Depends(get_current_user),
) -> OrjsonResponse:
    kwargs = body.model_dump()
    kwargs["from_rating"] = np.asarray(kwargs["from_rating"], dtype=np.float64)
    kwargs["to_rating"] = np.asarray(kwargs["to_rating"], dtype=np.float64)
    try:
        result: Any = _e_ratings_migration_matrix(**kwargs)
    except _INPUT_ERRORS as exc:
        raise HTTPException(
            status_code=status.HTTP_422_UNPROCESSABLE_ENTITY,
            detail=str(exc),
        ) from exc
    if not isinstance(result, dict):
        result = {"result": result}
    return OrjsonResponse(content=result)


@router.post(
    "/retail_scorecard_pd_model",
    response_model=RetailScorecardPdModelResponse,
    summary="Retail Scorecard Pd Model",
)
async def retail_scorecard_pd_model(
    body: RetailScorecardPdModelRequest,
    user: TokenPayload = Depends(get_current_user),
) -> OrjsonResponse:
    kwargs = body.model_dump()
    kwargs["feature_values"] = np.asarray(kwargs["feature_values"], dtype=np.float64)
    kwargs["points_per_feature"] = np.asarray(kwargs["points_per_feature"], dtype=np.float64)
    try:
        result: Any = _e_retail_scorecard_pd_model(**kwargs)
    except _INPUT_ERRORS as exc:
        raise HTTPException(
            status_code=status.HTTP_422_UNPROCESSABLE_ENTITY,
            detail=str(exc),
        ) from exc
    if not isinstance(result, dict):
        result = {"result": result}
    return OrjsonResponse(content=result)


@router.post(
    "/corporate_credit_scoring_model",
    response_model=CorporateCreditScoringModelResponse,
    summary="Corporate Credit Scoring Model",
)
async def corporate_credit_scoring_model(
    body: CorporateCreditScoringModelRequest,
    user: TokenPayload = Depends(get_current_user),
) -> OrjsonResponse:
    kwargs = body.model_dump()
    kwargs["factor_scores"] = np.asarray(kwargs["factor_scores"], dtype=np.float64)
    kwargs["factor_weights"] = np.asarray(kwargs["factor_weights"], dtype=np.float64)
    try:
        result: Any = _e_corporate_credit_scoring_model(**kwargs)
    except _INPUT_ERRORS as exc:
        raise HTTPException(
            status_code=status.HTTP_422_UNPROCESSABLE_ENTITY,
            detail=str(exc),
        ) from exc
    if not isinstance(result, dict):
        result = {"result": result}
    return OrjsonResponse(content=result)


@router.post(
    "/sovereign_credit_risk_assessment",
    response_model=SovereignCreditRiskAssessmentResponse,
    summary="Sovereign Credit Risk Assessment",
)
async def sovereign_credit_risk_assessment(
    body: SovereignCreditRiskAssessmentRequest,
    user: TokenPayload = Depends(get_current_user),
) -> OrjsonResponse:
    kwargs = body.model_dump()
    try:
        result: Any = _e_sovereign_credit_risk_assessment(**kwargs)
    except _INPUT_ERRORS as exc:
        raise HTTPException(
            status_code=status.HTTP_422_UNPROCESSABLE_ENTITY,
            detail=str(exc),
        ) from exc
    if not isinstance(result, dict):
        result = {"result": result}
    return OrjsonResponse(content=result)


@router.post(
    "/sector_default_rate_analysis",
    response_model=SectorDefaultRateAnalysisResponse,
    summary="Sector Default Rate Analysis",
)
async def sector_default_rate_analysis(
    body: SectorDefaultRateAnalysisRequest,
    user: TokenPayload = Depends(get_current_user),
) -> OrjsonResponse:
    kwargs = body.model_dump()
    kwargs["sector_defaults"] = np.asarray(kwargs["sector_defaults"], dtype=np.float64)
    kwargs["sector_obligors"] = np.asarray(kwargs["sector_obligors"], dtype=np.float64)
    try:
        result: Any = _e_sector_default_rate_analysis(**kwargs)
    except _INPUT_ERRORS as exc:
        raise HTTPException(
            status_code=status.HTTP_422_UNPROCESSABLE_ENTITY,
            detail=str(exc),
        ) from exc
    if not isinstance(result, dict):
        result = {"result": result}
    return OrjsonResponse(content=result)

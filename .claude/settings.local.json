{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "bash -c 'BRANCH=$(git -C /workspace/pyvar branch --show-current 2>/dev/null); if [ \"$BRANCH\" = \"main\" ]; then echo \"[pyvar-hook] ERROR: Direct write to main blocked. Use feat/* or fix/*.\"; exit 1; fi'"
          }
        ]
      }
    ]
  }
}

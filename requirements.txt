# ============================================================
# requirements.txt — pyvar.com application dependencies
# Also installed at Docker build time (after requirements-heavy.txt).
# Separated so the heavy layer cache is not invalidated when
# adding a lightweight package here.
# ============================================================

# ── API gateway ───────────────────────────────────────────────
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
pydantic>=2.6.0
pydantic-settings>=2.2.0
python-jose[cryptography]>=3.3.0
slowapi>=0.1.9
orjson>=3.10.0
python-multipart>=0.0.9

# ── Task queue ────────────────────────────────────────────────
celery>=5.3.6
redis>=5.0.3

# ── Storage ───────────────────────────────────────────────────
sqlalchemy>=2.0.28
asyncpg>=0.29.0
boto3>=1.34.0
alembic>=1.13.0
psycopg2-binary>=2.9.9         # sync driver for Alembic migrations

# ── Observability ─────────────────────────────────────────────
prometheus-fastapi-instrumentator>=6.4.0
sentry-sdk[fastapi]>=1.43.0
structlog>=24.1.0

# ── Security ──────────────────────────────────────────────────
bandit[toml]>=1.7.8

# ── AWS CDK ───────────────────────────────────────────────────
aws-cdk-lib>=2.130.0
constructs>=10.0.0

# ── Testing & dev tooling ────────────────────────────────────
pytest>=8.1.0
pytest-asyncio>=0.23.0
pytest-cov>=5.0.0
pytest-benchmark>=4.0.0
httpx>=0.27.0
locust>=2.28.0
mypy>=1.10.0
ruff>=0.4.0
black>=24.4.0
isort>=5.13.0
